# 65 Dashboard Pages — Page→Card 현행화 완료보고서 (2026-06-17)

> 오너의 코드-없는 추적 채널. **무엇 / 왜 / 변경표 / 검증수치 / 다음**.

## 무엇

`docs/agent_specs/65_dashboard_pages_v1.0.md` 의 **PART I (§2~§9) 를 현 6 클라이언트 분석 페이지 기준으로 전면 현행화** + **§2.4 "Page → Card 한눈 매트릭스" 신설**. 사용자 요청 = *"어떤 페이지에 어떤 카드가 있는지"* 를 기준으로 60번대 프론트 문서를 현재 코드와 맞춤.

- **대상 페이지 (Sidebar `client` context 분석 6)**: 대시보드 `/dashboard` · 월간 결산 `/monthly` · 채널 `/channel` · 트렌드 `/trend` · 소재 `/creatives` · 비용 `/cost`
- **범위 결정 (사용자 선택)**: "page→card 중심 갱신" — §2 인벤토리 + §3 카드 구성 교체 + §2.4 매트릭스 신설 + §4~§7 페이지 참조 현행화. PART II·III(§10~§17 방법론·tool 로드맵)는 *미갱신*(별도 작업), caveat 로 명시.

## 왜

구 v1.0 (2026-05-27) 의 PART I 은 **2026-06-08 실데이터 재구축 이전의 mock 페이지**(`useMock*` / `/api/mock/*` / frontend 계산)를 박제하고 있었음 → 현 코드와 **거의 완전 불일치**. 헤더만 v1.1 인데 본문은 옛 mock 이라 자기모순 상태였음 (다른 채널의 부분 커밋 `7c76fb2` 잔재 포함).

## 변경표

| § | 구 v1.0 (mock 박제) | v1.1 (현 코드) |
|---|---|---|
| 제목/메타 | v1.0 | **v1.1** + PART I 현행화 경고 + PART II/III stale caveat |
| 입구 인덱스 | §2/§3 mock | §2 현 6 페이지 · **§2.4 신규** · §3 "카드 구성" |
| §2.1 인벤토리 | `dashboard1`·`dashboard_v1`·`/api/admin/pipelines/*` | 현 6 페이지 + `useXxxOverview`/`useMonthly*` → `/api/dashboard1/*` |
| §2.2 비분석 | `hitl`·`agent`(폐기됨) 포함 | client 2(workflow·conversations) + system 7 정합 |
| §2.3 N:M | 6×2 mock 매트릭스 | 6 페이지 공통 backend + `useCurrentClient` 분기 |
| **§2.4** | (없음) | **⭐ Page→Card 한눈 매트릭스 신규** — 페이지별 카드 렌더 순서 |
| §3 | "4 Layer 분해" (Dashboard1·useMock·AreaChart·RadarChart·AI추천…) | **"카드 구성"** — 6 페이지 × (카드·종류·표시내용·데이터 바인딩·상태분기) |
| §4 표시패턴 | AreaChart·PieChart·RadarChart 등 | MetricChainStrip·FunnelChart·ChannelComparison·PacingWidget·DataTable(히트/막대/footer) |
| §5 데이터흐름 | "두 흐름(Dashboard1+mock)" | 단일 흐름 + 페이지↔hook↔endpoint 매핑 |
| §6 하드코딩 | AI_RECOMMENDATIONS 등(소멸) | `PERIOD` 6곳·채널 매핑·**'페이싱(데모)' stale 라벨**·GradeDots VIP 선 미렌더 |
| §7 진화경로 | mock DATA_DIR fix(완료) | 전역 기간 선택기·라벨 정정·client 데이터 확충·PART II/III 갱신 |
| §8/§9 | — | v1.1 이력 1줄 + 트리거 현행화 |

### 페이지별 카드 (현 코드 검증 결과)

| 페이지 | 콘텐츠 카드 |
|---|---|
| 대시보드 | 마케팅 퍼널 사슬(MetricChainStrip) · 일별 ROAS(LineChart 목표·BE선) |
| 월간 결산 | Hero 사슬 + KPI 9 그리드 + MoM 4 + 광고비 5매체(Bar) + 채널분포(2표) + 회원·게스트 요약 + 등급 시계열(Line) + 등급표 + 연령 bucket(Bar) + 카테고리표 (10) |
| 채널 | 채널 비교(ChannelComparison small multiples) · 전환 퍼널(FunnelChart 3단계) |
| 트렌드 | 일별 ROAS(Line) · 일별 노출·전환(이중축 ComposedChart) |
| 소재 | 소재 성과 표(DataTable 8컬, ROAS 막대·히트·피로) |
| 비용 | KPI 3 · 채널 예산 비중(표) · 키워드 ROI(표) · 예산 페이싱(PacingWidget) |

## 검증 수치

- **6/6 페이지 코드 직독** (DashboardPage·MonthlyPage·ChannelPage·TrendPage·CreativePage·CostPage + sub-components + hooks).
- **workflow `wf_a203d962`**: 12 agent (6 추출 + 6 적대 검증), page→card 인벤토리 cross-check. 적대 검증이 잡은 항목:
  - monthly `useCurrentClient` 2종 분리 (헤더=데이터주도 / 데이터 hook=`clumi` 폴백)
  - trend 카드 2 = 이중 축 ComposedChart (단순 Bar 아님)
  - GradeDots `GRADE_KEYS` 에 VIP 있으나 VIP `<Line>` 미렌더
  - Cost 헤더 '페이싱(데모)' 라벨이 stale (실제는 실데이터 조인)
- **문서 정합성**: §0~§17 구조 무손상, 섹션 중복 0, PART I(1~480행) 잔존 mock 페이지 박제 0 (caveat 참조만 존재).
- **변경량**: 65 문서 151 insertions / 286 deletions (net −135, mock 장황 → 카드 집약).

## ⚠️ 사고 기록 (다른 채널 동시 편집)

작업 중 **다른 Claude 채널이 같은 65번 파일을 동시 편집** → 그 채널의 stale 버퍼 write 가 내 §3~§7 편집을 1회 덮어씀(유실). 사용자 확인(다른 채널 중단) 후 §3~§7 재적용으로 복구. 헤더는 "현행화 완료"인데 본문은 옛 mock 이던 자기모순 상태를 해소.

## 다음 (권장, §7.1)

1. **P1** 전역 기간 선택기 — 6 페이지 `const PERIOD='2026-04'` 하드코딩 제거.
2. **P1** Cost '페이싱(데모)' 라벨 정정 (실데이터 → '데모' 제거). 1줄.
3. **P2** GradeDots VIP 선 처리 (의도적 제외 명시 or 추가).
4. **P2** client 데이터 확충 (blooming/asyou/bluban raw) → N:M 실증.
5. **P2** PART II·III(§10~§17) 갱신 — 방법론·tool 로드맵의 페이지 매핑(K##)을 현 6 페이지로 재작성.
