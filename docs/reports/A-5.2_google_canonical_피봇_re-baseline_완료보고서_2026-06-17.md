# A-5.2 google canonical 피봇 — re-baseline 완료보고서 (2026-06-17)

> 커밋 `1fe59e8` · 상위 [ROADMAP §2 A-5](../_claude/ROADMAP.md) · 결정="가"(오너) · 직전 = A-5.1 ad_cost_helper 폐기(`2266a62`).

## 1. 무엇을 했나 (한 줄)
google_ads_performance를 canonical 광고 매체(6번째)로 포함시켜, 시스템 전역의 "정답"을 **총마케팅비 18,306,923 / MER 6.53 → 26,806,923 / 4.46** 으로 일괄 재기준(re-baseline).

## 2. 왜 (근본 연결)
- google은 **실채널**(data/clumi/raw/google_ads_performance.csv, 4월 180행·cost 8,500,000). 종전 canonical은 google을 "알려진 gap"으로 제외(G5) → 대시보드/에이전트가 실제보다 **낮은 마케팅비**를 답함 = 데이터 불일치 = 할루시 유발원.
- ROADMAP §0 본 목표(할루시 근본 = 데이터 엔지니어링)에 직결: canonical에 실채널을 정확히 반영 = 본선 수술.

## 3. 헤드라인 수치 (독립 검증됨)
| 지표 | 종전(5매체) | 신(6매체, google 포함) | 산식 |
|---|---|---|---|
| 총마케팅비 | 18,306,923 | **26,806,923** | + google 8,500,000 |
| MER / ROAS | 6.53 | **4.46** | 119,539,660 / 26,806,923 |
| CAC | 30,512 | **44,678** | 26,806,923 / 600 |
| promotion_roas | 2.37 | **1.62** | 43,400,360 / 26,806,923 |
| weighted_avg_cac | 30,512 | **44,678** | = CAC |
| total_ad_cost | 18,235,453 | **26,735,453** | (msg 71,470 불변) |
| tacos_pct | 15.25 | **22.37** | 26,735,453 / 119,539,660 |
| google by_channel | (없음) | **8,500,000** | raw cost Σ |

검증 경로: ① raw CSV 직접 산술(8,500,000·180행 전부 4월) ② 파일럿 run_pilot **9/9 PASS** ③ translator execute() in-memory ④ 라이브 적재 후 blended_computed.

## 4. 가드레일 준수 (오너 명시 3)
1. **파일럿 먼저**: data_pilot_project(pipeline·compute·run_pilot)에 google 추가 → 26.8M/4.46 검증 **후** production translator 수정.
2. **data layer만**: translator는 `self.fetch("google_ads_performance")`(FileDataSource, 이미 SOURCE_REGISTRY 등록)로만 접근. raw 직독 0. BOM·PK유일성 사전 검증.
3. **interface contract**: 레이어 I/O 계약 변경 없음(채널 추가는 spec 내부).

## 5. 변경 파일 (29, 커밋 `1fe59e8`)
- **파일럿(3)**: data_pilot_project/{pipeline,compute,run_pilot}.py — google 채널 + EXPECT 26.8M.
- **translator(1)**: canonical_translator.py — AD_CHANNELS + `_CHANNELS` google spec + `_m_google`.
- **라우트/스키마(3)**: canonical.py(`_AD_TABLES`/`_AD_CAMPAIGN` google), dashboard1.py·schemas/outputs/dashboard1.py(docstring 정답값).
- **catalog yaml(6)**: canonical_translator·ad_cost_total·roas_overall·cac_overall·promotion_roas·channel_cac_compare — golden_output 26.8M/파생.
- **tool docstring(5)**: ad_cost_total·roas_overall·cac_overall·promotion_roas·channel_cac_compare.
- **에이전트(1)**: clumi.yaml metric_glossary(ROAS 4.46·CAC 44,678).
- **테스트(10)**: canonical 5(test_canonical_translator·relational_load·pipeline_connection·marketing_performance·normalized_pivot_baseline) + dashboard1 5(ad_cost_total·metrics_marketing·outputs·route·comparison_4). EXPECT 상수·by_channel google·함수명 정합.

## 6. 검증 (회귀)
- **영향 103 테스트 전부 PASS** (canonical + dashboard1 re-baseline).
- **전체 1036 passed** (종전 997 → google 채널 단언 추가분 증가).
- **신규 실패 0.** 10 실패는 전부 pre-existing(실측 확인): parquet env(pyarrow 미설치) 4 · `_scratch/` 구조 drift 4 · `test_DC_PERM_6`(prompt_loader baseline) · `test_o04`(creative_id CR-015 정렬). 어느 것도 18.3M/26.8M/6.53/4.46/google 미참조.

## 7. 정직 경고 (잔여·과신 금지)
1. **dashboard1 운영 라우트는 옛 18.3M 서빙 유지.** `_cached_or_run`이 PostgresWorkspace `_workspace` 캐시(google 미포함)를 읽음. 테스트는 빈 캐시→라이브 tool=26.8M이라 통과하지만, **운영 5p는 캐시 cutover(C-2/A-3) 전까지 18.3M.** = 의도적 잔여 dual-world, A-3에서 해소.
2. **methodology 원본 문서(§S003 등)는 미갱신.** docstring에 "원안 5매체 18,306,923" 병기로 lineage 보존했으나, docs/ 방법론 SSOT 자체는 6매체 반영 안 됨(별도 문서 작업).
3. **format_normalizer 미삭제** — A-5.3 daily 전환과 동반 예정.
4. dashboard1 re-baseline은 "가" 결정의 연쇄(전환 5 tool이 canonical 공유). 운영 대시보드를 18.3M로 동결하려면 tool을 dual-world로 되돌려야 하므로(=할루시 재도입) **단일 세계 26.8M 채택**. 되돌리려면 기대값을 18.3M로 + dashboard1 tool 분기.

## 8. 다음
**A-5.3 daily_performance 4 tool → canonical 일별집계 전환** (파일럿 먼저). channel_aggregate·conversion_funnel·daily_performance_totals·daily_performance_aggregate가 옛 World-C(daily_performance.csv, 26.6M 미reconcile) 직독 → canonical(report_date) 일별집계로 전환 → format_normalizer 동반 삭제 → daily_performance.csv 폐기. 이후 A-3 DB 정상화(캐시 cutover) → A-7 TOOL 전체 구조 점검 → C2 에이전트 e2e.
