# 완료보고서 — [3] M1 멀티쿼리 수술 (S1~S3 + T3 재판정, 2026-06-12)

> 계획 = [계획_멀티쿼리 v2](계획_멀티쿼리_복합의도_수직슬라이스_2026-06-12.md) (오너 승인 3). 수술표 출처 = [측정_M0 보고서](측정_M0_멀티쿼리_e2e_완료보고서_2026-06-12.md) §3.
> 커밋: `c61f069` (S1·S2·S3) + `77d7aaf` (S1c insights 렌더 + 판정기 보정) + 본 보고서 커밋.

## 한 줄

"만들고도 안 보여주던" 표시 층을 수술해 **의도별 표출률 47.3% → 66.7% (+19.4%p — 합격 임계 3.2%p의 6배)**,
G7 기준질문 통과. 잔여 미달은 표시가 아니라 **인식·계획 축**으로 귀속 확정 (M1 범위 밖 — 백로그 박제).

## 수술 내역

| 수술 | 코드 | 내용 |
|---|---|---|
| S1 표시 합성 | `responder.py` build_display_payload | or-체인(1개 선택) 폐지 → 서술 산출(report/answer/recommendation) 전부 합성 + 서술이 말한 수치 중복 금지 |
| S1b 분해 렌더 | `responder.py` `_render_breakdowns` | rows 표·{그룹:값} dict 컴팩트 렌더 (cap 10행) — "채널별 ROAS" 표출 0%의 직접 수술 |
| S1c insights 렌더 | `responder.py` `_render_insights` | **T3-1 재실행이 적발**: insight_extractor 산출 키(insights)가 표시 어휘 밖 → '개선안' 침묵. 라운드 2가 경고한 함정 류의 실증 |
| S2 G19 의도 단위화 | `responder.py` build_missing_period_payload | ask 선두 불변(D3) + 완료 서술·insights·파일 공존 + 보류 분석 명시. 스코프 미정 *수치*는 계속 미표시 |
| S3 드롭 가시화 | `execution_stage.py` `_build_execution_result` + responder 고지 | 계획 대비 무기록 증발 todo → SKIPPED(not_executed) 등기 + "실행되지 않은 분석: ..." 고지 (G19 경로 포함) |

## T3 재판정 (동일 corpus 14쿼리 × 동일 3런, M0와 같은 측정기)

| 지표 | M0 (수술 전) | T3-2 (수술 후) | 판정 |
|---|---|---|---|
| ④ 표출률 (run-level mean) | 47.3% (45.2/48.4/48.4) | **66.7% (67.7/61.3/71.0)** | **+19.4%p > 임계 3.2%p ✓** |
| 표시 축 systematic | "채널별 ROAS" 등 7쿼리 0~50% | **"채널별 ROAS" 100% 등 표시 기인 청산** | ✓ |
| G7 ("매출+추천 둘 다") | 한쪽 탈락 실증 | **2/2런 완전 통과** (재실행 1회 0.75 — 계획 비결정) | ✓ |
| G8 ("한쪽 깨져도 침묵 금지") | 3/3런 깨짐·침묵 | ask+완료 의도 공존 / 사유 고지 (0.75) | 부분 ✓ |

판정기 보정 1건(투명 공개): 분해 렌더가 영문 데이터 키(channel/roas)를 노출 → 한국어 키워드 판정의 false-red.
판정기에 중첩 any-of 추가 + corpus 판정 어휘만 보정 (**쿼리 원문 14개 불변**).

## 잔여 미달의 귀속 (정직 고지 — DoD "전축 systematic 0"은 미달)

| 잔여 | 축 | 귀속 근거 | 처분 |
|---|---|---|---|
| "리뷰 키워드 뽑고 추천" 0% | **① 인식** | n_sub=0 — cognitive 빈 출력 (M0와 동일 재현) | S4 관찰 (v2 박제 그대로) |
| "소재별 CTR" 류 | **② 계획** | 소재별 *분해* tool 미선정 (avg 만) — G05 영역 | 백로그 (계획 축 후속) |
| G7 1런 0.75 | **② 계획** | 추천 의도가 plan 에서 탈락하는 런 (LLM 비결정) | 동상 |
| lv5 PDF 25% | 혼합 | G19 + 계획 탈락 + degrade 어휘 | 동상 |

**표시 층이 만든 것을 숨기는 기전은 청산** — 남은 빨간불은 "안 보여줌"이 아니라 "안 만듦/안 계획함"이며,
이는 M0 보고서가 처음부터 분리해 둔 축이다 (수술표 채택 시 S4=관찰로 오너 승인).

## 검증

- 박제 테스트 12 (`test_m1_display_merge.py`) + 슬라이스1·표시 계약 회귀 green
- 전체 suite **956 pass / 2 fail(기지 dc_perm·batch5 — household 는 복원으로 해소) / 2 skip** (test_I04 1회 flaky — 단독·모듈 통과 확인)
- 재현: `python -m scripts.agent_lang_diagnostics.measure_stage1_coverage --corpus corpus_compound.yaml --runs 3 --judge` (+ `corpus_g7g8.yaml`)

## 다음 (백로그 — 마스터 §4 합류)

1. 계획 축: 추천류 의도의 plan 탈락 (G7 비결정 런) — R2(sub_intents 본배선) 재평가의 1차 근거
2. 인식 축: lv4 추천류 cognitive 빈 출력 (S4 관찰 — fresh 측정으로 빈도 추적)
3. 소재별 분해 tool 선정 (G05 확장)
4. degrade 고지 어휘("진단") 표시 정합
