# A-7 TOOL 전체 구조 점검 + 게이트 27 감사 (2026-06-18)

> 워크플로 `w8477io1y`(5 에이전트·431k 토큰, census→triage→적대적 synthesis, 전부 file:line 실측). 상위 [ROADMAP §2 A-7 / 가로지름 gate](../_claude/ROADMAP.md). **이 문서 = 오너 결정 근거. 자동 변경 없음.**

## §0 ★★ 가장 중요한 발견 — 할루시는 2축이다 (로드맵 정정)
ROADMAP §0 가정: "데이터가 깨끗하면 gate 증식 멈춤"(gate=데이터 증상). **감사 결과 이 가정은 부분적으로 틀림:**
- **gate 28개 중 "데이터 수술로 측정근거 소멸"한 것 = 0개.** canonical cutover는 tool을 하나도 안 지우고 데이터소스만 재지정했고, period:'all' 오염원 5곳(utm_normalizer·member_guest_stats·category_multi_distributor·ad_cost_total·channel_attribution)은 **전부 live**(가드 유지).
- gate들은 **World-B '데이터'가 아니라 데이터소스-무관 메커니즘**을 겨냥: "LLM이 스코프 param을 빠뜨림 → optional-period tool이 전기간으로 넓어짐 → 0건 → silent-0(거짓 보고서)".

→ **할루시 = 2축.** ①**데이터 정확성**(canonical로 ✅해결) ②**에이전트 planning 취약성**(스코프 누락·빈입력 지어내기·부분실행 둔갑 — gate가 지킴, **여전히 필요**). canonical은 ①만 고쳤고 gate는 ②를 지킨다. **"데이터 고치면 gate 줄어든다"는 두 축을 혼동한 것.** 남은 할루시 본 작업(C2)은 ②축이다.

## §1 A-7 TOOL 전체 구조 점검 — 오너 4문항 답
**Q3(재구축 vs 점검) 먼저 = ★점검 후 정합(refine), 완전 재구축 반대.** 근거: 데이터·tool 이미 canonical 전환 완료(깨진 토대 없음), [[project_normalized_pivot_scope]] "빅뱅·카테고리 재설계 ✗"와도 정합.

- **Q1 카테고리**: 11개 "수정"(축소+정합). ★핵심 = **3중 진실 불일치**: `catalog/_schema.yaml`(정식 8) ↔ 디스크(11, decision/qa/rendering 미등재) ↔ team_catalog(또 다름). → 9~10으로 정합. 카테고리 재설계가 본질 아님, **3중 drift 봉합이 우선**.
- **Q2 구성**: 4 응집 문제 — ①**metrics 35개 비대**(전체 38%): 정제 위장 4개(ad_cost_total·ga4_session_aggregator·category_multi_distributor·member_guest_stats가 yaml에 `clumi_methodology:cleaning` 자인) + 카운트 패스스루 혼재 ②**정제 개념이 3 카테고리 파편화**(cleaning·metrics·normalization) ③normalization 3 이질책임 ④analysis 3 책임묶음(llm reasoning·ml text·대시보드 mock 카드).
- **Q4 방향**: 정합 먼저(_schema 봉합) → 중복 1건 통합 → methodology 정렬(폴더=결 1:1) → gate 정리 → dimension 흡수(P5). 모두 data_pilot 파일럿 검증 후·한 턴 ONE 변경.

**살릴/수정/삭제:**
- **KEEP**: collection 22(단일책임 모범)·rendering 3·report 2·canonical_translator(6 tool 의존 = 최강 load-bearing)·순수 KPI군·실제 정제 2·comparison·analysis llm/ml.
- **★정정(KEEP)**: daily_performance 4 tool·ad_cost_total — census는 "World-C 직독·할루시 유발·은퇴"로 봤으나 **STALE**. 실측상 이미 A-5.3 canonical 전환 완료(daily_performance_totals.py:17,28 `load_canonical_ad_rows` / ad_cost_total.py:37-40 canonical delegate). **재구축 대상 아님.**
- **MODIFY(분류 정합)**: _schema taxonomy 8→9·cleaning-rule tool들 metrics→cleaning 회수·grade_timeseries comparison→metrics·review_normalizer→text·missing_value_diagnostic→validation. (코드 아닌 **분류/네이밍** 정합)
- **DELETE 후보**: ai_recommendation/recommender 중 1개(동일 ml_model 엔진) → **단, 적대검증서 반박(§3 결정)**.

## §2 게이트 감사 — 28 → 현역 23 (코드 안전장치 전부 유지)
| 처리 | 수 | 내용 |
|---|---|---|
| **retire(강등)** | 2 | G01(clarification_question)·G08(validate_dag) = **미배선**(신호 생산하나 소비자 0·로그only, grep 확인). 코드 삭제 아니라 대장 '현역'→'planned' 배지. 배선 시 재등재 |
| **merge(대장 통합)** | 5→2 | A: G11+G12(executor 입력 위생) · B: G13+G14(LLMTool silent-0 가드) · C: G16+G17(state 평면분리). 같은 함수·기준을 입구/출구로 쪼갠 쌍 → 대장 1행씩. **코드 안전장치 전부 유지** |
| **keep** | 17 | G09·G10·G18·G20·G21·G28 등 — canonical과 직교한 실 안전장치 |

- **retire_count = 2** (데이터 무관·미배선). **measurement 소멸 retire = 0.**
- ★ **과도은퇴 위험(실재)**: G11을 "World-B 소멸"로 retire하면 오염원 5곳이 live라 **silent-0 재발**. 반드시 keep.
- "gate 증식 멈춤"의 진짜 의미 = 새 gate 안 만들고 **미배선 배선완료 + silent-0 변종 통합** (데이터정합이 아니라).

## §3 ★ 적대적 교차검증 — 오너 결정/주의
1. **[BLOCKER·결정 필요] ai_recommendation/recommender 통합** — triage는 "1개 삭제" 권고했으나, `recommender.py:5-6` docstring이 **"중복 아님: 카드 vs 대화 에이전트 두 surface가 같은 ml 엔진 위임"**으로 명시 방어. 둘 다 실배선(ai_recommendation→대시보드 O05 카드 / recommender→decision_agent 대화). 같은 엔진+출력 facade는 ad_cost_total이 canonical_translator를 쓰는 **KEEP 패턴과 동일**. 삭제하면 배선된 surface 파손 → **어느 surface를 끊을지는 오너 결정**(코드만으로 판단 X). 권장: **유지**(통합 이득 < 파손 위험).
2. **[WARN] G04→G03 merge는 코드머지 불가** — `ensure_interpretation_fed.py:402-403`이 "해석tool은 consumes 미선언이라 G03이 못 잡는 케이스를 G04가 전담" 명시. **대장 문서 그룹핑만** 허용, 코드 통합 시 빈입력 보고서 재발.
3. **[WARN] census의 daily/ad_cost 위험평가 STALE** — 이미 canonical 전환됨. census 단독 참조 시 불필요 대규모 손질 위험 → 본 문서가 정정 박제.
4. **[INFO·선행] _schema.yaml 3중 drift 봉합이 모든 후속의 단일 기반** — gate 정리·dimension 흡수 전에 이걸 먼저.

## §4 권장 다음 순서
1. **_schema.yaml taxonomy 봉합** (3중 진실 → 1, ONE 변경) ← 모든 후속 기반
2. **gate 대장 정리** (28→23: 미배선 2 강등 + 중복 5→2 문서통합, 코드 유지) — 대장 문서 작업, 저위험
3. **methodology 분류 정합** (cleaning-rule tool 회수 등) — 파일럿 검증 후
4. **dimension normalizer 4종**은 P5 canonical dimension_maps 흡수 후 은퇴 (선삭제 금지)
5. → **C2 에이전트 e2e·할루시 ②축 검증** (본 목표): 데이터(①)는 끝났으니 planning 취약성(②)을 라이브로 관찰

> ai_recommendation/recommender 통합·daily tool 추가 손질은 §3 반박으로 **보류 권장**. 재구축 아닌 정합이 결론.
