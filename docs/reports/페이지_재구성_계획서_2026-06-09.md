# 페이지 재구성 — 계획서 (2026-06-09)

> 사용자 요청: ① 포트폴리오 → "시스템" 명명 + 5 페이지 이동 / ② 에이전트 + 승인대기 페이지 삭제.
> 결정: 옵션 1 (보수, 한 commit) + fullscreen 옵션 A (Maximize2 폐기).

## 1. 의도

| 측면 | 결정 |
|---|---|
| TopBar 토글 | "포트폴리오" 라벨 → **"시스템"** (type `'portfolio'` 그대로, 라벨만) |
| CLIENT → SYSTEM 이동 | 리포트 · 메모리 · 에이전트 관찰 · System · DB (총 5) |
| 삭제 | `/agent` (AgentChatPage) · `/hitl` (HitlCenterPage) — 폴더/라우트/탭 모두 |
| SideChatPanel Maximize2 | 버튼 + `onFullScreen` prop 폐기 (옵션 A) |
| 적용 범위 | 한 commit, 30~40분 |

## 2. 신 SYSTEM_TABS / CLIENT_TABS 매트릭스

### 신 SYSTEM_TABS (5)

| 그룹 | 항목 (id · label · path) |
|---|---|
| 포트폴리오 | `portfolio · 포트폴리오 · /portfolio` |
| 리포트 | `report · 리포트 · /report` |
| 관찰 | `agent-observability · 에이전트 관찰 · /agent-observability` · `memory · 메모리 · /memory` |
| 시스템 | `system-console · System · /system` · `data-console · DB · /db` |

→ ~~hitl (`/hitl`)~~ = 삭제. PORTFOLIO_TABS 에서 제거.

### 신 CLIENT_TABS (8 — 17→8, -9)

| 그룹 | 항목 |
|---|---|
| 분석 (6) | dashboard · monthly · channel · trend · creatives · cost |
| AI (2) | workflow · conversations |

→ ~~agent~~ 삭제 / ~~memory · agent-observability~~ 이동 / ~~hitl · report~~ (리뷰 그룹) 삭제+이동 / ~~system-console · data-console~~ (시스템 그룹) 이동.

## 3. 4 작업 단위

### 3.1 agent 삭제

| 변경 | 위치 |
|---|---|
| 파일 삭제 | `features/agent/AgentChatPage.tsx` |
| 라우트 삭제 | `routes/router.tsx` — `agentRoute` + import + `addChildren` |
| 탭 삭제 | `features/navigation/store.ts` — CLIENT_TABS 의 `agent` |
| Sidebar ICON_MAP | `MessageSquare` 사용처 확인 — TopBar 의 AI 버튼이 사용 → **보존** |
| GlobalLayout `onFullScreen={() => onNavigate?.('/agent')}` | 호출 자체 폐기 |
| SideChatPanel `onFullScreen` prop + `Maximize2` 버튼 | 폐기 (옵션 A) |

**보존 (의존성 있음)**: `features/agent/{SideChatPanel·ChatTodoCard·PauseBox·Attachments·SlideView·PhaseIndicator·chatPanelStore·store}.tsx` — useWebSocket / GlobalLayout / agent_observability 가 사용.

### 3.2 hitl 삭제

| 변경 | 위치 |
|---|---|
| 폴더 삭제 | `features/hitl/` 통째 (HitlCenterPage · PlanReviewModal · store · store.test) — 외부 의존성 **0** 확인 |
| 라우트 삭제 | `routes/router.tsx` — `hitlRoute` + import |
| 탭 삭제 | `store.ts` — PORTFOLIO_TABS · CLIENT_TABS 둘 다 |
| Sidebar ICON_MAP | `UserCheck` 다른 사용처 0 → 함께 제거 |

### 3.3 PORTFOLIO_TABS → SYSTEM_TABS 재구성

| 변경 | 위치 |
|---|---|
| 라벨 변경 | TopBar `CONTEXTS` 의 `label: '포트폴리오'` → `'시스템'` |
| 상수명 변경 | `store.ts` `PORTFOLIO_TABS` → `SYSTEM_TABS` (변수명 통일) |
| 5 페이지 추가 | report·memory·agent-observability·system-console·data-console |
| CLIENT_TABS 축소 | 위 5 항목 제거 (분석 6 + workflow + conversations 만) |
| type `'portfolio'` | **그대로 유지** (localStorage 호환). 자취 주석 추가 |

### 3.4 router.tsx — 17 → 15 라우트

| 항목 | 변경 |
|---|---|
| 라우트 수 | 17 → **15** (-2: agent, hitl) |
| import 제거 | `AgentChatPage`, `HitlCenterPage` |
| `addChildren` | `agentRoute`, `hitlRoute` 제거 |
| 자취 주석 | 2026-06-09 갱신 |

## 4. 영향 / 위험 / 검증

| 항목 | 평가 |
|---|---|
| 외부 의존성 | hitl 0 / agent 1 (router.tsx 만, agent feature 의 다른 컴포넌트는 보존) |
| localStorage migration | type `'portfolio'` 유지로 불필요 |
| build 영향 | TS 에러 가능성 — agent/hitl import 잔존 시 |
| audit:tokens | 기존 5 룰 통과 유지 — 새 위반 없음 예상 |
| visual 변화 | TopBar 토글 라벨 + Sidebar 그룹 재배치 (큰 변화) |

## 5. 미래 변경 용이성 — 박제

[tag] 패턴 컨벤션 + 자취 주석 + spec 64 §6 자취 = 페이지 추가/삭제 시 **3 곳 손대면 됨** (페이지 파일 + router.tsx + store.ts). 페이지 빈번 변경 시에도 부담 적음.

본 작업의 변경 폭이 일회성으로 큰 이유 = **컨텍스트 의미 자체 재정의** (포트폴리오 → 시스템) 때문. 일회성 정리 후 미래 추가/삭제는 다시 단순.

## 6. 자취

- **2026-06-09 (계획서)** : 본 문서
