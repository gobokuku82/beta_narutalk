# 계획 — 작업 ⑰ metrics 35 등재 (invisible 52→17, Q1 본질 진단 67% 해소)

> **상태**: **v3** (2차 적대적 검증 = minor_fix_then_proceed). **사용자 승인 대기**.
> v1 → v2 → v3: 1차 13 + 2차 11 = 24 변경. 3차 우회 (ROI 限界).

---

## 0. 메타 + 본질 진단 정정

### 0.1 메타

| 항목 | 값 |
|---|---|
| 작업 번호 | ⑰ |
| 작업명 | metrics 35 등재 (신 metrics_agent) |
| 작성일 | 2026-06-01 (v3) |
| 패턴 | 작업 ⑤·⑨·⑪·⑫·⑭ |
| 분량 | 中 (**6 commit (계획서 v3 + Phase 0~3 = 5 작업) · 약 21 파일 · 약 250 lines**) — 2차 정정 §0.1↔§4 통일 |
| 후속 | ⑲ 자동 sync (우선순위 격상, ⑰ Phase 3 직후 kickoff) + ⑳ 잔존 17 + DAG audit |

### 0.2 본질 진단 Q1 정량

| 시점 | invisible | 해소율 |
|---|---:|---:|
| 작업 ⑱ 후 (현 baseline) | 52 | 31.6% |
| **작업 ⑰ 후** | **17** | **77.6%** |
| 잔존 17 (⑳ 후속) | 0 | 100% |

---

## 1. 사용자 결정 7 (페어 박제 정합)

| # | 결정 | 권장 |
|---|---|---|
| D1 | agent | **B 신 metrics_agent** |
| **D2** | TaskType | **`METRIC_CALCULATION` 추가** + **`GoalType.METRIC` 유지** (페어, 1차 critical) |
| D3 | Phase 0 분리 | **분리** |
| **D4 (2차 명확화)** | Pattern B 메타 | **per-tool yaml 본문 무변경 (회귀 테스트 의존)**. team_catalog 측 entry 4-key (name/description/handles_tasks/produces) 만 reflect, clumi_methodology/answer_values/storage 메타는 catalog 측 strip |
| D5 | ⑲ 자동 sync | **⑰ 선행 + 우선순위 격상**. ⑰ Phase 3 commit 직후 ⑲ 계획서 kickoff (사용자 결정 surface — §12 D5 확인) |
| D6 | Stage 3 example | **+1** (광고 KPI) |
| D7 | token 부담 | **OK** |

### 1.1 D2 페어 박제 (1차 critical 해소)

KPI 질의 ("4월 ROAS 알려줘") = **goal.type=METRIC + task=METRIC_CALCULATION** (페어). GoalType.METRIC 死코드 폐기 = 본 ⑰ scope 외.

---

## 2. 현황 spot-check (2차 정정)

### 2.1 metrics 35 = 2 패턴 (2차 정정 — 라벨 정확화)

| 패턴 | 카운트 | **실 기준** (2차 정정) | 부수 특성 |
|---|---:|---|---|
| Pattern A (compact) | 19 | **clumi_methodology 박제 0** | K-code, 메타 없음 |
| Pattern B (heavyweight) | 16 | **clumi_methodology 박제** | S-code, answer_values/storage 메타 풀세트 |

> **2차 정정**: K/S-code = 부수 특성. 기준은 `clumi_methodology` 박제 유무.
> Pattern A 19 중 **client default = 12** (Phase 0 scope), 나머지 7 = label/unit default.

### 2.2 7 sub-domain (합계 35)

| domain | 카운트 | 패턴 |
|---|---:|---|
| campaign KPI (K10-K13) | 5 | A |
| creative KPI (K18-K20, ab_test) | 5 | A |
| cost/budget/channel (budget_stacked 포함) | 8 | A |
| revenue/ROAS (S004 등) | 4 | B |
| member/segment | 6 | B |
| cleaning hybrid | 4 | B |
| conversion | 3 | B |
| **합** | **35** | — |

### 2.3 12 yaml `client default=clumi` (Phase 0 scope, 2차 정합)

> 1차 정정 명단:

1. ab_test_table 2. budget_channel_share 3. budget_stacked 4. budget_totals 5. campaigns_table 6. channel_aggregate 7. conversion_funnel 8. creative_cards 9. daily_performance_aggregate 10. daily_performance_totals 11. keyword_metrics_avg 12. keyword_top_roas

### 2.4 dependencies 빈/누락 (35 모두) — 별 후속 (DAG audit)

### 2.5 TaskType + GoalType 현 상태

[structured_query.py:21-62](../../backend/app/dream_agent/schemas/structured_query.py#L21-L62):

- **TaskType** = **17** → 추가 후 **18**
- **GoalType** = 6 (페어 유지, METRIC active)

### 2.6 team_catalog 현 구조 (실측)

| 항목 | 값 |
|---|---|
| 총 entry | **52** |
| analysis_team agents | 8 (collection 22 + cleaning 3 + text_preprocessing 1 + channel_normalizing 2 + analysis 5 + report_text 2 + pdf 5 + ppt 3) |
| creative_team agents | 4 / 9 tool |
| **합** | **12 agent / 52 entry** |
| invisible | 52 (metrics 35 + comparison 7 + analysis 6 + normalization 4) |

### 2.7 회귀 risk = low (2차 정정 — frontend grep 명령 정확화)

#### Phase 0 frontend audit (2차 critical 정정)

**v2 오류**: `grep -rn 'client.*clumi' frontend/src/` → 실측 **16 hit** (mock + 주석 + 안내). v2 "0 hit 기대" 단정 = false-alarm risk.

**v3 정정 (명령 정확화)**:
```bash
# 의미: tool yaml 직접 호출 hit 0 확인 (mock/주석/K-code 무관 hit 사전 박제)
# (a) 좁힌 패턴 — 실 tool API 호출만
grep -rn '/api/tools.*clumi\|fetch.*client.*clumi' frontend/src/ 2>/dev/null
# 기대: 0 hit

# (b) 의미 검증 — useDashboard1Data.ts 가 /api/dashboard1 만 호출하는지 수동 확인
grep -n "/api/dashboard1" frontend/src/api/dashboard1.ts frontend/src/features/dashboard1/*.ts 2>/dev/null
# 기대: dashboard1 endpoint 호출만 (tool yaml 직접 의존 0)
```

→ **실측 박제**: useDashboard1Data.ts = /api/dashboard1 직접 호출 (tool yaml 의존 0) → Phase 0 frontend 영향 0 **확정**.

---

## 3. 변경 명세 (6 commit, 2차 정정 commit message 박제)

### 단계 0 — 진입 직전 spot-check (2차 정정)

```bash
# 1. 카운트 baseline
ls backend/app/dream_agent/tools/catalog/metrics/*.yaml | wc -l   # 35
grep -l 'default: clumi' backend/app/dream_agent/tools/catalog/metrics/*.yaml | wc -l   # 12

# 2. enum baseline
cd backend && uv run python -c "
from app.dream_agent.schemas.structured_query import TaskType, Source, GoalType
print(f'TaskType: {len(TaskType)} (기대 17)')
print(f'Source: {len(Source)} (기대 17)')
print(f'GoalType: {len(GoalType)} (기대 6)')
"

# 3. Phase 0 frontend 영향 (2차 정정 — 명령 정확화)
grep -rn '/api/tools.*clumi\|fetch.*client.*clumi' frontend/src/ 2>/dev/null
# 기대: 0 hit (tool yaml 직접 호출)
```

### Phase 0 (commit 1) — client default 12 yaml cleanup

**commit message** (2차 정정): `refactor(catalog): ⑰.A client default 12 yaml cleanup (POC convention)`

12 yaml 의 `client` parameter entry 제거. recent ①.6c 정합.

### Phase 1 (commit 2) — TaskType + cognitive.yaml (페어 박제)

**commit message**: `refactor(schemas): ⑰.B TaskType.METRIC_CALCULATION + cognitive.yaml (페어 박제, GoalType.METRIC 유지)`

#### 1.1 [structured_query.py:21-51](../../backend/app/dream_agent/schemas/structured_query.py#L21-L51)

```python
class TaskType(str, Enum):
    DATA_COLLECTION       = "data_collection"
    DATA_PREPROCESSING    = "data_preprocessing"
    SENTIMENT_ANALYSIS    = "sentiment_analysis"
    KEYWORD_EXTRACTION    = "keyword_extraction"
    TREND_ANALYSIS        = "trend_analysis"
    COMPETITOR_COMPARISON = "competitor_comparison"
    CAUSAL_ANALYSIS       = "causal_analysis"
    METRIC_CALCULATION    = "metric_calculation"   # ⑰ 신 (페어: GoalType.METRIC)
    # ... (기존 9 무변경)
```

→ TaskType 17 → **18**. **GoalType.METRIC 유지** (active).

#### 1.2 cognitive.yaml 갱신

- L74 "ONLY these 17 values" → "**18 values**" + METRIC_CALCULATION 박제
- §3 매핑 표 행 추가 (1줄, 표 형식 통일, 2차 low 정정):
  ```
  | "ROAS / CAC / 매출 / 광고비 등 KPI 수치 질의" | metric | text |
  ```
- few-shot 예시 신규 (광고 KPI, source=meta)

### Phase 2a (commit 3) — team_catalog metrics_agent + Stage 1·2 prompt (2차 정정 — yaml 구조 + Stage 2 exclusion)

**commit message**: `refactor(catalog+prompts): ⑰.C metrics_agent 신설 + Stage 1·2 prompt`

#### 2a.1 [team_catalog.yaml](../../backend/app/dream_agent/planning/catalog/team_catalog.yaml) metrics_agent 신설

위치: cleaning_agent 다음, text_preprocessing_agent 앞.

```yaml
metrics_agent:
  description: "KPI 메트릭 계산 (35 tool)"
  handles_tasks: [metric_calculation]
  # ⑰ (2026-06-01) 신설 — metrics 100% 가시화. description = per-tool yaml 단일 진실 (D6).
  tools:
    # 35 entry × 5-6 lines = ~180 lines, per-tool yaml 그대로 reflect
    ...
```

#### 2a.2 implicit_prerequisites 정정 (2차 critical 정정 — yaml 구조 list-of-dicts)

> **v2 오류**: `metric_calculation: [data_collection, data_preprocessing]` dict-mapping = 실 schema 와 mismatch.
> **v3 정정**: 기존 첫 dict 의 `tasks_requiring_data` enum 에 `metric_calculation` **append** (prerequisites 동일).

**파일**: [team_catalog.yaml:474-485](../../backend/app/dream_agent/planning/catalog/team_catalog.yaml#L474-L485)

```yaml
# 현재
implicit_prerequisites:
  - tasks_requiring_data:
      [sentiment_analysis, keyword_extraction, trend_analysis,
       competitor_comparison, causal_analysis, insight_generation]
    prerequisites: [data_collection, data_preprocessing]

# v3 정정 (metric_calculation enum append, list-of-dicts 정합)
implicit_prerequisites:
  - tasks_requiring_data:
      [sentiment_analysis, keyword_extraction, trend_analysis,
       competitor_comparison, causal_analysis, insight_generation,
       metric_calculation]   # ⑰ 신 — KPI = 정제 raw 필요
    prerequisites: [data_collection, data_preprocessing]
```

> **2차 박제**: `planner.py` 미소비 → 문서 가치만 (자동 enforcer 후속).

#### 2a.3 task_agent_hints 갱신

```yaml
task_agent_hints:
  ...
  metric_calculation: metrics_agent   # ⑰ 신
```

#### 2a.4 [planning_stage1_team.yaml](../../backend/app/dream_agent/llm_manager/prompts/planning_stage1_team.yaml#L25-L27) (1차 high)

```yaml
# L25-27 분석 계열 enum 에 metric_calculation 추가
· 분석 계열 (sentiment_analysis, keyword_extraction, trend_analysis,
  competitor_comparison, causal_analysis, metric_calculation,   # ⑰ 신
  insight_generation, report_generation, summary_generation,
  data_collection, data_preprocessing) → analysis_team
```

#### 2a.5 [planning_stage2_agent.yaml](../../backend/app/dream_agent/llm_manager/prompts/planning_stage2_agent.yaml#L25-L27) (1차 high + 2차 exclusion)

```yaml
# 신 rule + exclusion 명시 (2차 medium 정정)
- task_type=metric_calculation → agents_selected 에 metrics_agent 추가
  + collection_agent 의무 선행
  + cleaning_agent (정제 raw 필요 시)
  + channel_normalizing_agent (광고 출처 시)
  # ⚠ exclusion (v3 신설): metric_calculation 은 분석 계열 text_preprocessing_agent 강제에서 제외
  # — text 정제 불요 (raw → cleaning → normalizing → metrics chain)
```

### Phase 2b (commit 4) — Stage 3 prompt 분기 + example

**commit message**: `refactor(prompts): ⑰.D Stage 3 분기 + 광고 KPI example`

#### 2b.1 [planning_stage3_todo.yaml](../../backend/app/dream_agent/llm_manager/prompts/planning_stage3_todo.yaml) 분기

```yaml
# ⑰ (2026-06-01) metrics 분기
- targets.source + task=metric_calculation → metrics_agent 35 tool 중 source/KPI 매칭:
  · revenue/ROAS → revenue_total / roas_overall / promotion_revenue
  · CAC/회원 → cac_overall / grade_revenue / new_members_monthly
  · 광고비 → ad_cost_total / budget_totals / daily_performance_*
  · 캠페인 KPI → campaign_active_count / campaigns_table / campaign_*
  · creative KPI → creative_count / creative_ctr_avg / creative_cards / ab_test_table

# Stage 3 example 신규 (광고 KPI, source=meta)
- {id: "todo_001", task_type: "data_collection", ..., tool: "orders_collector", ...}
- {id: "todo_002", task_type: "metric_calculation", ..., tool: "revenue_total", tool_params: {period: "2026-04"}, ...}
```

> **2차 박제 (Phase 2a → 2b forward dependency)**: 2a commit 후 2b commit 전 — Stage 1·2 가 metrics_agent 선택해도 Stage 3 분기 부재 = tool 매핑 fallback (default tool=revenue_total). **권장 = 2a+2b 가 same session 연속 실행** (manual smoke test 는 2b 후 1회).

### Phase 3 (commit 5) — ADR-027 §3 박제 (doc-only, 2차 정확화)

**commit message**: `docs(adr): ⑰.E ADR-027 §3 박제 (metrics drift 100% 해소)`

ADR-027 §3 박제 row 추가 + 잔존 작업 표 갱신:

```
✅ 작업 ⑰ (2026-06-01) 부분 해소 — metrics 35 등재 (신 metrics_agent) + TaskType.METRIC_CALCULATION + cognitive.yaml + Stage 1·2·3 prompt.
   GoalType.METRIC + TaskType.METRIC_CALCULATION 페어 (KPI 질의).
   metrics 카테고리 dual-source drift 100% 해소 (invisible 52 → 17, Q1 진단 77.6%).

잔존 (별 작업):
- ⑲: ToolRegistry → team_catalog 자동 sync (⑰ Phase 3 직후 kickoff)
- ⑳: comparison 7 + analysis 6 + normalization 4 = 17 등재
- DAG audit: collection → cleaning → metrics 3-hop dependencies
- K-code/S-code taxonomy ADR
```

→ 잔존 작업 표에서 기존 ⑰ 줄 **제거** (2차 should 정정 — 정합).

---

## 4. 영향 범위 (6 commit / 약 21 파일, 2차 정정 §0.1 통일)

| 영역 | 파일 | commit |
|---|---|---|
| 계획서 | 본 파일 v3 | 0 |
| 백엔드 catalog yaml | metrics/*.yaml 12 (client param) | 1 (Phase 0) |
| 백엔드 schema + LLM prompt | structured_query.py + cognitive.yaml | 2 (Phase 1) |
| 백엔드 catalog + LLM prompt | team_catalog.yaml + planning_stage1/2 | 3 (Phase 2a) |
| LLM prompt | planning_stage3_todo.yaml | 4 (Phase 2b) |
| ADR | ADR-027 §3 | 5 (Phase 3) |
| **합** | **약 21 파일 / 6 commit** | — |

### 4.2 무영향

- ToolRegistry 분포 (85) / 분석 team / dashboard1 / sprint15 / frontend type-check baseline 변동 0
- **GoalType.METRIC** (유지)
- ADR-027 §1 권한 매트릭스 (정합)

---

## 5. 단계별 commit (6 commit, 2차 baseline 표기 통일)

| commit | Phase | 회귀 baseline (2차 정정 통일) |
|---|---|---|
| 0 | plan v3 | doc-only |
| 1 | Phase 0 | dashboard1 (303/3, pyarrow) + ToolRegistry 85 + sprint13 단독 (190/0/6) + sprint13+14 통합 (293/11/2) + frontend grep 정확 명령 0 hit |
| 2 | Phase 1 | sprint13 단독 (190/0/6) + TaskType 18 + GoalType 6 유지 + Pydantic round-trip |
| 3 | Phase 2a | sprint13+14 통합 (293/11/2) + _load_catalog drift (metrics 35 → 0) + Stage 1·2 매핑 |
| 4 | Phase 2b | sprint13 단독 (190/0/6) + manual smoke (광고 KPI Stage 3 분기 검증) |
| 5 | Phase 3 | doc-only |

---

## 6. 미해결 결정 (2차 정합)

§1 결정 표 single source. D4 명확화 (2차) + D5 ⑲ kickoff surface (2차 user_decision §12).

### 6.3 후속 작업

- **⑲ 자동 sync** — ⑰ Phase 3 commit 직후 kickoff (사용자 surface §12 D5)
- ⑳ 잔존 17 등재
- DAG audit (collection → cleaning → metrics 3-hop)
- K-code/S-code taxonomy ADR

---

## 7. 회귀 baseline 검증 명령 (2차 정정 통일)

```bash
# 0. 단계 0 + Phase 0 사전
cd backend && uv run python -c "
from app.dream_agent.schemas.structured_query import TaskType, Source, GoalType
print(f'TaskType: {len(TaskType)} (기대 17)')   
print(f'GoalType: {len(GoalType)} (기대 6)')   
"
# Phase 0 frontend (2차 정정 — 정확 명령)
grep -rn '/api/tools.*clumi\|fetch.*client.*clumi' frontend/src/ 2>/dev/null
# 기대: 0 hit (tool yaml 직접 호출)

# 1. Phase 0 후
cd backend && uv run pytest tests/{pipelines,dashboard1,data_sources,workspace,permissions,ml_models} -q
# 기대: 303/3 (pyarrow)
cd backend && uv run pytest tests/sprint13 -q   # 기대: 190/0/6 단독
cd frontend && pnpm exec tsc --noEmit   # exit 0

# 2. Phase 1 후
cd backend && uv run python -c "
from app.dream_agent.schemas.structured_query import TaskType, GoalType
assert len(TaskType) == 18, f'{len(TaskType)} != 18'
assert len(GoalType) == 6, f'{len(GoalType)} != 6'  # METRIC 유지
print('TaskType 18 / GoalType 6 OK')
"

# 3. Phase 2a 후
cd backend && uv run pytest tests/sprint13 tests/sprint14 -q
# 기대: 293/11/2 통합
cd backend && uv run python -c "
from app.dream_agent.planning.planner import _load_catalog
cat = _load_catalog()
metrics_agent = cat['teams']['analysis_team']['agents'].get('metrics_agent', {})
print(f'metrics_agent tools: {len(metrics_agent.get(\"tools\", []))}')   # 35
"

# 4. Phase 2b 후 — drift + manual smoke
cd backend && uv run python -c "
from app.dream_agent.planning.planner import _load_catalog
from app.dream_agent.tools.registry import get_registry
cat = _load_catalog()
metrics_agent = cat['teams']['analysis_team']['agents']['metrics_agent']
reg = get_registry(); reg.load()
metrics_reg = {t.name for t in reg.get_all() if t.category.value == 'metrics'}
metrics_cat = {t['name'] for t in metrics_agent['tools']}
print(f'invisible: {sorted(metrics_reg - metrics_cat)}')   # 기대: []
"
# manual smoke: '4월 ROAS' Cognitive → Planning → Execution chain LLM 1회 (비용 ~$0.01)
```

---

## 8. rollback (Phase 2a → 2b forward dependency, 2차 정정)

| revert | 영향 | cross 의존 |
|---|---|---|
| 5 (ADR) | doc-only | 무관 |
| 4 (2b Stage 3) | Stage 3 분기 폐기 → 2a 단독 잔존 = LLM Stage 3 분기 0, **default tool fallback 발생 가능** | 무관 |
| 3 (2a catalog + Stage 1·2) | metrics_agent 폐기, Stage 1·2 복귀 | 2 (Phase 1) 단독 잔존 = TaskType 18 + agent 미존재 = Planner ValidationError → 함께 revert 필수 |
| 2 (Phase 1) | TaskType 17 복귀 | 3 (2a) handles_tasks 미정의 → 함께 revert |
| 1 (Phase 0) | 12 yaml client param 복귀 | 무관 |

---

## 9. 검증 체크리스트 (사용자 승인)

### 9.1 정확성 (2차 정정)
- [x] §2.1 Pattern A/B 라벨 = clumi_methodology 박제 (K/S-code 격하)
- [x] §2.3 12 yaml 명단 (Phase 0 scope)
- [x] §2.5 TaskType 17 baseline / GoalType 6 유지
- [x] §2.6 team_catalog 52 entry / 12 agent
- [x] §2.7 frontend grep 명령 정확화 (`/api/tools.*clumi`)

### 9.2 완전성 (1차+2차 critical 해소)
- [x] **GoalType.METRIC 페어 박제** (1차 critical)
- [x] **implicit_prerequisites yaml 구조 list-of-dicts** (2차 critical) — 기존 enum append
- [x] **frontend grep 명령 정확화** (2차 critical) — false-alarm 차단
- [x] Stage 1·2 prompt + cognitive.yaml L74 17→18
- [x] Stage 2 metric_calculation exclusion (text_preprocessing 강제 제외)

### 9.3 실행 안전
- [x] Phase 2a → 2b forward dependency 박제 (2b 후 1회 manual smoke)
- [x] commit message subject convention (recent ⑭/⑱ 패턴)
- [x] sprint13 단독 (190/0/6) vs sprint13+14 통합 (293/11/2) 통일 표기
- [x] §0.1 ↔ §4 commit 수 (6 commit) 통일

### 9.4 사용자 원칙
- [x] convention 우선 (description per-tool yaml)
- [x] 기본값 없음 (Phase 0 cleanup)
- [x] 死코드 폐기 — 신중 (GoalType.METRIC 페어)
- [x] v1/v2 섞임 금지
- [x] POC over-engineering 회피
- [x] tool / data / agent 분리
- [x] 한 turn ONE 변경 (Phase 분리, atomic 강화)
- [x] 전문가 단일 권장

### 9.5 의존성
- [x] 작업 ⑭+⑱ 의존
- [x] 후속 ⑲ (우선순위 격상) · ⑳ · DAG audit 박제

---

## 10. 참조

- 측정 workflow (2026-06-01, 9 agent)
- 1차 적대적 검증 (5 agent) — major_revision, 13 → v2
- **2차 적대적 검증** (5 agent) — minor_fix_then_proceed, 11 → v3, 3차 우회 (ROI 限界)
- 작업 ⑭ v3 / 작업 ⑱
- ADR-022 / ADR-027 / ADR-014 v2
- 메모리 [feedback_convention_over_hardcoding] / [project_poc_single_client_clumi] / [한 turn ONE 변경]
- compact v5

### 10.1 compact v6 trigger

작업 ⑭+⑰+⑳ 완료 후 작성.

---

## 11. 변경 이력

| 버전 | 날짜 | 내용 |
|---|---|---|
| v1 | 2026-06-01 | 최초 (4 commit + 계획서) |
| v2 | 2026-06-01 | 1차 major_revision. must 6 (GoalType 페어 박제 + Stage 1·2 + TaskType 17→18 + 명단 정정 + Phase 분할 + implicit_prerequisites) + should 4 + optional 3 = 13. 5 commit + 계획서 |
| v3 | 2026-06-01 | 2차 minor_fix_then_proceed. must 4 (implicit_prerequisites yaml 구조 + frontend grep 명령 + Pattern A/B 라벨 + baseline 표기) + should 4 (Stage 2 exclusion + commit message + forward dependency + ADR-027 잔존 줄 제거) + optional 3 = 11. **6 commit / 약 21 파일** |

---

## 12. 사용자 결정 surface (v3 최종 — 2차 신설)

| # | 결정 | 적용 |
|---|---|---|
| D1 | agent | B 신 metrics_agent |
| D2 | TaskType + GoalType | METRIC_CALCULATION + METRIC 유지 (페어) |
| D3 | Phase 0 분리 | 분리 |
| D4 | Pattern B 메타 (2차 명확화) | per-tool yaml 본문 무변경 + catalog 측 4-key reflect (메타 strip) |
| **D5 (2차 surface)** | ⑲ 자동 sync kickoff | **⑰ Phase 3 commit 직후 ⑲ 계획서 kickoff 자동** (비전공자 surface) |
| D6 | Stage 3 example | +1 |
| D7 | token 부담 | OK |
| (1차) | Phase 2 분할 (2a + 2b) | 분할 |
| (1차) | Stage 1·2 prompt 갱신 | 필수 |
| **(2차 신설)** | **Phase 2a atomic boundary** | **5 영역 단일 commit** (catalog + task_agent_hints + implicit_prerequisites + Stage 1 + Stage 2). 분할 시 ⑭ 패턴 — 사용자 결정 |
| **(2차 신설)** | **Phase 2b manual smoke** | **필수** (광고 KPI Stage 3 분기 LLM 검증, 비용 ~$0.01) |

**기본 = 모든 권장 적용**. 다른 결정 시 surface 후 v4 갱신.

---

**상태**: v3 작성 완료. **다음 단계**: 사용자 승인 → commit 0 (계획서 v3) → 단계 0 spot-check → Phase 0 진입.
