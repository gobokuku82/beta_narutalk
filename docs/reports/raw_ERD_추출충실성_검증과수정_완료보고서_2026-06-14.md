# raw→computed ERD 추출 충실성 검증 + 결함 수정 — 완료보고서 (2026-06-14)

> 오너 지시: "아래 문서가 raw에서 제대로 추출돼서 만들어졌는지부터 확인 … raw→computed ERD 다시 체크하고 진행".
> 대상: erd_octorad_raw_v1.0.{dbml,md} · octorad_raw_metadata_v0.1.{yaml,md} · 컬럼설명. 결정: 결함 1+3 수정(오너 승인).

---

## 1. 무엇을 했나

**전수 자동 diff 검증기**(`docs/_claude/data/erd/verify_raw_erd.py`, 신규)로 실 raw 재추출→발행본 대조. 발견된 결함 중 **1(타입)·3(17자리)을 수정**, 2(GA4 커버리지)는 후속.

## 2. 검증 결과 — ★ 핵심: 사전(stale)이 아니라 실 raw에서 추출됨

| 검사 | 결과 |
|---|---|
| 테이블 34 | raw == dbml == metadata 완전 일치 ✓ |
| ERD dbml 컬럼 path | raw 대비 누락 0·잉여 0 — 완전 충실 ✓ (naver `ccnt`/`statDt`/`nccCampaignId` 올바름 = 사전 `convCnt`/`stat_dt`/`camp_id` drift **미상속**) |
| metadata path | raw 충실 + dict drift를 `semantic_note`로 명시 기록 ✓ (내 dict_gate 18건과 독립 일치) |
| normalized/computed 레이어 | 재생성 diff 0 = 최신 ✓ (검증된 contract+materialized에서 생성) |

> 컬럼명·존재·관계 = 100% 충실. 결함은 전부 *파생 속성*(추론 타입/커버리지/설명텍스트).

## 3. 수정 내역

### 결함 1 — 타입 오분류 (10 컬럼) ✓ 수정
- **근본 원인**: `extract_raw_schema.py:_scalar_type` 의 `\d{8} → date(yyyymmdd)` 가 8자리 KRW 금액을 날짜로 오인.
- **근본 수정**: `_valid_yyyymmdd()` 추가 — 8자리가 *실제 달력 날짜*(연 2018~2030·월 1~12·일 1~31)일 때만 date. 금액은 varchar(타 CSV 숫자컬럼과 동일 컨벤션). `event_date`(실날짜 20260401) = date 유지 검증.
- **발행본 반영**: `erd_octorad_raw_v1.0.{dbml,md}` 의 10 컬럼 `date [note:'date(yyyymmdd)']`→`varchar`. 대상: budget_allocation 5예산·campaigns.monthly_budget·category_sales(order_amount·new_buyer_amount·repeat_buyer_amount)·household_structure.total_revenue_krw.

### 결함 3 — campaign_id 설명 "17자리" → "18자리" (6곳) ✓ 수정
- 생성 소스 `_wf_meta_{meta,naver,demo}.json` 7곳 수정 → `assemble_metadata.py` 재생성 → 발행 yaml/md 6곳 반영. (raw 실측 18자리, A2와 동일. path는 이미 정정돼 있었고 설명 텍스트만 잔존했음.)

### 결함 2 — metadata 커버리지 175 컬럼 미기재 = 후속 (오너 결정)
- GA4(143)+talktalk(32) 깊은 중첩(`collected_traffic_source.*`·`manual_campaign.*`·`message_blocks.*` 등 대부분 null/분석무관). 712/893=80%. 비용 대비 가치 낮아 보류.

## 4. 변경표

| 파일 | git | 변경 |
|---|---|---|
| `docs/agent_specs/ERD/erd_octorad_raw_v1.0.dbml` | ✅커밋 | 10 컬럼 date→varchar |
| `docs/agent_specs/ERD/erd_octorad_raw_v1.0.md` | ✅커밋 | §3 임베드 dbml 동일 10 |
| `docs/agent_specs/ERD/octorad_raw_metadata_v0.1.yaml` | ✅커밋 | 17→18 6곳 |
| `docs/agent_specs/ERD/octorad_raw_metadata_v0.1.md` | ✅커밋 | 17→18 6곳 (+ 외부검증 행 = 동일) |
| `docs/_claude/data/erd/extract_raw_schema.py` | gitignore | `_valid_yyyymmdd` 근본수정 |
| `docs/_claude/data/erd/assemble_metadata.py` | gitignore | 외부검증 행 생성기化(수동편집 박제) |
| `docs/_claude/data/erd/_wf_meta_*.json` | gitignore | 17→18 source |
| `docs/_claude/data/erd/verify_raw_erd.py` | gitignore | ★신규 검증기 |

> ERD 빌드 툴체인은 전부 `docs/_claude/`(gitignore) — 기존 컨벤션. 발행 산출물만 커밋.

## 5. 검증 수치 (✓ 실행)

- verify_raw_erd: **타입버그 0 · 17자리 0** · 34테이블 일치 · dbml path 0 drift → ✅ 충실
- 발행 dbml 잔존 amount-as-date = 0 · `event_date` date 유지
- data_pilot gate **OVERALL ✅ PASS** (run_pilot 8/8·verify 12/12·coverage·dict_gate) — ERD 수정이 pilot 무영향(독립) 회귀 확인
- git diff: 32 ins/32 del = 의도(타입 10+10·17→18 6+6)와 정확히 일치, 의도외 변경 0

### ⚠ 작업 중 잡은 자기 결함 2건 (정직 기록)
1. verify_raw_erd 초판 [C]가 **vacuous green** — dbml 타입토큰('date')만 보고 note 속 'yyyymmdd'를 못 봐 0개 검사하고 ✓. raw 인벤토리 타입 기준으로 수정 → 실제 10건 적발.
2. 메타 재생성이 발행본의 **수동 추가 행('외부 검증')을 순삭제** → git diff로 포착 → 생성기에 박아 복원(재생성 안전화).

## 6. 다음

- raw→computed ERD 충실성 = **검증 완료**(구조 건전, 결함 1·3 수정, 2 후속). 진행 가능.
- 갈림길 복귀: **P2**(GA4 native·reconciliation·ctr/cvr/cpc) / **Ⓑ tools 전환**(pilot 레퍼런스→채널 translator+config) / 결함 2(GA4 커버리지) 보강.

## 변경 이력
| 날짜 | 내용 |
|---|---|
| 2026-06-14 | raw ERD 추출 충실성 전수검증(verify_raw_erd 신규) + 결함 1(타입 10)·3(17자리 6) 수정. 구조 100% 충실 확인. |
