# ERD/ 새 세계 오염 감사 + 라벨 청소 — 완료보고서 (2026-06-14)

> 오너 우려: "ERD/에 새 세계(World A, raw→canonical)를 깨끗이 신축 중인데 기존 tools(World B)가 자꾸 섞인다. 오염되면 안 됨. **다시 시작해야 하나 점검할 것.**"

---

## 1. 판정: ★ 다시 시작 불필요 (restart 아님) — 구조 오염 0건

16파일 적대검증 워크플로(34 에이전트) 결과:

| 분류 | 건수 | 의미 |
|---|--:|---|
| **contamination** (구조/네이밍/경로/코드를 새 설계 일부로 채택 → 재작업 필요) | **0** | 없음 |
| **minor_coupling** (World-B 수치를 검증 oracle·주석으로 soft 참조) | 17 | 동작 무영향 |

- **두 권위 SPEC(`octorad_canonical_contract.yaml`·`conversion_config.yaml`)는 구조적으로 100% 깨끗**: 자체 영문 snake_case measure + 독립 cluster ID(A/M/I/T). S코드 테이블구조·한글컬럼·clumi_cleaned/computed 경로 = **0건 채택**.
- 17 minor_coupling 전부 동일 패턴: World-A가 canonical 입력에서 **독립 재계산**(검증 8/8·12/12) → 옛 정답(18,306,923·119,539,660·6.53) 재현 여부를 **검증 oracle/주석으로 대조**. = 오너가 과제에서 직접 minor_coupling 예시로 든 그 패턴.
- 진짜 World-B 구조물(format_normalizer·ad_cost_helper·blooming·`광고비(원)`)은 **전부 `referrence/` 진단문서 or "폐기 demo용" 명시 거부** = legit reference (채택 아님).

> "자꾸 섞인다"의 정체 = pilot이 World-B 산출값을 **검증 oracle**로 쓴 것(cross-validation, 엔지니어링상 정상)이 문서에 `=S003` 라벨로 박혀 섞인 *모습*이 됨. 값이 아니라 라벨이 문제.

## 2. 조치: 전체 라벨 청소 (오너 선택)

S코드/methodology **라벨만** 제거, EXPECT 검증값(18.3M·6.53·119.5M)은 회귀 테스트 정답키라 **유지**(라벨만 변경). 구조 변경 0.

| 파일 | 변경 |
|---|---|
| `octorad_canonical_contract_v0.1.yaml:150` | mer formula 주석 `= 우리 '전사 ROAS 18.3M 분모'` → `(blended ROAS)` — 권위 SPEC의 유일 접점 |
| `erd_octorad_canonical_layers_v0.1.md` | `=S003`·`=S001`·`=S004` → `(MER 분모)`·`(MER 분자)`·`(신뢰)` (생성소스 gen_layer_docs.py 수정 후 재생성) |
| `octorad_metric_registry_v0.1.md` | `=옛 '18.3M 분모' 6.53` → `blended ROAS = 6.53`; `분모=18.3M(=total_ad_cost) 확정` → `분모=total_ad_cost` |
| `octorad_pilot_verification_v0.1.md:10` | `(ad+msg=S003)` → `(ad+msg)` |
| `compute.py`·`run_pilot.py`·`README.md` | `methodology S003/S001/S004` 라벨 → `2026-04 mock 재계산 기준값`. EXPECT 값 유지 |

> 유지(손대지 않음): `광고비(원) 폐기 demo용`·`format_normalizer 폐기 예정`·referrence/ 진단문서 = legit reference(명시적 거부 근거라 제거 시 "왜 안 쓰는지" 사라짐). `C6.3`=World-A 자체 설계 규칙이라 유지. INDEX:60 `분모 18.3M`=값(라벨 아님) 유지.

## 3. 검증 (✓ 실행)
- 발행 ERD/ + pilot 코드 S코드/methodology 라벨 잔존 = **0건** (referrence/ 제외, `__pycache__` stale 바이트코드 제외)
- data_pilot gate **OVERALL ✅ PASS** (8/8·12/12·coverage 배지) — 라벨 청소가 동작에 무영향
- 재생성 문서 diff = 라벨 변경분만 (값 18,306,923·119,539,660·6.53 보존)

## 4. 남은 점검 항목 (직전 갭체크 §)
오염 점검과 별개로, 갭체크에서 확정된 잔여(오너 결정 대기):
- **설계미스**: normalized/computed ERD가 raw처럼 **발행 .dbml 부재**(canonical_layers.md 임베드만) / `acquisition_mer`가 미정의 measure 참조 / computed가 11 metric 중 3 구현
- **누락**: normalized/computed 독립 metadata 문서 없음(임베드만) / description 공란 5 / computed 방법론 절 없음

> 즉 **오염은 해소(restart 불필요·라벨 청소 완료)**. 단 "새 세계 완성도"(ERD .dbml·metadata 독립문서·미구현 metric)는 별도 잔여로 남음.

## 변경 이력
| 날짜 | 내용 |
|---|---|
| 2026-06-14 | 오염 감사(16파일 적대검증) → restart 불필·구조오염 0 판정. 17 minor_coupling 라벨 청소(값 유지). gate PASS. |
