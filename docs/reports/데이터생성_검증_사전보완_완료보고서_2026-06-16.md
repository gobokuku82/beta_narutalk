# 완료보고서 — 데이터 생성 검증 + 데이터사전 보완 (2026-06-16)

> 세부계획 [01 데이터 생성/보완](../_claude/plans/normalized_pivot_세부01_데이터_생성보완_2026-06-16.md)의 데이터층 실행·검증. 오너 지시 "먼저 데이터생성부터 이후 검증".
> ⚠ 이번 작업 산출물 대부분이 gitignored(`data/` 전체·`docs/_claude/`) → **본 보고서가 유일 git-추적 기록**.

## 한 줄
**데이터 생성 = google 1건이 유일했고 이미 완료 — 결정론 byte-identical 실증.** 새로 생성할 데이터 없음(나머지 원천은 raw 실재 → 추출=P2). 데이터사전의 canonical 원천 7컬럼 누락만 보완.

## 무엇을 했나
1. **google 생성 결정론 검증** — 생성기 재실행 → SHA256 before==after (byte-identical), git diff 0. cost 정확히 8,500,000·180행·6 campaign.
2. **8 measure 원천 raw 실재 재확인** — meta `inline_link_clicks`=388 / kakao `open_count`=1059·`click_count`=191·`total_cost_krw`=29120 / talktalk 516·88·6225 / advoost `view_through_conversions` / orders `is_first_order`. **JSON summary 키 직접 실측**(이전엔 grep만).
3. **데이터사전 보완(local)** — raw엔 있으나 사전 미기재였던 canonical 원천 7컬럼 등재: meta `inline_link_clicks` + kakao/talktalk 각 `summary.total_cost_krw`·`open_count`·`click_count`. 의미는 contract 인용.
4. **gate 회귀 검증** — dict_gate 누락 **133→126**(정확히 −7), OVERALL **PASS 유지**(run_pilot 8/8·verify 12/12·coverage·dict_gate).

## 적대 검증으로 잡은 것 (정직)
- **false positive 1건 철회**: "google이 36 설계doc에 미등재 = gap" → **틀림**. 36 doc은 *대시보드 demo 7종 전용*이고 canonical 벤더 소스(meta/naver/advoost/kakao/talktalk)는 36에 *원래 없음*(ERD/contract가 home). google도 그 convention대로 등재됨 = 올바른 home. 36에 넣었으면 demo로 오분류될 뻔.
- **사전 vs raw 혼동 해소**: 데이터사전엔 kakao `open_rate`(%)만 있어 "open_count 부재"로 의심 → JSON 실측 결과 raw엔 `open_count`·`open_rate` *공존*. 즉 raw는 완비, **사전이 불완전**이 진실. → 세부01 §2 "8 원천 전부 실재" 주장 **재확인(정확)**.

## 인프라 발견
- **`data/` 전체 gitignored**(36 doc §0 명시). google CSV·데이터사전 = **local**. git 추적 권위 = 36 doc + ERD dbml + contract + schema(backend). 7e17c24의 "google committed"는 schema/contract/dbml/dict-description이고 CSV 자체는 local.
- 카탈로그 nit: 데이터사전 file_no **18 충돌**(crm_messages.sql ↔ google_ads_performance.csv).

## 오너 영역 — 손대지 않음
- dict_gate **DRIFT 18**(campaign_id "17자리" vs raw 18자리 등 사전 오기) = ★사전 *값 정정*은 오너 영역 → 보고만(비차단). 나는 *누락 컬럼 추가*(사실 lineage)만 함.
- blocked 4(`msg_delivered`·`open_rate`·`click_rate`·`ctor` 분모) = 외부 doc 정의 공백 → fabricate 금지, P2서 명시 라벨.

## 다음
데이터층 = **생성·검증 완료, P2 추출 준비됨.** 새 생성 없음. 다음 실작업 = [02 tool 구현] P1(받침대)→P2(canonical_translator + google 6채널 통합 + measure 추출 + re-baseline 18.3M→26.8M). 오너 승인 시 착수.
