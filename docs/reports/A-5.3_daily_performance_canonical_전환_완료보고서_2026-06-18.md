# A-5.3 daily_performance → canonical 전환 완료보고서 (2026-06-18)

> 커밋 `b9dd42d`(4 tool) · `a020901`(라우트) · `83b8d60`(csv 폐기). 상위 [ROADMAP §2 A-5](../_claude/ROADMAP.md). 매핑 워크플로 `wmtg52y0k`.

## 1. 무엇 (한 줄)
옛 `daily_performance.csv`(World-C 별개 mock, 진짜 raw와 미reconcile)를 읽던 4 tool + 운영 라우트 3곳을 canonical(AD 4채널 normalized) 집계로 전환하고, csv·로더·SourceSpec을 폐기.

## 2. 왜
daily_performance.csv는 **가짜 요약본**이었음(실측 증거):
- 8일치(04-01~08)인데 총액이 한 달치와 비슷, 채널 배분도 raw와 전혀 다름(google 10.9M vs 진짜 8.5M).
- campaign_id가 가짜(`BRP-001` 등 = campaigns.csv 예산코드)라 예산 조인을 위조 — 진짜 플랫폼 ID(`GADS...`·`120210...`)와 0 매칭.
→ 화면이 진짜 데이터와 어긋남 = 할루시 유발원. canonical 단일진실로 통일.

## 3. 전환 결과 (canonical, 옌 csv와 다름 = 정합)
| tool/라우트 | 옛 csv | canonical |
|---|---|---|
| daily_performance_totals | imp 1,548,400·conv 2,010·cost 26,579,010 | **imp 4,834,911·conv 1,695·cost 26,735,453** |
| channel_aggregate | google/kakao/meta/naver·roas % | **advoost/google/meta/naver_sa·roas 배수**(kakao=메시징 제외) |
| conversion_funnel | (csv 합) | **4,834,911→63,659→1,695** |
| daily_performance_aggregate | 8일 | **30일** |
| /trend·/channel 라우트 | csv 직독 | canonical 집계(인라인) |

오너 결정 반영: roas=배수 · 채널=canonical 진짜셋(naver 분리·kakao 메시징) · 30일 · 프론트 폴리시 ③ 연기.

## 4. 가드레일
- **파일럿 먼저**: 매핑 워크플로 capability 에이전트가 translator로 실제 집계 검증(26,735,453 등) 후 production 전환.
- **data layer만**: `shared/canonical_daily.py` 헬퍼가 `translator.execute()`(self.fetch)만 사용. raw 직독 0.

## 5. 변경 (커밋 3)
- `b9dd42d` (8): canonical_daily.py 신설 + 4 tool 전환 + 테스트 rebaseline(일별 8→30, 채널셋 앵커, k14 값 앵커).
- `a020901` (2): dashboard1.py 라우트 3 직독 전환(/trend·/channel spark·/cost pacing 제거) + _route_ctx + e2e SEED_SOURCES canonical 7종.
- `83b8d60` (5): daily_performance.csv 삭제(gitignored) + 로더·SourceSpec·__init__ 재export 제거 + test_file 카운트(17→16·31→30).

## 6. 검증
- 영향 테스트 전부 PASS (batch2/3/4·channel_aggregate 라벨 소비처·e2e Postgres canonical 라운드트립).
- **전체 1035 passed · 신규 실패 0** (10 실패=A-5.2 때와 동일 pre-existing: parquet env 4·_scratch 구조 4·DC_PERM_6·o04).
- 신선 캐시 검증: /channel-overview = advoost/google/meta/naver_sa·퍼널 4,834,911 (canonical 정상).

## 7. 정직 경고 (잔여)
1. **★ 운영 대시보드 파이프라인 결과는 아직 옛 캐시 서빙.** 인라인 직독(trend/spark/pacing)은 canonical 즉시 반영됐으나, **파이프라인 경유(channel_table_detailed·conversion_funnel·daily line·dashboard1 KPI 9)는 persistent `_workspace` 캐시(옛 값)를 읽음.** 캐시 비우면(=A-3) 전부 canonical. = 다음 작업.
2. ~~format_normalizer 미삭제~~ → **✅ 폐기 완료 (`d38f006`)**: tool·catalog·unit test 8 삭제 + team_catalog 노드·executor 분기·프롬프트 안내 교정(광고 지표=canonical 내부소비) + test(tripwire 제거·catalog 93→92·planning_gaps 픽스처). 회귀 1026 passed 신규실패0. **옛 World-B normalizer 전멸.**
3. **_campaign_pacing 제거됨** (오너 결정). 기획예산↔실플랫폼 campaign_id 크로스워크 부재로 canonical 재현 불가. /비용 페이지 pacing=빈 배열. 크로스워크 생기면 복원.
4. **channel_targets.csv·프론트 라벨/roas표기 미정합**(③). target_roas가 naver_sa/advoost엔 None(targets는 옛 채널명). roas 배수를 프론트가 %로 표시. 채널 페이지 폴리시는 ③.

## 8. 다음
**A-3 캐시 cutover (권장)**: 실측(`_workspace`에 무엇이 있고 누가 읽는지) 후 옛 캐시 비우기 → 운영 대시보드 전체가 canonical(26.8M·4.46) 반영. 또는 format_normalizer 삭제(A-5.3 잔여). 이후 A-6 분리 재확인 → A-7 TOOL 전체 구조 점검 → C2 에이전트 e2e.
