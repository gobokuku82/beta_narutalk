# 측정 M0 — 멀티쿼리 e2e 4단 측정 완료보고서 (2026-06-12)

> 마스터 [1] 산출물. 제품 코드 무수정 — 측정기 정비 4(T1~T4, 커밋 `9425118`) + 실행 + 본 보고서.
> 원시 데이터: `docs/_claude/4layer_system/diag_results/fullgraph_compound_20260612-174710.{json,md}` (14쿼리×3런 full-graph, judge on) ·
> `baseline_compound_20260612-180645.{json,md}` (fresh 재기준선 5런×14쿼리).

## §1 한눈 요약 — 4단 통과율 (run-level)

| 단 | 지표 | 실측 | 판정 |
|---|---|---|---|
| ① 인식 | sub_intents 분해 | 복합 12쿼리 중 10 안정 / **2 출렁(⚠ n_sub 0↔2)** | 대체로 건강, 국소 불안정 |
| ② 계획 | coverage (judge) | **fresh 재기준선 100.0% (σ=0.0, stable-full 14/14)** | **무죄 확정** (구 98.5%는 수술 전 regime — 06-12 수술이 계획층을 해치지 않았고 오히려 안정화) |
| ③ 실행 | 의도 완주율 | 91.7 / 93.8 / 95.7% (런간 변동 4.0%p) | 양호하나 **침묵 드롭** 존재 (§3 축3) |
| ④ 표시 | 의도 표출률 | **45.2 / 48.4 / 48.4%** (런간 변동 **3.2%p**) | **주범 확정** — 절반이 안 보임 |

**한 줄**: 만들 줄은 아는데(③ ~94%) 보여줄 줄을 모른다(④ ~47%). 깨짐은 flaky가 아니라 **systematic**(3/3런 동일) — 결정론 수술 대상.

## §2 무너지는 지점 3축 (file:line + 실측 증거)

### 축1 — ④ 표시 단일선택 (systematic, 영향 7쿼리, 최대 부위)

`response/responder.py:279-284` — display 서사가 `summary → report_markdown → answer → recommendation_text` **or-체인으로 1개만 선택**, `:192 _find_artifact`는 첫 일치만 반환. 의도가 N개여도 서사는 1개.

증거 (3/3런 동일):
- "4월 **채널별** ROAS 보여줘": channel_aggregate **completed**인데 응답 = "ROAS 6.53" (전체값만, 채널 분해 무표출)
- "채널별 ROAS랑 소재별 CTR 같이": 둘 다 **completed**인데 응답 = "평균 CTR 3.62%" 한 줄 (둘 다 무표출 — 표출률 0%)
- lv3 3쿼리 표출 0~50% 동일 기전

### 축2 — 스코프 되묻기의 전면 점령 (G19의 복합 미대응)

`build_missing_period_payload`(G19)는 period SKIP 시 응답 **전체**를 "기간을 알려주세요"로 채운다. 단일 의도엔 정답(D3)이나, 복합에서는 **완료된 비스코프 의도까지 침묵**:
- r2 lv5 PDF: diagnoser·ai_recommendation·report_writer·pdf_renderer **전부 completed** (PDF 생성됨!) → 응답 = 기간 물음 한 줄
- "부진 채널 찾아서 개선안": insight_extractor completed(개선안 존재) → 무표출

### 축3 — 실행 침묵 드롭 + ① 인식 간헐 전멸

- **침묵 드롭**: r3 "부진 채널" — 계획 11 todo 중 **실행 8개만** (diagnoser·ai_recommendation·summary_generator는 exec_todos에 아예 부재, st=[]) + 응답 = fallback "분석을 완료했습니다."(EMPTY). 떨어뜨렸다는 신호 0.
- **인식 전멸**: "리뷰 키워드 뽑고 그걸로 캠페인 추천" — 3런 중 2런 cognitive가 빈 출력(tools=[], n_sub=0, **응답 = 빈 문자열**). 나머지 1런은 완벽(REAL, 둘 다 완주). flaky 인식 + 빈 응답 무음.

## §3 수술표 초안 (M1 후보 서열 — 계획서 v2 승격은 오너 승인)

| 서열 | 수술 | 부위 | 근거 | 예상 효과 |
|---|---|---|---|---|
| S1 | **다중 산출 표시 병합** (계획서 C1) — or-체인을 의도별 섹션 합성으로 | `responder.py:279-284`·`:192` (response 층만 — 위험 최소) | 축1 systematic 7쿼리 | ④ 47%→ 추정 80%+ (임계: 런간 변동 3.2%p 초과 — 여유 큼) |
| S2 | **G19 의도 단위화** — 스코프 의도만 되묻고 완료 의도는 함께 표시 (C5 흡수) | `responder.py` G19 분기 | 축2 (완료 산출 침묵) | 복합+무월 쿼리 구제 |
| S3 | **실행 드롭 가시화** (C2 연계) — 계획 todo가 실행 안 되면 SKIPPED(사유) 강제, fallback "완료했습니다" 금지 | execution/executor (phase 잔여) + G21 | 축3 침묵 드롭 | EMPTY 둔갑 차단 |
| S4 | (관찰) 인식 간헐 전멸 — lv4 추천류 재시도/G22 배선 | cognitive | 축3 (1쿼리 2/3런) | R2 본배선은 여전히 미정당 — S1~S3 후 재측정으로 재평가 |

## §4 기준질문 확정

- **G7** (기존): "2026년 4월 총매출 알려주고, 부진 채널 개선안도 추천해줘" — 유지.
- **G8 원문 확정 (실측 박제)**: **"부진 채널 찾아서 그 채널 개선안 알려줘"** — 3/3런 전부 깨졌고 매런 다르게 깨짐(DEGRADE·DEGRADE·EMPTY). 합격 기준: "부진 채널(스코프 필요)은 기간 되묻고, 개선안 의도는 완료분 표시 또는 사유 명시" — 침묵·전멸 금지.

## §5 [3] 합격 임계 (T3 재실행 — 약속 박제 이행)

동일 corpus·동일 3런으로 T3 재실행 시: **④ 표출률 개선폭 > 3.2%p(M0 런간 변동폭 실측) + systematic 탈락 쿼리 0** + G7·G8 채팅 합격. (③ 완주율 비교 시 변동폭 4.0%p 적용)

## §6 검증·재현

- 측정기: 단위 7 passed (`tests/test_m0_judges.py`) + smoke 1 — 커밋 `9425118`
- 재현: `python -m scripts.agent_lang_diagnostics.measure_stage1_coverage --corpus corpus_compound.yaml --runs 3 --judge` / `python -m scripts.agent_lang_diagnostics.baseline_compound --runs 5` (backend/, File 백엔드 기본)
- 비파괴: 구 기준선 `baseline_compound.{json,md}`(98.5% regime) 보존 — 신 산출은 run-id 파일명

## §7 다음

1. 오너: 본 보고서 + 수술표 검토 (→ 멀티쿼리 계획서 v2 승격 승인 = [3] 시작 조건)
2. Claude: **[2] B pushdown 착수** (마스터 게이트 충족 — [1] 완료. 병행 규정: 오너 검토 ∥ B 구현)
3. 별건 대기: household 12행 복원 (판정 완료 — 오너 한마디)
