# 계획서 — 작업 ⑤ 32 v1.1 §4~§9 outdated 정합 (4 섹션 갱신)

> 작성: 2026-05-31. 작업 ④-L7 후속 큰 항목.
> 작업 ④ = 코드/카테고리 정합 (enum·yaml·frontend), 작업 ⑤ = **박제 단일소스(32) 의 outdated 본문 정합**.
> 참조 = [계획_작업④L5_카테고리enum정합_2026-05-31.md](./계획_작업④L5_카테고리enum정합_2026-05-31.md), [session_compact_recovery_2026-05-30_v2.md](./session_compact_recovery_2026-05-30_v2.md)

---

## 1. 배경 — 32 가 박제 단일소스인데 본문 outdated

### 1.1 박제 단일소스의 누설

- 32 v1.1 의 §2.5·§2.6·§2.7·§2.8 (작업 ③ 박제, e8d231f) + §3 (디렉터리, 829d6e0) = **정합 ✅**
- 32 v1.1 의 §4·§5·§6·§7·§8·§9 = **outdated** (Sprint 15 이전 박제, 작업 ③·④ 후 미갱신)
- §10·§11 = gap/접점 박제 — 본 계획 범위 외 (별 점검 필요 시 별 ONE)

### 1.2 측정 결과 (workflow 6 parallel + 1차 적대적 검증, 2026-05-31)

| § | outdated_level | 갱신 분량 | 갱신 전략 | 비고 |
|---|---|---|---|---|
| **§2.5** 카테고리 정의 (북극성) | **high** | small | patch | 1차 검증 발견 — line 14·20·70-82·87 "7 카테고리" + report 보조 누락 |
| **§4** Tool 실행 파이프라인 | **none** ✅ | 0 | — | executor.py 정합 |
| **§5** BaseTool 인터페이스 | high | medium | patch (4~6 항목) | line 373·407·408·413·414·447-451·453 |
| **§6** YAML 메타카드 스키마 | high | large | rewrite | 32 §6 + **_schema.yaml line 20 함께** |
| **§7** 구현/Stub/Planned 전수표 | high | very-large | **폐기** | 헤더만 유지 |
| **§8** 실 Tool 체인 데이터 흐름 | high | large | rewrite | 다이어그램 8 카테고리 + 2 경로 (ads/review) |
| **§9** 새 Tool 추가 Step-by-Step | **none** ✅ | 0 | — | 자체 정합 |

**실 갱신 대상 = §2.5·§5·§6·§7·§8 (5 섹션) + _schema.yaml**, total outdated 항목 = ~25건.

### 1.3 1차 적대적 검증 (workflow 6 perspectives, 2026-05-31)

| verdict | 수 | 결과 |
|---|---:|---|
| pass | 0 | — |
| warn | 5 | 정확성·완전성·단계순서·검증가능성·롤백 — 모두 갱신 후 안전 |
| fail | 1 | 함정 누락 (적대적) — 카테고리 7/8 + super() 호출 + client fallback HIGH 발견 |
| **합 findings** | **38** | high 12 / medium 14 / low 12 |

→ 본 계획서 v2 = 1차 검증 발견 사항 5건 (U1~U5) 반영.

### 1.4 2차 적대적 검증 (workflow 6 perspectives, v2 대상, 2026-05-31)

| verdict | 수 | 결과 |
|---|---:|---|
| pass | 1 | — |
| warn | 1 | — |
| fail | 4 | 다수 fail = "계획서 미실행" 오인 (실 파일 v1 상태가 정상, 계획은 미래 청사진) |
| **합 findings** | **63** | high 27 / medium 24 / low 12 |

**진짜 v3 갱신 가치 발견 (5건, 노이즈 분리 후)**:
- U6: 33/README.md:3 "카테고리 7 정의" outdated (C0 확대)
- U7: 33/README.md:22 "analysis 6, 합 87" outdated (C0 확대 — 작업 ④-L7 commit 3738be6 의 33_analysis 9 정합 누락)
- U8: ADR-022:132-142 본문 outdated (client fallback 패턴, default 박제) — **별 ONE 권장**
- U9: API_SPEC.md:834 + 30_DATA_MODELS_v1.1.md:409 옛 4 카테고리 박제 — **별 ONE**
- U10: §7 v1.2 timeline 명확화 ("다음 32 본 문서 갱신 시")

→ 본 계획서 v3 = 2차 검증 발견 사항 5건 반영. C0 확대 (33/README) + §6 후속 확대 (ADR-022·API_SPEC·30_DATA_MODELS) + C3 timeline 명확화.

---

## 2. 영향 범위 (line 확인 완료)

### 2.0 §2.5 카테고리 정의 + 33/README.md (1차+2차 검증 발견, line 14·20·70-87 + 33/README:3·22)

| 파일·line | 현 박제 | 신 정합 | 근거 |
|---|---|---|---|
| **32 line 14** | "카테고리 **7개**의 의도·경계·decision tree 박제" | "카테고리 **8개** (7 본 + 1 보조 report)" | enum ToolCategory 8값 + 33/* 8 문서 |
| **32 line 20** | "§2.5 **카테고리 7 정의** (collection·...·analysis)" — report 누락 | "§2.5 카테고리 8 정의 (..., report)" | 동상 |
| **32 line 70-82** | 표 7 행 (1~7), report 없음 | 표 **8 행** — #8 report (보조 카테고리, 33_report.md 박제 정합) | 33_report.md |
| **32 line 87** | "report·pdf·image_creation·video_creation — planned/stub" | "pdf·image_creation·video_creation — planned/stub" (report 본 카테고리로 승격, 단 33 박제 = "보조 카테고리" 유지) | 33_report.md = "보조 카테고리" 명시 |
| ⚠️ **33/README.md line 3** *(2차 검증 발견)* | "32 §2.5 의 **카테고리 7 정의** 가 잣대" | "32 §2.5 의 **카테고리 8 정의** (7 본 + 1 보조 report) 가 잣대" | 동상, README 자체 정합 (line 12·23 은 이미 8 명시) |
| ⚠️ **33/README.md line 22** *(2차 검증 발견)* | "analysis ... 6" → 합 87 | "analysis ... **9**" → 합 **90** (실 yaml count 정합, 작업 ④-L7 commit 3738be6) | 33_analysis.md line 10 = "tool 수 9" + 실 catalog/analysis yaml 9 |

### 2.1 §5 BaseTool 인터페이스 (line 367-455)

| line | 현 박제 | 신 정합 (구체 코드) | 근거 |
|---:|---|---|---|
| **373** | `def __init__(self, spec: ToolSpec):` | `def __init__(self, spec: ToolSpec, data_source: DataSource \| None = None):` | base_tool.py:20 (BaseTool 시그니처 정합) |
| **407** | `def __init__(self, spec: ToolSpec, ds: DataSource \| None = None):` (이미 부분 정합) | 유지 (변경 X) — ds 명은 짧은 alias 로 OK | — |
| **408** | `super().__init__(spec)` — data_source 전달 안 함 | `super().__init__(spec, ds)` — data_source 도 부모에 전달 | base_tool.py:20 시그니처 |
| **409** | `self.ds = ds or get_default_data_source()` (이중 초기화) | **삭제** — 부모가 `self.ds = data_source or get_default_data_source()` 처리 (base_tool.py:27) | base_tool.py:27 |
| **411-414 (execute body)** | `merged = self.merge_params(params)` + `client = merged.get(...)` + `df = self.ds.get(client, "orders")` | `merged = self.merge_params(params)` + `df = self.fetch("orders", context)` (client 결정 헬퍼가 흡수) | base_tool.py:29-42 (helper-B) |
| **413** | `client = merged.get("client", "clumi")` — params fallback | **삭제** (fetch helper 가 context.client_id 사용) | base_tool.py:36-41 fail-fast |
| **447-451** | client 결정 3순위 (params → context → "clumi" fallback) | **rewrite**: "client 는 ExecutionContext.client_id 한 곳에서만 (fail-fast). 진입점(runner/API)이 채운다. Tool 의 params 에는 client 없음." | ADR-022 + 작업 ②-a (commit `4fc3f4f` workspace default 폐기) |
| **453** | "Sprint 16 의 46 tool (25 metrics + 21 collection)" | "현재 **90 tool** (collection 27 + normalization 6 + cleaning 3 + preprocessing 1 + metrics 35 + comparison 7 + analysis 9 + report 2)" | 작업 ④ 완료 시점 commit `3738be6` |

> ⚠️ **검증 명령** (line 번호 drift 함정 회피, 메서드명 기준):
> ```bash
> grep -A 2 "class BaseTool" backend/app/dream_agent/tools/base_tool.py | grep "__init__"
> grep -A 3 "def fetch" backend/app/dream_agent/tools/base_tool.py | grep "client_id"
> ```

### 2.2 §6 YAML 메타카드 스키마 (line 457-500) + _schema.yaml line 20

| 파일·line | 현 박제 | 신 정합 | 근거 |
|---|---|---|---|
| **32 §6 line 465** | `category: collection            # data \| analysis \| content \| ops` (4 카테고리) | `category: collection            # collection \| normalization \| cleaning \| preprocessing \| metrics \| comparison \| analysis \| report` (8) | enums.py:29-40 (작업 ④-L5 C2 dd9dbd1) |
| **32 §6 line 466** | `status: implemented             # implemented \| stub \| planned` | 폐기 — _schema.yaml 에 status 필드 없음, 90 yaml 모두 미사용 | catalog/_schema.yaml |
| **32 §6 line 468** | `executor: null                  # null → 경로 규칙으로 자동 import` | 유지 (executor 필드 자체는 정합) | registry.py:207-225 |
| **32 §6 line 492** | `null (권장) → app.dream_agent.tools.<category>.<name>.<PascalCase>` (category = 4 중 하나 가정) | category = 8 중 하나 — 예: `app.dream_agent.tools.collection.naver_collector.NaverCollector` | 폴더 구조 |
| **32 §6 line 495-498** | `status 의미` 설명 단락 (implemented/stub/planned) | 단락 전체 폐기 — status 필드 자체 폐기. 대신 33/* 의 `status` 컬럼 (complete/partial/planned) 안내 | 33_tools_by_category/*.md tool 표 |
| ⚠️ **_schema.yaml line 20** (catalog 의 실 schema, **1차 검증 발견**) | `category: string                    # 실행 카테고리: data \| analysis \| content \| ops` | `category: string                    # 실행 카테고리: collection \| normalization \| cleaning \| preprocessing \| metrics \| comparison \| analysis \| report` | enums.py + 90 yaml 실 분포 정합 |

> ⚠️ **C2 확대 (1차 검증 발견)**: 본 commit C2 = 32 §6 만 갱신 X. **_schema.yaml line 20 함께** 갱신 (catalog 의 진짜 schema 파일, 32 가 인용).

### 2.3 §7 구현/Stub/Planned 전수표 (line 502-580)

- **line 504-506**: 이미 DEPRECATED 박제됨 ("2026-04-24 시점 ~10 tool 전제, 현 시스템 ~89 tool 로 의미 상실. 진실 소스 = 33_tools_by_category/. §7 전체는 향후 폐기 (v1.2+)")
- **line 508-578**: §7.1 + §7.2 표 = **전체 outdated**:
  - 9 카테고리 (collection/preprocessing/analysis/report/pdf/image_creation/video_creation/shared) = 실 8 카테고리
  - format_normalizer = preprocessing 박제 → 실 normalization (작업 ③ 이동)
  - summary_generator = shared 박제 → 실 report (commit `906f4a3`)
  - pdf·image_creation·video_creation·shared 카테고리 = 폐기됨
  - implemented 8개 → 실 90 tool (yaml count)
  - planned 40+ → 33/* 가 진실 소스, 표 자체 무의미
- **처분**: 전체 표 (line 508-578) 폐기, 헤더 (line 502-506) 만 유지하되 링크 갱신:
  ```
  ## 7. 구현/Stub/Planned 전수표 — **POLAR (v1.2 폐기)**

  > 본 §7 은 2026-04-24 시점 ~10 tool 전제로 작성됨. 작업 ③·④ 후 90 tool 정합.
  > 진실 소스 = [33_tools_by_category/](33_tools_by_category/) (8 카테고리 인벤토리 + tool status).
  > §7 의 표 폐기 (commit X-Y, 2026-05-31). 본 헤더만 v1.2 까지 유지 후 완전 제거.
  ```

### 2.4 §8 실 Tool 체인 데이터 흐름 (line 582-629)

| line | 현 박제 | 신 정합 |
|---:|---|---|
| **588** | naver_collector (collection) | naver_sa_collector / naver_gfa_collector (ADR-014 분리 후, "naver_collector" 명 폐기) |
| **594** | format_normalizer (preprocessing) | format_normalizer **(normalization)** — 카테고리 이동 + review 경로는 review_normalizer 별도 |
| **607** | sentiment_analyzer (analysis/ml) | sentiment_analyzer (analysis) — ml/ 은 sub-folder, category enum 값 아님 |
| **615** | insight_extractor (llm) | insight_extractor (analysis) — llm/ 은 sub-folder |
| **다이어그램 전체** | 1 경로 (POC 시나리오) — review 흐름 추정 but 옛 tool 이름 | 2 경로 분리 권장: (a) ads chain: ads_collector → format_normalizer (normalization) → ... (b) review chain: review_collector → review_normalizer → text_preprocessor → sentiment_analyzer → ... |

**rewrite 전략**: 다이어그램 통째 재작성 — POC 시나리오 1 (review 경로) + 시나리오 2 (ads 경로) 둘 다 명시.

---

## 3. 작업 단계 (5 commit, 의미 단위 ONE)

> 1차 적대적 검증 (2026-05-31) 발견 사항 5건 (U1~U5) 반영. **C0 신설** + C2 확대 + C1 명세 구체화.
> 의미 단위 = ① §2 카테고리 박제 정합 (C0) ② §5 BaseTool ADR-022 정합 (C1) ③ §6 + _schema.yaml YAML 스키마 정합 (C2) ④ §7 전수표 폐기 (C3) ⑤ §8 다이어그램 rewrite (C4).

---

### commit C0 — §2.5 카테고리 박제 정합 + 33/README 갱신 (7 → 8, report 행 추가)

**ONE = "§2.5 카테고리 정의 + 33/README 헤더 통합 갱신"** (1차+2차 검증 발견 통합).

대상:
- **32 §2.5** line 14, 20, 70-82, 87
- **33_tools_by_category/README.md** line 3, line 22 (2차 검증 발견)

작업:
1. **32 line 14**: `카테고리 7개의 의도·경계·decision tree 박제` → `카테고리 **8개** (7 본 + 1 보조 report) 의 의도·경계·decision tree 박제`
2. **32 line 20**: `§2.5 **카테고리 7 정의** (collection·...·analysis)` → `§2.5 **카테고리 8 정의** (..., report)`
3. **32 line 70-82** §2.5 표에 **8번째 행** 추가 (표 마지막 행):
   ```
   | 8 | **report** (보조) | 분석 결과를 사람이 읽는 *텍스트* 로 (요약·보고서) | summarize, narrate, format | 텍스트 (markdown) |
   ```
4. **32 line 87** "카테고리 외 (보조)" 단락에서 **report 제거** (보조 행 #8 에 통합), pdf·image_creation·video_creation 만 유지
5. **33/README.md line 3**: `32 §2.5 의 **카테고리 7 정의** 가 잣대` → `32 §2.5 의 **카테고리 8 정의** (7 본 + 1 보조 report) 가 잣대`
6. **33/README.md line 22**: `analysis ... 6` → `analysis ... **9**` (실 yaml + 33_analysis.md 정합)

**검증** (Bash):
```bash
# 32 갱신 확인
grep -n "카테고리 8\|카테고리 7" docs/agent_specs/32_execution_agent_tools_v1.0.md
# 기대: "8" 박제, "7" 잔존 0 (단 §7 헤더 "2026-04-24" 같은 history 제외)

# 33 README 갱신 확인
grep -n "카테고리 8\|analysis" docs/agent_specs/33_tools_by_category/README.md
# 기대: "카테고리 8" + "analysis ... 9"

# enum 정합
grep "REPORT" backend/app/dream_agent/models/enums.py
# 기대: REPORT = "report" 박제 확인
```

**commit 메시지**: `docs(32+33): §2.5 + README 카테고리 8 정합 (7 → 8, report 행 + analysis 9)`

---

### commit C1 — §5 BaseTool 인터페이스 patch (ADR-022 정합)

**ONE = "§5 의 BaseTool 코드 박제를 ADR-022 정합으로 patch"** (1차 검증 명세 구체화 반영).

대상 line: 373, 407, **408, 409**, 411-414, 413, 447-451, 453 (7 항목, 1차 검증 발견 추가).

작업:
1. line 373 `def __init__(self, spec: ToolSpec):` → `def __init__(self, spec: ToolSpec, data_source: DataSource | None = None):` (실 시그니처 정합)
2. line 407 `def __init__(self, spec: ToolSpec, ds: DataSource | None = None):` — **유지** (이미 부분 정합, ds alias OK)
3. line 408 `super().__init__(spec)` → `super().__init__(spec, ds)` — data_source 부모 전달 (base_tool.py:20 시그니처 정합)
4. line 409 `self.ds = ds or get_default_data_source()` → **삭제** (부모가 base_tool.py:27 에서 이미 처리, 이중 초기화 폐기)
5. line 411-414 execute body:
   - 옛: `merged = ...` + `client = merged.get(...)` + `df = self.ds.get(client, "orders")`
   - 신: `merged = ...` + `df = self.fetch("orders", context)` (fetch helper 가 client 결정 흡수)
6. line 413 `client = merged.get("client", "clumi")` → **삭제** (fetch helper 가 context.client_id 사용, fail-fast)
7. line 447-451 client 결정 3순위 (params → context → "clumi") → **rewrite**:
   ```
   **`client` 결정** (POC 컨벤션, ADR-022): tool 은 client 모름.
   `ExecutionContext.client_id` 한 곳에서만 흐른다 (fail-fast).
   진입점(runner/API/agent)이 ctx.client_id 를 채운다. params 에는 client 없음.
   ```
8. line 453 "Sprint 16 의 46 tool" → "현재 **90 tool** (collection 27 + normalization 6 + cleaning 3 + preprocessing 1 + metrics 35 + comparison 7 + analysis 9 + report 2)"

회귀: **문서 변경**. 코드 무관, 회귀 명령 불필요.

**검증** (메서드명 기준, line 번호 drift 함정 회피):
```bash
grep -A 2 "class BaseTool" backend/app/dream_agent/tools/base_tool.py | grep "__init__"
grep -A 3 "def fetch" backend/app/dream_agent/tools/base_tool.py | grep "client_id"
# 32 문서 갱신 후
grep -n "client = context.client_id\|self.fetch" docs/agent_specs/32_execution_agent_tools_v1.0.md
```

commit 메시지: `docs(32): §5 BaseTool ADR-022 정합 patch (시그니처·super호출·fetch helper·client_id·90 tool)`

---

### commit C2 — §6 + _schema.yaml YAML 메타카드 스키마 rewrite (status 폐기 + 8 카테고리)

**ONE = "§6 YAML 예제 + _schema.yaml 신 스키마로 통째 재작성"** (1차 검증 확대: _schema.yaml 함께).

대상:
- 32 §6 line 463-498 (YAML 예제 + status 의미 단락)
- **backend/app/dream_agent/tools/catalog/_schema.yaml line 20** (1차 검증 발견 — catalog 의 진짜 schema)

작업:
1. **32 §6 YAML 예제 (line 463-488) 통째 재작성**:
   - `category` 주석: 4 카테고리 → 8 카테고리
   - `status` 필드 폐기 (line 466 삭제)
   - `executor: null` 주석 유지
   - 다른 필드 (description, parameters, produces, timeout_sec, max_retries, requires_approval, has_cost) 유지
2. **32 §6 `executor` 경로 규칙** (line 491-493): category = 8 카테고리 명시
3. **32 §6 `status` 의미 단락** (line 495-498) 전체 폐기. 대체 단락 추가:
   ```
   **상태 표기**: yaml 의 `status` 필드는 폐기 (작업 ③·④, 2026-05-30/31).
   tool status 는 [33_tools_by_category/*.md](33_tools_by_category/) 의 tool 표 `status` 컬럼 박제
   (complete / partial / planned / deprecated). registry.py 는 `status` 필드를 읽지 않음.
   ```
4. **_schema.yaml line 20** 갱신 (32 가 인용하는 진짜 schema 파일):
   - 옛: `category: string                    # 실행 카테고리: data | analysis | content | ops`
   - 신: `category: string                    # 실행 카테고리: collection | normalization | cleaning | preprocessing | metrics | comparison | analysis | report`

회귀: 문서 + 1 yaml schema 변경, 코드 무관.

**검증** (bash glob 의존성 회피 — find 사용):
```bash
# 1. yaml status 필드 진짜 0 hit
find backend/app/dream_agent/tools/catalog -name "*.yaml" -type f -exec grep -l "^status:" {} \;
# 기대: 출력 0 줄

# 2. _schema.yaml line 20 갱신 확인
grep -n "실행 카테고리:" backend/app/dream_agent/tools/catalog/_schema.yaml
# 기대: 8 카테고리 표기

# 3. registry.py 가 status 필드 안 읽는지 확인 (BREAKING change 검증)
grep -n "status" backend/app/dream_agent/tools/registry.py
# 기대: status 박제 0 또는 deprecation 주석만
```

commit 메시지: `docs(32) + chore(schema): §6 YAML 메타카드 + _schema.yaml category 8 정합 (status 폐기)`

---

### commit C3 — §7 구현/Stub/Planned 전수표 폐기 (전체 표 삭제)

**ONE = "§7 의 outdated 전수표 폐기, 헤더만 v1.2 마감 표시 유지"**.

대상 line: 502-580 (헤더 + §7.1 + §7.2 전체).

작업:
1. 헤더 (line 502-506) 갱신:
   ```
   ## 7. 구현/Stub/Planned 전수표 — POLAR (폐기됨)

   > 본 §7 의 표는 2026-04-24 시점 ~10 tool 전제로 작성됨. 작업 ③·④ (2026-05-30/31) 후 90 tool 정합.
   > **진실 소스** = [33_tools_by_category/](33_tools_by_category/) (8 카테고리 인벤토리 + tool status).
   >
   > 본 §7 의 표 (옛 §7.1 / §7.2) = commit 폐기 (2026-05-31, 작업 ⑤). 헤더만 **다음 32 본 문서 갱신 시 (v1.2 또는 sprint 17+ 시점)** 완전 제거 예정. v1.2 의 구체 timeline 미정 — 32 §2·§5·§8 추가 갱신 필요 시 묶음 처리.
   ```
2. §7.1 표 (line 508-569) 전체 삭제
3. §7.2 표 (line 571-578) 전체 삭제

→ §7 = 헤더 4~5 줄로 축소.

회귀: 문서 변경. 검증 = 33_tools_by_category/ 8 문서 모두 정합 (이미 검증됨).

commit 메시지: `docs(32): §7 구현 전수표 폐기 — 33/* 가 진실 소스`

---

### commit C4 — §8 실 Tool 체인 데이터 흐름 rewrite (8 카테고리 + 90 tool)

**ONE = "§8 의 다이어그램을 신 카테고리 + 신 tool 이름으로 재작성"**.

대상 line: 582-629 (다이어그램 + 설명).

작업:
1. 다이어그램 전체 재작성. POC 시나리오 2 경로 분리:

   **경로 A: 광고 성과 분석 (ads chain)**
   ```
   ┌──────────────────┐
   │ meta_collector / │
   │ naver_sa_*       │ (collection)
   └────────┬─────────┘
            │ raw_ads
            ▼
   ┌──────────────────┐
   │ format_normalizer│ (normalization)
   └────────┬─────────┘
            │ normalized_ads
            ▼
   ┌──────────────────┐
   │ daily_performance│
   │ _aggregate       │ (metrics)
   └────────┬─────────┘
            ▼
   ┌──────────────────┐
   │ report_writer    │ (report)
   └──────────────────┘
   ```

   **경로 B: 리뷰 감성·키워드 (review chain)**
   ```
   ┌──────────────────┐
   │ review_collector │ (collection)
   └────────┬─────────┘
            │ raw_reviews
            ▼
   ┌──────────────────┐
   │ review_normalizer│ (normalization)
   └────────┬─────────┘
            │ normalized_reviews
            ▼
   ┌──────────────────┐
   │ text_preprocessor│ (preprocessing)
   └────────┬─────────┘
            │ cleaned_texts
            ▼
   ┌──────────────────┐
   │ sentiment_analyzer (analysis, ml/ sub) │
   │ + keyword_extractor (analysis, ml/)    │
   └────────┬─────────┘
            ▼
   ┌──────────────────┐
   │ insight_extractor (analysis, llm/)     │
   └────────┬─────────┘
            ▼
   ┌──────────────────┐
   │ report_writer + summary_generator (report) │
   └──────────────────┘
   ```

2. 카테고리 표기 정합 (sub-folder ml/llm 은 괄호 안 명시, category 값은 'analysis' 단일).
3. 박제 설명 (line 583-585) 갱신: "실제 end-to-end 경로 2: ads + review" 명시.
4. 90 tool 전체는 33/* 참조.

회귀: 문서 변경. 검증 = 신 tool 이름 (review_normalizer 등) 정확성 (yaml 존재).

commit 메시지: `docs(32): §8 데이터 흐름 다이어그램 rewrite (8 카테고리 ads/review 2 경로)`

---

## 4. 회귀·검증 기준

### 4.1 baseline

본 계획 = **문서 변경만**. 코드 무관 → 코드 회귀 명령 불필요.

| 영역 | baseline | 결과 |
|---|---|---|
| dashboard1 영역 | 303/3 | ✅ 불변 (코드 무관) |
| 분석 team (sprint13+14) | 275/11/2 | ✅ 불변 |
| sprint15 broken | 17/54 | ✅ 불변 |

### 4.2 박제 정확성 검증 (각 commit 후) — Bash + find 기준

> 🔧 **실행 환경**: 모든 명령은 **Bash** (Claude Code Bash 툴 또는 git-bash). PowerShell 직접 실행 시 변환 필요. glob 의존성 회피 위해 `find` 사용.

| commit | 검증 명령 |
|---|---|
| **C0 (§2.5)** | `grep -n "카테고리 8\|report.*보조" docs/agent_specs/32_execution_agent_tools_v1.0.md` (8 카테고리 박제 + report 행 추가 확인) + `grep "REPORT" backend/app/dream_agent/models/enums.py` (enum 정합) |
| **C1 (§5)** | `grep -A 2 "class BaseTool" backend/app/dream_agent/tools/base_tool.py \| grep "__init__"` (실 시그니처 확인) + `grep -A 3 "def fetch" backend/app/dream_agent/tools/base_tool.py \| grep "client_id"` (fetch helper fail-fast 확인) + `grep -n "client = context.client_id\|self.fetch" docs/agent_specs/32_execution_agent_tools_v1.0.md` (32 갱신 정합) |
| **C2 (§6 + _schema.yaml)** | `find backend/app/dream_agent/tools/catalog -name "*.yaml" -type f -exec grep -l "^status:" {} \;` (출력 0 줄 = status 진짜 폐기) + `grep -n "실행 카테고리:" backend/app/dream_agent/tools/catalog/_schema.yaml` (8 카테고리 정합) + `grep "status" backend/app/dream_agent/tools/registry.py` (registry 가 status 안 읽음 확인) |
| **C3 (§7)** | `ls docs/agent_specs/33_tools_by_category/*.md \| wc -l` (8 .md + README = 9 줄) + `grep "tool 수" docs/agent_specs/33_tools_by_category/*.md` (합 = 90 확인) |
| **C4 (§8)** | `find backend/app/dream_agent/tools/catalog -name "review_normalizer.yaml" -o -name "format_normalizer.yaml" -o -name "review_collector.yaml" -o -name "naver_sa_collector.yaml"` (4 신 tool 존재 확인) |

### 4.3 의미 검증

본 계획 완료 후 32 v1.1 → v1.2 가능성. 헤더 (line 1-10) 의 version 박제 갱신 검토. 단 본 계획 외 (별 ONE).

---

## 5. 함정·체크포인트

### 5.1 박제 정확성 함정

| # | 함정 | 영향 | 해결 |
|---|---|---|---|
| **F1** | base_tool.py line 번호 시간순 변동 (commit 누적) | §5 patch 의 line 인용 outdated 즉시 가능 | **메서드명 기준 grep** 검증 (`grep -A 2 "class BaseTool"`) — §4.2 C1 검증 명령에 박제됨 |
| **F2** | ADR-022 본문 outdated 가능 | §5 인용 outdated 위험 | C1 실행 전 ADR-022 spot-check (Read) |
| **F3** | §7 헤더 deprecated 박제 보존 (v1.2 후 폐기 예정) | 본 commit C3 가 약속 위반 위험 | 헤더에 "본 계획 commit 으로 표 폐기" 명시 + v1.2 timeline = "다음 본 문서 갱신 시" (열린 형) |
| **F4** | §8 다이어그램의 신 tool 이름 오타 | yaml 존재 안 하는 tool 인용 | C4 검증 = `find ... -name "*.yaml"` 으로 4 신 tool 존재 확인 (§4.2 박제) |
| **F13** *(1차 검증 발견)* | 32 §2.5 의 "7 카테고리" 박제가 다른 곳 (33_*.md, ADR, _claude/) 에서 인용될 수 있음 | C0 만으로 정합 안 됨 | C0 실행 후 `grep -rn "카테고리 7\|7개의 카테고리" docs/` 로 잔존 spot-check |
| **F14** *(1차 검증 발견)* | C2 의 status 폐기 = BREAKING change. 외부 yaml 작성자 (개발자) 가 status 작성 가능 | registry 가 무시할 텐데 silent 통과 위험 | C2 의 §6 대체 단락에 명시 안내 + registry.py 에 status deprecation 경고 검토 (별 작업 가능) |
| **F15** *(1차 검증 발견)* | C3 의 v1.2 timeline 모호 (구체 sprint/date 없음) | 헤더 deprecated 박제가 "언제 폐기?" 의문 잔존 | "v1.2 또는 다음 본 문서 갱신 시" 열린 형 명시 |

### 5.2 의존 함정

| # | 함정 | 영향 | 해결 |
|---|---|---|---|
| **F5** | §5·§6 의 카테고리 박제 (4 카테고리) 가 다른 곳에서도 인용 가능 | 32 외 추가 outdated 위험 | 본 계획 외 — 별 grep ("data \| analysis \| content \| ops" 패턴) |
| **F6** | C2 의 status 폐기 후 33/* 의 `status` 컬럼이 박제 단일소스 | 33/* 의 status 표기 정합 필요 (이미 박제됨, 검증만) | C2 검증 단계에서 33/* status 컬럼 정합 확인 |
| **F7** | §7 폐기 = 33/* 가 유일한 진실 소스 | 33/* 의 정합·완성도가 33/* 자체에 의존 (외부 보장 X) | 33/* 의 outdated 검토 = 별 ONE (작업 ④-L7 의 33_analysis.md 갱신 commit `3738be6` 같은 패턴) |

### 5.3 검증 함정

| # | 함정 | 영향 | 해결 |
|---|---|---|---|
| **F8** | 문서 변경만이라 회귀 검증 누락 | 의외의 인용 오류 발견 못함 | 각 commit 후 grep spot-check (line 5.2 검증 명령) |
| **F9** | 4 commit atomic 안에서 *순서* 중요? | 본 계획 = 모두 문서, 순서 무관 | 그래도 권장 = C1 → C2 → C3 → C4 (간단→복잡) |

---

## 6. 작업 외 항목 (별 ONE)

- §10·§11 (Gap·접점) outdated 점검 — 별 작업
- 32 v1.1 → v1.2 헤더 갱신 (line 1-10 version 박제)
- 33/* 8 문서 outdated 전체 점검 (33_analysis.md 외 7 문서)
- ⚠️ **ADR-022 본문 outdated** *(2차 검증 발견)*:
  - ADR-022:132-142 = 옛 client fallback 패턴 (`client = merged.get("client", "clumi")`)
  - ADR-022:154 = `client_id: str = "clumi"` default 박제 (실 코드 = default 폐기, fail-fast)
  - **별 ONE 권장** (ADR Accepted 상태, 갱신 = supersede 절차 필요)
- ⚠️ **다른 문서 4 카테고리 박제** *(2차 검증 발견)*:
  - `docs/specs/API_SPEC.md:834` = "Filter by category (data, analysis, content, ops)"
  - `30_DATA_MODELS_v1.1.md:409` = `category: ToolCategory # data|analysis|content|ops` (1차 검증 agent 보고, spot-check 필요)
  - **별 ONE** (4 카테고리 박제 일괄 grep + 갱신)
- _claude/* 의 32 인용 outdated grep — 별 작업
- session_compact·계획서 인용 (이력 박제) — 갱신 불요 (과거 박제 보존)

---

## 7. 진행 순서 요약

```
C0: §2.5 카테고리 박제 (7 → 8, report 행 추가)     → commit 1   [1차 검증 발견]
    검증 = "카테고리 8" grep + enum REPORT
C1: §5 BaseTool patch (7 항목, medium)              → commit 2   [1차 검증 명세 구체화]
    검증 = base_tool.py 메서드명 grep + 32 갱신 정합
C2: §6 + _schema.yaml YAML 스키마 정합              → commit 3   [1차 검증 확대]
    검증 = find status 0 hit + _schema.yaml 8 카테고리 + registry status 안 읽음
C3: §7 전수표 폐기 (헤더만 유지)                    → commit 4
    검증 = 33_tools_by_category/ 9 파일 (8 .md + README) + 합 90
C4: §8 다이어그램 rewrite (8 카테고리 2 경로)      → commit 5
    검증 = 4 신 tool yaml find
```

**총 5 commit** (1차 검증 후 C0 신설). 분량:
- C0 patch ~10 줄
- C1 patch ~40 줄 (7 항목 명세 구체화)
- C2 rewrite ~45 줄 (32 §6 + _schema.yaml)
- C3 삭제 ~70 줄 (표 통째)
- C4 rewrite ~60 줄

---

## 8. 롤백 절차

### 8.1 commit 별 롤백

| commit | 롤백 |
|---|---|
| C0~C4 | `git revert <hash>` (C2 만 yaml 1개 함께, 그 외 문서만, 영향 zero) |

### 8.2 비상 안전

문서 + yaml schema 1 파일 변경이라 비상 시나리오 zero. 단 일괄 폐기:
```bash
git revert <C4-hash> <C3-hash> <C2-hash> <C1-hash> <C0-hash>
```

### 8.3 부분 롤백 주의 (1차 검증 발견 F3)

- C3 의 §7 폐기 단독 revert 시 — 헤더 deprecated 박제 (이미 829d6e0 commit) 와 본 C3 의 "표 폐기 commit (2026-05-31)" 문구 사이 모순 가능
- 권장: C0~C4 는 *원자적* revert (단독 X)
- 또는 부분 revert 후 헤더 수동 정리

---

## 9. 완료 기준

1. ✅ **5 commit** 모두 진행 (C0·C1·C2·C3·C4) — 1차 검증 발견 C0 신설 포함
2. ✅ 박제 정확성 검증 5건 (C0 카테고리 8 + C1 base_tool 메서드명 + C2 status 0 hit + _schema.yaml 정합 + C3 33/* 9 파일 + C4 4 신 tool yaml)
3. ✅ 회귀 baseline 불변 (문서 + yaml schema 1 파일, 코드 무관)
4. ✅ 32 v1.1 의 **§2.5·§5·§6·§7·§8** outdated 0 (단 §10·§11 + 33/* 8 문서 전수 점검 + _claude/* 잔존 = 별 ONE)

본 계획 완료 후 = 32 본문 + _schema.yaml = 작업 ③·④·⑤ 박제와 완전 정합. 다음 작업 시 32 가 신뢰 가능한 박제 단일소스.

---

## 10. 진입 가능 여부

- 측정 완료 ✅ (workflow 6 parallel)
- 1차 적대적 검증 완료 ✅ (workflow 6 perspectives, findings 38)
- 1차 발견 5건 (U1~U5) 모두 본 계획서 v2 반영 ✅
- 영향 범위 line 확인 완료 ✅ (§2.5·§5·§6·§7·§8 spot-check)
- _schema.yaml line 20 outdated 발견 ✅ (C2 확대 반영)
- 32 §2.5 "7 카테고리" outdated 발견 ✅ (C0 신설 반영)
- base_tool.py 시그니처 정합 확인 완료 ✅
- 33/* 진실 소스 정합 ✅
- 회귀 = 문서 + yaml schema 1 파일, 코드 무관 ✅
- 함정 12건 박제 + 해결 (F1~F12 + 1차 발견 F13~F15) ✅
- 롤백 절차 + 완료 기준 박제 ✅
- 검증 명령 보강 (find 기반, glob 의존성 회피) ✅

**남은 검증**: 2차 적대적 검증 (multi-perspective) → 3차 재검증 루프 → 사용자 승인 → C0 진입.

→ 사용자 승인 시 C1 즉시 진입 가능.
