# ② 데이터 의미 전달 — A/B 2차(전달 *방식* 가설) 완료보고서 2026-06-18

> 커밋 `d7f03c4`. 깊은 기록(전 가설·1·2차) = `docs/_claude/plans/②의미전달_의사결정_dossier_2026-06-18.md` §9·§10. 본 문서 = 오너용 요약.

## 무엇을 했나
A/B 1차가 "데이터 의미(단위·함정)는 프롬프트에 들어갔는데 LLM이 무시"를 보였다. 오너가 제안한 두 보강안을 **가설로 만들어 측정**:
- **v3 강제형** — 의미 블록을 "반드시 이 단위로만, 혼동 금지" 헤더로 감싸고, 핵심 단위 규칙을 system_prompt(상위 권위)에 주입.
- **v4 인라인 동봉** — 별도 사전 블록을 없애고 값에 단위를 붙여 전달(`tacos_pct: 22.37` → `"22.37 (%)"`).
- **v5** — 블록 + 인라인 둘 다 (블록 제거가 해인지 분리).

A/B 하네스(`c2_ab_glossary.py`)를 3조건 → **6조건**(off/v1/v2/v3/v4/v5)으로 확장, LLM 분석기 4종 × 동일 하드 입력 × reps 3 = 조건당 n=12, 단위오독을 LLM이 판정.

## 왜 (구조 우선·측정=확인 원칙)
할루시는 증상이지 작업 대상이 아니다. "전달 *방식*을 바꾸면 ②축 할루시가 v2보다 줄까"는 **구조 가설**이고, 측정은 그 가설을 *확인/반증*하는 도구다. 공정성을 위해 단위·의미는 전부 contract(SSOT)에서 파생했고 정답(TRUTH)은 프롬프트에 주입하지 않았다(teaching-to-test 금지).

## 변경표
| 파일 | 변경 |
|---|---|
| `tools/shared/semantic_contract.py` | `unit_token(key)` 추가 — 값에 동봉할 단위 토큰만(원/배수,%아님/%/비율/정수), contract 파생. v4 인라인의 단위 소스이자 production 재사용. |
| `scripts/c2_ab_glossary.py` | 3→6 조건. v3(강제형 헤더+system_prompt 메타규칙), v4(블록 제거+인라인), v5(둘다). 전역 가변상태 패치라 직렬 실행(병렬 금지 명시). |
| `tests/test_semantic_contract_equivalence.py` | `unit_token` 토큰 구분 테스트(+1). |

## 검증 수치 (n=12/조건)
| 조건 | unit_error | fab | 판정 |
|---|---|---|---|
| off (사전 없음) | 8 | 0 | bare baseline |
| v1 (손코딩 COL_DESC) | **3** | 0 | ★최저(1차와 일치) |
| v2 (contract 파생) | 4 | 2 | provenance |
| **v3 (강제형)** | **5** | **3** | ⚠ v2보다 나쁨(날조↑) |
| **v4 (인라인)** | 4 | 1 | v2 동률 |
| **v5 (둘다)** | 5 | 1 | 개선 없음 |

- 회귀: `test_semantic_contract_equivalence.py` 7 passed (unit_token 신규 포함).

## 결론 (정직 — 내 가설+오너 제안 둘 다 기각)
1. **off 8 → on 3~5 재확인**: ② 의미배선은 작동한다(1차 off=7과 일치). 이건 변함없다.
2. **v3·v4·v5 전부 v2 미개선.** 전달 방식(강제형/인라인/둘다)을 바꿔도 ②축 할루시가 **~4/12 바닥에서 안 내려간다.** v3 강제형은 외려 **날조를 늘렸다**(강한 명령형의 부작용).
3. **바닥(floor)의 정체 = 전달 실패가 아니다 = 콘텐츠/이름 문제.** 잔존 오류 지배 요인:
   - **frequency_ratio 강-prior** — 블록·강제형·인라인 **3가지 전달 전부** '구매빈도/횟수'로 오독. 프롬프트로 안 꺾인다는 게 *3중 입증*됐다.
   - promotion_roas trap(분모=전사 총마케팅비), msg_roi_pct(ROI≠ROAS 축) — semantic 미묘함.

⚠ noise: n=12·단일 판정기·페이로드1. "v3/v4가 v2보다 *낫다*"는 확실히 거짓, "*나쁘다*"는 불확실. 단 **"v3/v4가 v2를 이긴다"는 기각은 robust**(어느 조건도 directional 우위 없음).

## 다음 (전달 방식 막다른 길 → 콘텐츠로 전환)
- **(c) frequency_ratio류 강-prior 키 개명** (예 `impression_frequency`) → 측정으로 floor 깨지나 확인. **최우선**(3중 입증). → ★아래 3차에서 측정·확정.
- (b) provenance(v2)는 SSOT/유지보수 가치로 유지 — 할루시 감소는 애초에 v2의 몫이 아니다.
- (d) 바닥 trap이 semantic 미묘함이면 전달로 못 깬다 — floor 수용 여부는 오너 판단.

---

## 3차 — 개명 가설(v6/v7) 측정 ★확정 (`c2_ab_glossary.py 3 freq`, freq_misread 격리지표)
2차가 "전달 방식으론 안 됨, 이름이 문제"를 가리켰다 → **이름만 바꿔 측정**(캐논키 손 안 대고 하네스에서). 통제 v2(현 이름, 구매빈도 경고 포함) vs v6(`impression_frequency`)·v7(`exposures_per_reach`, 둘 다 경고 없는 깨끗한 정의).

| 조건 | 이름 | unit_error | **freq_misread** | fab |
|---|---|---|---|---|
| v2 | `frequency_ratio` (경고 있음) | 6 | **7** | 2 |
| v6 | `impression_frequency` (경고 없음) | 6 | **0** | 2 |
| v7 | `exposures_per_reach` (경고 없음) | 8 | **2** | 4 |

**결론 (강함)**: **freq_misread 7→0(v6)**. v2는 "구매빈도 무관" 경고를 *갖고도* 7/12 오독, v6은 경고 *빼고도* 0/12. **정보가 적은데 오독이 사라짐 = 이름이 원인**(프롬프트 아님). 2차 "프롬프트로 안 꺾인다"(3중) + 3차 "개명으로 꺾인다" = 가설 폐곡선. ★ "구조(이름) 고치니 할루시 사라짐"의 첫 정량 증거 = 본 프로젝트 근본원인 명제 실증.
- ⚠ v6(0) > v7(2), v7은 unit/fab도 높음 → **업계 표준 용어**(impression_frequency=Meta 공식)가 신조어보다 우월. **개명은 표준 용어로.**
- unit_error 6~8 잔존 = 다른 키(msg_roi·promotion_roas) 문제, frequency 무관.
- ⚠ noise: n=12·단일 판정기. 단 7→0은 큰 효과라 noise로 설명 안 됨.

## 제안 (오너 도메인) → ★승인·적용 완료 (2026-06-19)
캐논키 `frequency_ratio` → `impression_frequency` 실개명 (오너 승인). **스코프 = 4곳 런타임**: contract YAML(SSOT) · `canonical_translator.py:65,137` · `col_dictionary.py:38` · `coverage.py:34` (+테스트·하네스·ERD doc 7). **프론트 무관**(creative `frequency`는 별필드). **데이터 마이그 0**(meta cleaned에 frequency 키 부재=P3 미추출 입증). v1/v2 혼재 없이 한 번에(back-compat alias 미추가).

### 검증 (end-to-end)
- **pilot 9/9 PASS** (`run_pilot.py`, MER=4.46 불변 — frequency는 verify 셋 밖).
- **coverage 게이트 FAIL 0**: `impression_frequency(not_)`가 DEFERRAL 정당화로 인식. (+부수: d29a582서 누락됐던 cac/cpa/aov/promotion_roas/promotion_share_pct DEFERRAL 5건 추가 — 잠복 FAIL 해소.)
- **타깃 테스트 50 passed** (semantic_contract·canonical_translator·glossary), **전체 회귀 1008 passed / 5 failed(전부 pre-existing: parquet env·DC_PERM_6·o04 — 신규 0)**.
- ERD 스키마 doc 6 + 명명결정로그 1 일관화(catalog≠code drift 0). 잔존 `frequency_ratio` = contract 개명 히스토리 주석 + 하네스 freq-mode 3차 보존만(의도적).

---

## 4차 — 남은 floor 키(promotion_roas·msg_roi) 개명 점검 ★개명 레버 경계 (`c2_ab_glossary.py 4 roas`, n=16)
frequency가 통했으니 남은 floor 키도 개명 대상인지 측정. 통제 v2 vs v8(promotion_roas→promotion_roas_x, 정의 고정).

| 조건 | proas_unit_err | msgroi_axis_err |
|---|---|---|
| v2 (promotion_roas) | 2/16 | 3/16 |
| v8 (promotion_roas_x) | 1/16 | 3/16 |

**결론 (내 접미사 가설 기각 — 정직)**:
- **promotion_roas ≠ 개명 대상**: 2→1은 노이즈(둘 다 저율). 애초 거의 안 틀림 — 명시 semantic이 이미 작동. `_x` 무효.
- **msg_roi_pct = semantic 바닥**: 3→3 불변(개명 무관=격리 정상). ROI≠ROAS는 동음이의 아닌 *개념 혼동*, 이름 이미 맞음 → 개명 안 통함.
- **개명 레버 경계**: frequency만 진짜 동음이의 충돌(7/12)이라 결정적, 나머지는 저빈도+semantic. **레버는 frequency에서 큰 것 잡고 소진.** 남은 floor(msg_roi ~3/16)는 전달·개명 둘 다 안 통하는 semantic 잔차 → **floor 수용**.
