# P4 오분류 회수 — 실측 findings (감사 주장 검증) 2026-06-18

> 상위 = [tool 심층감사 §4](tool구조_심층감사_카테고리_프롬프트_layer_2026-06-18.md) P4 · 선행 = [P2 봉합](P2_schema_taxonomy_봉합_완료보고서_2026-06-18.md). census = 워크플로 `w15r89rxj`(6 후보 + blast radius 병렬 + 적대 synthesis, 442k tok, 전 항목 file:line + 코드 실측).
> **한 줄**: P4 "오분류 회수"를 실측 검증한 결과 — **감사 주장 6건 중 4건이 틀리거나 무효**. 그리고 **category 자체가 decorative**(라우팅 미사용)라 이동은 기능이득 0. P2에 이어 "감사도 틀린다" 재확인.

## §1 후보별 verdict (코드 실측)
| 후보 | 감사 주장 | verdict | 핵심 근거 |
|---|---|---|---|
| ad_cost_total | metrics→cleaning | **❌ keep** | canonical computed total 읽어 합산 = KPI. 정제 0 (py:40-41) |
| member_guest_stats | metrics→cleaning | **❌ keep** | count+비율 KPI. member_id는 groupby 술어지 산출 아님 (py:36-40) |
| ga4_session_aggregator | metrics→cleaning | **❌ keep** | 순수 COUNT GROUP BY = KPI 분모(S067). clumi=cleaning은 방법론 *출처* (py:43,47-52) |
| category_multi_distributor | metrics→cleaning | **⚠ owner_decide (medium)** | 콤마분리+매출 균등분배 하이브리드, storage.layer=normalized 자인. 자매 channel_attribution_normalizer(정제4)=normalization. cleaning/normalization 타깃=오너 |
| grade_timeseries | comparison→metrics | **⚠ owner_decide (high)** | 단일 fetch+GROUP BY=metrics. comparison=2시점 composer 패턴(new_members_mom) 미충족. _storage.layer=computed. 단 귀속=오너 |
| review_normalizer | normalization→**text** | **❌ keep (감사 무효)** | 'text' 카테고리 자체가 없음(_schema 11개·catalog/text/ 0건). 순수 normalization(컬럼·값 표준화, 행/계산 0). 감사 slip (REVIEW_FIELD_ALIASES 'text' 필드 또는 하류 text_preprocessor 혼동) |

→ **6건 중 keep 3(감사 틀림) + 무효 1 + owner_decide 2.** clear_move = **0건**.

## §2 ★ 메타 발견 (P4의 진짜 결론)
1. **category는 decorative** (3중 증명): registry.py:75-89 로드 시 enum 검증만 / 라우팅은 이름 기반(agent_pool.py:42 `tool_names={t['name']...}`, AgentSpec=team_catalog tools[].name) / grep `ToolCategory`·`.category` over planning = data-output 키 1건뿐, execution = 0. → **카테고리 이동 = 기능이득 0.**
2. **감사 "정제위장4"는 stale 메타 라벨에 속았다.** 4 tool의 `clumi_methodology:cleaning`은 *C:LUMI 방법론 정제규칙 N 충족*이라는 **provenance 메타**지 코드 카테고리 아님. 방법론 정제규칙은 코드 카테고리로 1:1 안 감(tool_매핑 §1.3: 정제1·3→cleaning, 2·4·8·9→normalization, 5·6·7·10→집계). 감사가 provenance를 category로 오인 → **과대진단.** **진짜 결함 = 카테고리 오분류가 아니라 한 tool 안 3신호(category/tags/clumi_methodology) 불일치.**
3. **진짜 폴더 이동은 비쌈** (registry.py:227-231이 모듈경로를 yaml 폴더에서 유도): yaml 옮기면 .py 동반 이동 → 하드코딩 import ~15곳 깨짐(dashboard1.py:60,78,79,82·signup_conversion.py:18·테스트 다수) + prose 카운트 5곳(team_catalog 주석·33_metrics.md:10·planning_stage3 프롬프트) 드리프트. **그런데 카테고리 단언 테스트는 6 후보 전부 0개** = 이동 후 lock 부재.
4. **ROADMAP 정합**: category cosmetics = 할루시 감소 0, canonical/할루시 본선의 하류. data-layer roadmap(연결테스트→DB재구축→디자인) 순서상 지금 우선순위 아님.

## §3 권장 (정직)
- **카테고리 자동 이동 ✗.** clear_move 0건 — 감사 주장 대부분 미성립 + 기능이득 0 + 비용 큼 + 귀속=오너 도메인.
- **유일 가치 = stale 신호 메타 정정** (다음 감사가 또 안 속게): ad_cost_total/member_guest_stats/ga4_session_aggregator의 tags(`methodology_cleaning_N`)·clumi_methodology(cleaning)를 코드(metrics)와 정합. **단 doc-only·코드영향 0이나 clumi_methodology 라벨은 오너 방법론-provenance 모델**이라 임의 변경 금지 — 오너 승인 후.
- **2 owner_decide**(category_multi_distributor·grade_timeseries): 별도 오너-게이트 pass(파이프라인 모델 판단 + 15 import 비용).
- **review_normalizer**: keep(감사 무효, 'text' 부재).

## §4 정직 경고
1. **이 결과는 감사(tool 심층감사 §4)를 4/6 뒤집는다** — P2처럼 감사 주장을 코드로 검증한 결과. 감사를 신뢰해 바로 옮겼으면 잘못된·비싼 이동을 할 뻔.
2. **category=decorative라 "오분류"의 실질 피해가 작다** — 다만 stale 라벨이 *감사를 오도*하는 2차 피해는 실재(본 건이 증거).
3. **clear_move=0은 소심이 아니라 규칙 충실** — signals_consistent=true가 correct을 함의하지 않음(grade_timeseries='coherent-but-wrong'), 어떤 후보도 기계적 자동이동 자격 미달.
4. 한계: 적대검증이 decorative 증명 + 2 이동후보 .py는 직접 재현·재독, 나머지 import-site/count-prose 목록은 blast_radius 근거 채택(registry.py:227-231·agent_pool.py 핵심만 spot-check 재확인).

## §5 다음 (오너 결정)
①findings만 박제·이동 skip (권장) / ②doc-only 메타 신호정정(오너 라벨 방향 확인) / ③2 owner_decide 이동(비싼·오너-게이트). 어느 쪽이든 **category 화장보다 canonical/할루시 본선 우선**이 ROADMAP 정합.
