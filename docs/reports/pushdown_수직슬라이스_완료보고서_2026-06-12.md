# 완료보고서 — data 조회 pushdown 수직 슬라이스 (마스터 [2], 2026-06-12)

> 계획 = [계획_pushdown_수직슬라이스](계획_pushdown_수직슬라이스_2026-06-12.md) v1.4 (오너 기승인 + §8 V1~V5 필수).
> 커밋: `04e0376` (ADR-031·관절·SQL override·G28) + `a28639c` (시범 tool 전환·query_iter).

## 무엇을

데이터 로드에 **조회의 두 번째 차원(범위·집계)**을 추가했다. 지금까지 tool은 "통째로 가져와 Python으로 거른다"
한 가지만 할 수 있었는데, 이제 관절(DataSource)에 `query`(부분 행)·`query_iter`(스트리밍)·`aggregate`(집계 내리기)가
생겼고, Postgres 행-테이블 소스는 이 호출을 **SQL로 내려** 처리한다. GA4 한 경로(ga4_session_aggregator)로 끝까지 증명.

## 효과 실측 (시범 tool 전체 실행 기준, run×2 평균)

| 경로 | before | after | 판정 |
|---|---|---|---|
| Postgres 시간 | 8.7초 | **2.8초 (3.1×)** | SQL GROUP BY + 투영 pushdown |
| Postgres 피크 메모리 | 63.8MB | 31.8MB | server-side 커서 |
| File 시간 | 4.9초 | 7.5초 (1.5×) | 2-pass 구조 — **V3 상한 2× 이내** |
| File 피크 메모리 | 0.1MB | **0.1MB** | **V3 비역행 충족** |

정답값 3개(24,000 / 12,496 / 1,823) + S067(2.50%) — **File·Postgres 양 경로 모두 불변** (교차 일관성 테스트 박제).

## 변경표

| 파일 | 내용 |
|---|---|
| `docs/agent_specs/adr/ADR-031_data_query_pushdown_contract.md` (신규) | 결정 5건 박제 — 필터 2연산자(텍스트 의미론)·규칙은 tool·같은답 계약·text2SQL 비채택·**마커 생존(G28)** |
| `backend/app/data_sources/base.py` | `query`/`query_iter`/`aggregate` + 기본 구현(jsonl = stream 1-pass, V3) + `stream_jsonl` ABC 승격(§8.2) |
| `backend/app/data_sources/postgres.py` | 행-테이블 SQL override — **두 모양 인지**(generic `data->>키` / typed 컬럼 직접), blob은 fallback, server-side 커서 |
| `backend/app/workspace/postgres.py` | **G28**: save가 기존 마커 또는 ≥10,000행 목록이면 save_stream 재라우팅 (blob 덮어쓰기 금지) + save_stream meta 전달 |
| `backend/app/dream_agent/tools/metrics/ga4_session_aggregator.py` | 시범 전환 — aggregate(SQL 1방) + query_iter(2컬럼 투영 스트리밍). `_extract_source` 규칙은 tool 유지(ADR-031-2) |
| `backend/tests/sprint15/test_datasource_query.py` (신규 8) | 기본 구현 — 연산자·투영·집계·빈결과·csv·**V3 스트리밍 증명**·시끄러운 실패 |
| `backend/tests/data_sources/test_datasource_query_pg.py` (신규 8) | generic/typed/blob 3모양 · **교차 일관성(ADR-3)** · G28 라우팅 2종 · **clumi 실DB 마커 가드(PQ6)** · **V5 정답값 postgres 실측(PQ7)** |
| 43 게이트 대장 v1.4 | G28 예정→**현역**, §2 관절 구현됨 + 실측, §5 오버레이 🅱 ✅ (그림·HTML 재생성, sync 2 passed) |

## §8 보완 5건 이행

- **V1 마커 복원**: 파일(진실원천, 38,319행 검증) → `save_stream` 재적재 5.4초 — 마커·generic 행-테이블·109MB blob 제거 동시 해결. 라이브 GA4 2종 마커 정상.
- **V2 = G28 현역**: save 라우팅 코드 + 실DB assert 테스트 (침묵 강등 → RED).
- **V3**: 적발 1건 — 1차 전환이 query() list 반환으로 File 피크 0.1MB→**186MB 역행** → **query_iter 신설**로 0.1MB 복원. 시간 1.5× (상한 2× 이내).
- **V4**: 착수 시점 기준선 실측 928 pass/3 fail(household·dc_perm·batch5)/2 skip → 완료 시 944 pass/**동일 3 fail**/2 skip (회귀 0, +16 신규).
- **V5**: `set_data_source(PostgresDataSource())` fixture로 정답값 3 + S067을 clumi 실DB에서 실측 (skip 0).

## 검증 수치

신규 16 테스트 green · 전체 suite 944 pass / 3 fail(기지·기준선 동일) / 2 skip · gate ledger sync 2 passed.

## 다음

1. (오너) 멀티쿼리 수술표 S1~S4 → 계획서 v2 승격 승인 = **[3] M1 시작 조건**
2. [3] A-M1 수술 — S1 표시 병합부터 (T3 재실행 임계 = 표출률 +3.2%p 초과 + systematic 0)
3. 백로그: signup_conversion 분모 월 필터 — 이번에 만든 `aggregate(where={"event_date__prefix": ...})`가 정확히 그 도구 (G19 아닌 데이터 정합 건이라 [3]와 독립, 오너 회귀값 확인 1회 필요)
4. (오너 대기) household 12행 복원 한마디
