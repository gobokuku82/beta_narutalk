# 페이지 재구성 — 결과 보고서 (2026-06-09)

> 계획서 `페이지_재구성_계획서_2026-06-09.md` 의 4 작업 적용 결과.
> 옵션 1 (보수) + fullscreen 옵션 A (Maximize2 폐기).

## 1. 적용 요약

| 작업 | 산출물 | 영향 파일 |
|---|---|---|
| **1** TopBar 라벨 변경 | `'포트폴리오'` → `'시스템'` (type `'portfolio'` 유지, localStorage 호환) | `TopBar.tsx` |
| **2** 5 페이지 이동 | report·memory·agent-observability·system-console·data-console 모두 SYSTEM_TABS 로 | `navigation/store.ts` |
| **3** agent 페이지 폐기 | `AgentChatPage.tsx` 삭제 + 라우트 + CLIENT_TABS 탭 + `SideChatPanel.onFullScreen` + Maximize2 버튼 + `GlobalLayout` onFullScreen 호출 | 5 파일 |
| **4** hitl 페이지 폐기 | `HitlCenterPage.tsx` + `store.test.ts` 삭제 + 라우트 + 양 TABS 의 탭 + Sidebar `UserCheck` 아이콘 | 5 파일 |

## 2. 신 TABS 구조

### SYSTEM_TABS (6, 구 PORTFOLIO_TABS 재구성)

| group | 항목 |
|---|---|
| (default) | 포트폴리오 (`/portfolio`) |
| 리포트 | 리포트 (`/report`) |
| 관찰 | 에이전트 관찰 (`/agent-observability`) · 메모리 (`/memory`) |
| 시스템 | System (`/system`) · DB (`/db`) |

### CLIENT_TABS (8, 17→8 -9)

| group | 항목 |
|---|---|
| 분석 (6) | dashboard · monthly · channel · trend · creatives · cost |
| AI (2) | workflow · conversations |

→ 삭제: agent / hitl (페이지). 이동: report·memory·agent-observability·system-console·data-console.

## 3. 라우트 매트릭스 — 17 → 15 (-2)

| 컨텍스트 | 라우트 |
|---|---|
| 시스템 (6) | `/`·`/portfolio`·`/report`·`/memory`·`/agent-observability`·`/system`·`/db` |
| 클라이언트 (8) | `/dashboard`·`/monthly`·`/channel`·`/trend`·`/creatives`·`/cost`·`/workflow`·`/conversations` |
| 공통 (1) | `/settings` |
| **폐기** | ~~`/agent`~~ · ~~`/hitl`~~ |

→ 시스템(7) + 클라이언트(8) + 설정(1) = **15 + index = 16** (index `/` → portfolio 별칭, 카운트는 15).

## 4. 의존성 처리

### hitl 부분 보존 (의외 발견)

`features/hitl/` 폴더 통째 삭제 시 빌드 실패 — store + PlanReviewModal 이 외부에서 사용:

| 사용처 | 의존성 |
|---|---|
| `api/hooks/useWebSocket.ts:13` | `@/features/hitl/store` |
| `components/layout/GlobalLayout.tsx:12` | `@/features/hitl/PlanReviewModal` |
| `features/workflow/WorkflowPage.tsx:30` | `@/features/hitl/store` |

→ **선택**: HitlCenterPage 만 폐기, `PlanReviewModal` + `store.ts` 보존.

| 삭제 | 보존 |
|---|---|
| `HitlCenterPage.tsx` (페이지 자체) | `PlanReviewModal.tsx` (workflow / GlobalLayout 가 hitl 인터럽트 시 사용) |
| `store.test.ts` (페이지 테스트) | `store.ts` (hitl 상태 — useWebSocket / WorkflowPage 가 구독) |

→ 사용자 의도 "페이지는 어디서도 안되도록" 정합 (페이지 라우트·탭 폐기 + 페이지 컴포넌트 삭제). 내부 store 는 워크플로우에서 사용 중이라 보존.

### agent 부분 보존 (예측됨)

`features/agent/` 의 다른 컴포넌트들은 보존:
- `SideChatPanel` · `chatPanelStore` · `ChatTodoCard` · `PauseBox` · `Attachments` · `SlideView` · `PhaseIndicator` · `store`
- 사용처: `GlobalLayout` · `useWebSocket` · `agent_observability` · `TopBar`

→ AgentChatPage 페이지만 폐기.

### SideChatPanel `onFullScreen` 처리 (옵션 A)

| 변경 | 위치 |
|---|---|
| `onFullScreen?` prop 폐기 | `SideChatPanel.tsx` interface |
| Maximize2 버튼 + 핸들러 폐기 | `SideChatPanel.tsx` body |
| `Maximize2` import 폐기 | `SideChatPanel.tsx` lucide import |
| `<SideChatPanel onFullScreen={() => onNavigate?.('/agent')} />` | `GlobalLayout.tsx` → `<SideChatPanel />` |
| `onNavigate` prop → `_onNavigate` (RootLayout 호출 backward 호환) | `GlobalLayout.tsx` |

## 5. 검증

```
=== build (typecheck + vite build) ===
✓ built in 9.04s

=== audit:tokens ===
✅ 모든 룰 정합 (5/5)
```

### 추가 정리 (audit 진행 중)

본 작업 외 발견되어 같이 정리:
- `GlobalLayout.tsx` 의 `currentPath` prop 미사용 → `_currentPath` rename (Phase 1 작업의 후처리)

## 6. 미래 변경 용이성 진단 (사용자 질문 답)

| 측면 | 평가 | 근거 |
|---|---|---|
| **페이지 추가** | 매우 쉬움 | 3 곳만: 페이지 파일 + `router.tsx` + `store.ts`. `[tag]` 컨벤션으로 자취 추적 |
| **페이지 삭제** | 의존성 확인 필요 | hitl 처럼 외부 사용 있으면 부분 보존 (페이지만 삭제, store 등 보존). 본 작업이 좋은 예 |
| **페이지 이동 (그룹 변경)** | 매우 쉬움 | `store.ts` 의 배열에서 한 줄 옮김 |
| **컨텍스트 의미 재정의** | 일회성으로 큼 | 본 작업이 그 예 (10 호출처 + 5 페이지 이동). 단 라벨만 변경 시 매우 쉬움 |
| **자취 추적** | 우수 | spec 64 §6 자취 + routes/README.md + 본 보고서 박제 |

**잦은 페이지 변경 시 부담 평가**: **낮음**. 페이지당 5~10분. `[tag]` 컨벤션 + `grep` 으로 자취 추적 + audit:tokens 로 자동 검증.

**개선 후보** (필요 시):
- (a) `route + tab + icon` 단일 source (route metadata 에 sidebar 정보 박제) — 현재 3 곳 분산, over-engineering 위험
- (b) `audit:tabs.sh` (tab path 와 route path 정합 검증) — 단순 추가 가능
- (c) localStorage migration helper (context type 변경 시) — 필요할 때 작성

→ 현재 패턴이면 **(b) 만 추가하면 충분**. (a)·(c) 는 over-engineering.

## 7. 사용자 의사 결정 항목 (보고 후)

| 결정 | 의미 |
|---|---|
| **(a)** Sidebar default = expanded? | 현재 collapsed default. 5 페이지 이동 후 SYSTEM 탭 가독성 ↑ 위해 default expanded 도 가능 |
| **(b)** group label 변경? | "관찰" / "리포트" / "시스템" 등. 다른 명칭 원하면 변경 |
| **(c)** `/agent-observability` 짧은 라벨? | "에이전트 관찰" (현) vs "관찰" (group 명과 중복 가능) |
| **(d)** type `'portfolio'` → `'system'` rename (Phase 2)? | localStorage migration + 10 호출처 영향 |
| **(e)** audit:tabs.sh 추가? | tab path ↔ route path 정합 자동 검증 |
| **(f)** 다음 작업 | 데이터/백엔드 / 빈 페이지 완성 (Settings·AgentObservability 등) / 다른 영역 |

## 8. 자취

- **2026-06-09 (계획서)** : `페이지_재구성_계획서_2026-06-09.md`
- **2026-06-09 (적용)** : 본 보고서 — 4 작업 적용 + 검증 통과 + hitl 부분 보존 발견
