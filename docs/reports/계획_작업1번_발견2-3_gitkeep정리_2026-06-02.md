# 계획서 — 작업 1번 / 발견 2-3 / .gitkeep + 빈 폴더 정리 (2026-06-02)

> 작업 1번 (frontend 코드 명확성 진단) 의 1차 발견 2-3 (死 marker + 빈 폴더) 처리 계획.
> 사용자 박제 패턴 = 계획서 V1 → 1·2차 적대적 검증 → V2/V3 → 사용자 승인 → 진입.

---

## §0 정정된 분석 (1차 진단 오류 자취 박제)

### 0.1 1차 진단 오류

| 항목 | 1차 surface | spot-check 정정 |
|---|---|---|
| `.gitkeep` 건수 | 13 | **15** (count 오류) |
| 빈 폴더 | 1 (markdown) | **5** (markdown + attachments + auth + routes/conversations + routes/settings) |

**원인**: Glob 결과 15건 나열 후 "13곳" 으로 잘못 셈. `features/auth` `routes/conversations` `routes/settings` 빠뜨림.
**교훈**: 진단 surface 전 count 자체 spot-check 필수.

### 0.2 정정된 .gitkeep 15건 객관 분류

| 분류 | 정의 | 건수 | 폴더 |
|---|---|---:|---|
| **A 死 marker** | 살아있는 폴더 + `.gitkeep` 잔존 = 불필요 | 10 | `components/ui`, `components/layout`, `api/hooks`, `features/{agent, hitl, workflow, conversations, memory, session, settings}` |
| **B placeholder** | 빈 폴더 + `.gitkeep` 만 = 의도 확인 필요 | 5 | `components/markdown`, `features/{attachments, auth}`, `routes/{conversations, settings}` |

### 0.3 분류 B 5건 의도 가설

| 폴더 | 자취 / 의도 가설 |
|---|---|
| `components/markdown/` | markdown 컴포넌트 구현 대기 placeholder? chat agent 응답 렌더링 등에 필요 가능 |
| `features/attachments/` | 첨부파일 기능 = 사용자 명시 "구현 대기" 범주 |
| `features/auth/` | 인증 = POC 외 (multi-user 일 때 필요), 명시적 구현 대기 |
| `routes/conversations/` | TanStack **file-based** router 자취 (현 router.tsx = code-based) = **不일치 死코드** 의심 |
| `routes/settings/` | 동일 |

**객관 의심**: `routes/conversations`, `routes/settings` = 과거 TanStack file-based router 시도 자취. 현 `router.tsx` 가 code-based 단일 진실 = **不일치 死코드** = 즉시 폐기 후보.

---

## §1 계획 V1

### 1.1 commit 단위

| commit | 대상 | risk |
|---|---|---|
| **C1** 분류 A 10건 `.gitkeep` 제거 | 살아있는 폴더의 死 marker | 0 (gitkeep 의 본래 목적 = 빈 폴더 추적, 폴더에 파일 있으면 무용지물) |
| **C2** routes/{conversations, settings} 폴더 폐기 | TanStack file-based router 不일치 잔존 | low (현 router.tsx 가 code-based 단일 진실, 의존 없음 검증 후) |
| **C3** 분류 B 잔존 3 (markdown / attachments / auth) 의도 박제 | placeholder 의도 명시 | 0 (코드 변경 X, README 또는 .gitkeep 주석만) |

### 1.2 C1 — 死 marker 10건 제거

대상 (10):
```
frontend/src/components/ui/.gitkeep
frontend/src/components/layout/.gitkeep
frontend/src/api/hooks/.gitkeep
frontend/src/features/agent/.gitkeep
frontend/src/features/hitl/.gitkeep
frontend/src/features/workflow/.gitkeep
frontend/src/features/conversations/.gitkeep
frontend/src/features/memory/.gitkeep
frontend/src/features/session/.gitkeep
frontend/src/features/settings/.gitkeep
```

명령 (PowerShell):
```powershell
# 모두 살아있는 폴더 = git rm 단순
Remove-Item -Path frontend/src/components/ui/.gitkeep, frontend/src/components/layout/.gitkeep, ... -Force
```

검증:
- 폴더 자체는 잔존 (다른 파일 보유)
- frontend type-check exit 0 유지

### 1.3 C2 — routes/{conversations, settings} 빈 폴더 폐기

검증 선행:
- `grep -rn "routes/conversations\|routes/settings" frontend/src` = **No files found** ✓ (이미 spot-check 통과)
- router.tsx 의 conversationsRoute / settingsRoute 는 `features/` 에서 import (file-based 와 무관)

명령:
```powershell
Remove-Item -Recurse -Force frontend/src/routes/conversations, frontend/src/routes/settings
```

검증:
- frontend type-check exit 0
- 라우트 정상 작동 (code-based router.tsx 만 영향)

### 1.4 C3 — 분류 B 잔존 3 placeholder 의도 박제

`features/attachments/` `features/auth/` `components/markdown/` 는 **구현 대기 의도** 박제 후 .gitkeep 유지.

방식:
- 각 폴더에 짧은 README.md 추가 (의도 박제)
- 또는 .gitkeep 에 인라인 주석 (gitkeep 가 binary 파일이라 주석 불가 → README.md 권장)

내용 예 (`features/auth/README.md`):
```markdown
# auth (placeholder)

POC 단계 미구현. multi-user 진입 시 활성화.
- 2026-06-02 placeholder 박제 (작업 1번)
```

### 1.5 commit message

```
chore(frontend): 작업 1번/발견 2-3 — .gitkeep 死 marker 정리 + 빈 폴더 의도 박제

발견 (1차 진단 정정):
- .gitkeep 15건 (1차 surface 13 → spot-check 정정 15)
- 빈 폴더 5건 (1차 1 → 정정 5)

C1 (본 commit, 10건): 살아있는 폴더의 死 marker .gitkeep 제거
  - components/ui, components/layout, api/hooks
  - features/{agent, hitl, workflow, conversations, memory, session, settings}

검증:
- frontend type-check exit 0
- baseline 불변 (sprint13+14 293/11/2 + dashboard1 303/3 + ...)

박제:
- docs/reports/계획_작업1번_발견2-3_gitkeep정리_2026-06-02.md
```

C2/C3 = 별도 commit.

---

## §2 검증 방법

### 2.1 회귀 baseline

| 검증 | 기대 |
|---|---|
| `cd frontend && pnpm exec tsc --noEmit` | exit 0 |
| `cd frontend && pnpm test --run` (vitest) | 기존 통과 갯수 유지 |
| `cd backend && uv run pytest tests/sprint13 tests/sprint14 -q` | 293/11/2 |
| `git status` | C1 commit 후 working tree clean |

### 2.2 의존성 검증 (제거 전)

각 .gitkeep 가 코드에서 참조되는지:
```bash
grep -rn "\.gitkeep" frontend/src
grep -rn "routes/conversations\|routes/settings" frontend/src
```

기대 = 모두 No match (gitkeep 은 git 만의 marker, 코드 무관).

### 2.3 rollback 절차

문제 발생 시:
```bash
git revert <commit-hash>
# 또는 빈 폴더 복원
mkdir -p frontend/src/routes/conversations frontend/src/routes/settings
touch frontend/src/routes/{conversations,settings}/.gitkeep
```

---

## §3 risk / blast radius

| risk | 등급 | 완화 |
|---|---|---|
| `.gitkeep` 제거로 의존 깨짐 | **無** | gitkeep = git 만의 marker, JS/TS 코드 무관 |
| routes/{conversations, settings} 폴더 폐기로 라우팅 깨짐 | **low** | grep "No files found" 확인 + router.tsx 가 code-based 단일 진실 |
| 빈 폴더 README 추가가 의도와 안 맞음 | **low** | 사용자 승인 후 진입 |
| 추가 .gitkeep 누락 (16번째?) | **無** | `find . -name ".gitkeep"` 전수 확인 ✓ |

---

## §4 사용자 결정 필요 (분류 B placeholder 의도)

분류 B 5건 중 routes/{conversations, settings} 2건은 **不일치 死코드 의심** 으로 폐기 권장.
나머지 3건 (markdown, attachments, auth) 는 사용자 의도 확인 필요:

| 폴더 | 옵션 (A) 유지 + README 박제 | 옵션 (B) 폐기 |
|---|---|---|
| `components/markdown/` | 향후 markdown renderer 진입 시 활용 | chat agent 메시지 = 이미 다른 컴포넌트 사용 중일 가능성 |
| `features/attachments/` | 사용자 명시 "구현 대기" 범주 = 유지 | POC 외 불요 |
| `features/auth/` | multi-user 진입 시 활성 = 유지 | POC = single-client clumi 외 불요 |

권장 = **(A) 유지 + README 박제** (사용자 명시 "구현 대기" 박제 정합).

---

## §5 1차 적대적 검증 (Plan agent, 완료)

### 5.1 잘못된 가정 점검
- **".gitkeep = git 만의 marker" — 참** (`grep gitkeep frontend/src` no match, Vite/Vitest/tsconfig/IDE 의존 0)
- **"routes/conversations = file-based 자취" — 추정 → 확정**. 결정적 증거 = `frontend/src/routes/README.md` (Sprint 0 commit `7f0d1e1`, 2025-05-13) 가 file-based 구조 (`__root.tsx`, `conversations/index.tsx`, `conversations/$id.tsx`) 명시 박제. 그 후 `router.tsx` code-based 단일 진실 채택. 빈 폴더 2건 = file-based 시도 잔존 100% 확정.
- **"분류 A 10건 = 살아있는 폴더" — 누락 없음** (전수 확인 ✓)

### 5.2 누락된 risk 발견
- **`routes/README.md` 의 file-based 라우트 매트릭스 자취** = C2 폐기 시 동시 갱신 의무 — **V1 누락** (doc-drift 위험)
- Vite/Vitest/tsconfig/playwright/CI 영향 = 全 無 (`.github/workflows/` 없음)

### 5.3 commit 단위
- C1 = 10건 한 commit = "한 의미 단위 = 死 marker 정리" 정합 ✓
- C1·C2·C3 분리 적절 (의미 다름)

### 5.4 C3 placeholder 처리
- **옵션 B (즉시 폐기) 권장 추가** — 근거:
  - `features/auth/`, `features/attachments/` = spec 61 Sprint 4·6 placeholder, import 참조 0
  - `components/markdown/` = `react-markdown` + `remark-gfm` deps 도입 완료 BUT 코드 import 0 = vibe coding 자취
  - README 박제 누적 = 박제 부담 증가 → 단순 폐기 + 재진입 시 재생성 권장
  - 그러나 사용자 명시 "구현 대기" = (A) 유지 → **사용자 결정 필수**

### 5.5 추가 死코드 발견 (범위 밖, 박제)
- **`frontend/e2e/` 폴더 부재** + `playwright.config.ts` testDir 박제 = 별개 작업 (본 계획 범위 밖)
- **`react-markdown` + `remark-gfm` deps** = `components/markdown` 폐기 시 함께 검토 (C3 옵션 B 선택 시)

---

## §6 2차 적대적 검증 (Explore agent, 완료)

### 6.1 가설 4개 객관 결론

| 가설 | 결론 | 근거 |
|---|---|---|
| H1: routes/conversations·settings = file-based 자취 | **참** | router.tsx 121-140 code-based + `routeTree.gen.ts` 없음 + vite.config TanStack plugin 없음 + 코드 참조 0 |
| H2: .gitkeep 15건 빌드/테스트/CI 영향 0 | **참** | package.json/vite.config/vitest.config 참조 0 + `.github/workflows/` 없음 |
| H3: 살아있는 10 폴더 모두 .gitkeep 외 파일 보유 | **참** | components/ui 16 / layout 7 / api/hooks 3 / features/agent 7 / hitl 4 / workflow 20 / conversations 1 / memory 1 / session 1 / settings 2 |
| H4: 빈 폴더 5건 정확 | **참** | routes/conversations·settings + components/markdown + features/{attachments, auth} (V1 정정 5건 정합) |

### 6.2 추가 발견 = 없음 (V1 분석 정정 후 정합)

---

## §7 V2 최종 — 검증 발견 반영

### 7.1 분류 B 5건 재분류 (검증 결과)

| 신규 분류 | 폴더 | 근거 | 처리 |
|---|---|---|---|
| **확정 死코드 (file-based 자취)** | `routes/conversations/`, `routes/settings/` | Sprint 0 박제 자취 확정 + 현 code-based 단일 진실 | **즉시 폐기 + routes/README.md 갱신 동시** (C2) |
| **placeholder (사용자 결정)** | `components/markdown/`, `features/attachments/`, `features/auth/` | 구현 대기 vs vibe coding 박제 부담 | **C3 옵션 A/B 사용자 결정** |

### 7.2 commit 단위 V2

| commit | 대상 | 검증 통과 |
|---|---|---|
| **C1** | 분류 A 死 marker 10건 `.gitkeep` 제거 | 1·2차 검증 ✓ |
| **C2** | routes/{conversations, settings} 폐기 + **`routes/README.md` 갱신** (file-based → code-based 박제, V1 누락 risk 반영) | 1·2차 검증 ✓ |
| **C3-A** | 분류 B 잔존 3 placeholder README 박제 (사용자 명시 "구현 대기" 정합) | 사용자 결정 |
| **C3-B** | 분류 B 잔존 3 즉시 폐기 + `components/markdown` 폐기 시 `react-markdown` + `remark-gfm` deps 점검 | 사용자 결정 |

### 7.3 사용자 surface 항목

1. **C1, C2 진입 OK?** (검증 통과, 즉시 진입 가능)
2. **C3 옵션 A vs B 결정?**
   - (A) README 박제 = "구현 대기" 명시 + 향후 진입 시 폴더 잔존
   - (B) 즉시 폐기 + react-markdown deps 점검 = vibe coding 정합 + 진입 시 재생성
3. **추가 검증 원하는지?** (3차 검증 / 별도 관점)

### 7.4 V2 → V3 진입 조건

사용자 승인 시 V3 = 최종 (C3 옵션 확정 + 진입 commit 박제). 본 V2 = 검증 완료 + 사용자 결정 대기.

---

**작성 시점**: 2026-06-02, 작업 1번 1차 진단 + 1·2차 적대적 검증 완료.
**상태**: V2, 사용자 결정 대기 (C1·C2 진입 가부 + C3 옵션).
**1차 적대적 검증 (Plan agent)**: 잘못된 가정 0 + 누락 risk 1건 (routes/README.md doc-drift) + C3 옵션 B 추가 권장.
**2차 적대적 검증 (Explore agent)**: 가설 4건 모두 참 + 추가 발견 0건.
