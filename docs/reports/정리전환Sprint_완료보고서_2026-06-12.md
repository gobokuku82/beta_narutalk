# 완료보고서 — 정리 전환 Sprint (죽은 코드 짝 단위 철거 + planned 마커)

> 일자: 2026-06-12 · 커밋 4건: `57ee019`(backend) · `a740f25`(루트) · `043ebe3`(frontend) · `b169622`(마커+선언)
> 분류: 정리 — 헌법 19 §5 라우팅 표·§8 슬라이스 매핑 집행. **총 ~4,320줄 삭제 / 신규 파손 0.**
> 원칙 이행: no_mixed_codebases("전환 Sprint에서 한 번에") + 죽음/미구현/유령 3분류 + 짝 단위 철거(5/31 반쪽 철거의 교훈)

---

## 1. 방법

워크플로 매니페스트(8 대상)가 세션 한도로 전멸 → **직접 targeted grep으로 대상별 caller-0 재검증 + 참조 연쇄 전수 확보** 후 집행. 모든 삭제 지점에 묘비(무엇·왜·대체재·복원=git 해시) 주석.

## 2. 삭제 내역 (① 죽음 부류)

| 대상 | 규모 | 재검증 결과 | 커밋 |
|---|---|---|---|
| models/domain.py 11종 + __init__ 재노출 | ~180줄 | 소비 0 (도구들은 ExecutionContext/ToolSpec만 import — "활성 공개 API" 큐레이션이 거짓이었음) | 57ee019 |
| core/errors.py (AgentError E1xxx) + error_handler 死경로 | ~190줄 | raise 0곳 — 소비자는 도달불가 핸들러뿐. 살아있는 generic Exception/ValueError 경로만 유지 | 57ee019 |
| core/decorators.py | 176줄 | import 0 (순수성 테스트는 파일 무관 — 통과 유지) | 57ee019 |
| session_manager/ + config 설정 2개 | ~370줄 | 참조 전부 자기 패키지. SESSION_TIMEOUT_SEC/MAX_TURNS는 session_manager조차 안 읽던 이중 죽음 | 57ee019 |
| executor.build_phases·_iso_now·미사용 import / mock_tools random·pdf 분기 | ~45줄 | build_phases 호출 0 — 활성 대체 = todo_manager._build_phases_from_plan | 57ee019 |
| 거짓 docstring 2건 (response_stage "LLM formatter" / builder "in-memory") | — | 실동작 문구로 교체 (진입점 첫인상 오독 해소) | 57ee019 |
| 루트 tests/ 죽은 v1 트리 | py 13, ~2,900줄 | pyproject testpaths=backend/tests — 수집 제외 유령. 테스트 트리 단일화 | a740f25 |
| _old_run_server.py + main.py dashboard mount/read_root + dashboard/ 2파일 | ~90줄 | mount는 디렉토리 부재로 도달 불가였음. 전용 import 3종 동반 제거 | a740f25 |
| 미추적 잔재: testsintegration__init__.py·collection/raw pycache·catalog 빈 폴더 4 | — | fs 삭제 | a740f25 |
| frontend schemas.ts mock 블록 (398-638) | 241줄 | /api/mock 폐기(05-28) 후 참조 0 — 계약 단일 진실 파일의 37.7%가 폐기 계약이었음 | 043ebe3 |
| 프롬프트 YAML 4종 llm_config 블록 (유령 선언 1차) | ~16줄 | 코드 소비 0 + 값도 실제와 다름(1500 vs 2500) → 진실소스 포인터 주석 대체 | b169622 |

## 3. 마커 부착 (② 미구현 부류 — 삭제 아님)

| 대상 | 마커 | 비고 |
|---|---|---|
| learning_manager 4파일 | `Status: planned` | Sprint 15 wiring + **date.replace 월경계 버그 수정 선행 조건 명시** |
| recovery/manager.py | `Status: partial` | 감지·메뉴까지 / resolve_choice 미배선 명시 |
| memory_manager·feedback_manager __init__ | `Status: planned` | 책임 경계 메모(episodic 설계 링크 / feedback_collector와 경계 결정 선행) |
| pipelines TriggerDef | `Status: planned` | 52/52 manual 실측 명시 |

→ 이로써 "호출자-0인데 마커 없는 코드"가 자동으로 ①죽음 의심 대상이 되는 체계 성립 (DC-10 포맷 검증 통과).

## 4. 검증

| 항목 | 결과 |
|---|---|
| backend 전체 회귀 | **869 통과 / 16 실패 / 2 skip** — 실패는 슬라이스 0 이전부터의 사전 존재 동일 목록, **신규 파손 0** |
| frontend | tsc -b 클린 + vitest **82/82** |
| DC-10 (마커 포맷) | 통과 |
| DC1 (문서 내 코드 경로) | 본 Sprint 유발분(spec 30의 hitl/domain 경로 3건) 해소 — 잔여 실패는 전부 사전 존재(아래 §5) |
| 스모크 | models/main/executor/mock_tools/error_handler import 전부 OK + YAML 4종 파싱 OK |

## 5. 범위 밖 잔여 (의도적 보류 — 다음 처분처 명시)

- **`_storage` 30곳 emit + tools/catalog storage 블록**: 헌법 라우팅 표 삭제 판정이나 dashboard1 회귀 테스트 연쇄가 커서 별도 단위로 (유령 선언 2차).
- **ToolSpec dependencies/timeout/retries 등**: admin API→팔레트 *표시* 소비가 있어 완전 죽음 아님 — 표시 유지 or 동반 제거 결정 필요.
- **ttl_seconds**: 구현 가치 검토 대상 (헌법 §5 — 캐시 신선도 H3와 연결, 슬라이스 2~3).
- **docs/specs/ 구세대 트리** (16파일, git 추적): agent_specs가 대체한 옛 spec 트리 — 삭제 or POC_legacy 식 보존 결정 대기.
- **사전 존재 문서 부패** (opt-in DC 테스트): mock_data.py 경로 잔재 5건(spec 15/63/66) · 깨진 문서 링크 11건 · 버전 메타 불일치 20건.
- **사전 존재 테스트 실패 16건** (hitl_timeout_integration 10 등 — 0-① 보고서 §4와 동일).
- errorMessages.ts·queryKeys.ts·Memory 스키마: **보존** (슬라이스 2-⑤/Sprint 5+ 배선 예정 — 삭제 금지로 명시).

## 6. 다음

**슬라이스 1 — period 정직** (헌법 §8, DoD=G2): ①오염원 5곳+카탈로그 produces의 period 제거 ②SCOPE_PARAMS 주입 금지 ③경계 YYYY-MM 검사(+_resolved_month 보강) ④executor validate_params ⑤gaps→"기간을 알려주세요" 정직 문구. D2(거부)·D3(정직 degrade) 비준 완료로 의존성 0.
