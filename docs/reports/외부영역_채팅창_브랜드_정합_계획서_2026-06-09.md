# 외부 영역 + 채팅창 + 브랜드 정합 — 계획서 (2026-06-09)

> 본문(트렌드·monthly 등) 디자인 시스템 적용 완료. 외곽 영역 (Sidebar·TopBar·SideChatPanel·ChatTodoCard) 과 브랜드 워드마크는 미적용.
> 사용자 결정 = **적극 결 (구조 변경 동반)** + **C+D 브랜드 조합 (◈ OctorAD ·)**.

## 1. 의도

| 측면 | 결정 |
|---|---|
| 본질 | 페이지 본문이 디자인 시스템 정합인데 외곽이 따로 노는 인상 제거 |
| 스코프 | Sidebar · TopBar · SideChatPanel · ChatTodoCard · 브랜드 워드마크 5 영역 |
| 강도 | **적극 결** — 톤 정합 + 구조 변경 동반 (collapsed/expanded · 메시지 구조 · 작업 단계 재배열) |
| 제약 | spec_64 8 룰 + PALETTE.md §8 효과 카테고리 안에서만 |

## 2. 진단 (4 영역 + 브랜드)

| # | 영역 | 위반/문제 |
|---|---|---|
| A | Sidebar | `bg-stone-900` 다크 strip + `text-stone-400` (Tailwind 외부, 본문 warm beige 와 톤 충돌) |
| B | TopBar | 토큰 정합 ✅. 단 `OctorAD` 평범한 굵은 텍스트, 브랜드 식별성 약함 |
| C | SideChatPanel | `bg-green-500` 연결 dot · 사용자 메시지 `bg-primary` 큰 옥스블러드 면 (강도 과함) |
| D | ChatTodoCard | `bg-yellow-500/15`·`text-yellow-700`·`bg-green-500/15`·`text-green-700` (Tailwind 외부 4 색) |
| E | 브랜드 | "OctorAD" 한 단어, 브랜드 색·마크·구분자 없음 |

## 3. 5 Phase 작업 분할

### Phase 1 · Sidebar (적극 결)

| 변경 | 룰 / 효과 |
|---|---|
| `bg-stone-900` → `bg-card border-r border-border` (라이트 뉴트럴 strip) | PALETTE 토큰 정합 |
| `text-stone-400` → `text-muted-foreground` | PALETTE 토큰 정합 |
| active `text-white bg-stone-800 border-l-2 border-primary` → `bg-accent text-foreground border-l-2 border-primary` | PALETTE §8.2 I (card lifting) + J (color strip) |
| **collapsed/expanded toggle** 신설 — store 추가, `w-20`(collapsed) ↔ `w-56`(expanded) | 적극 결, 구조 변경 |
| 활성 라우트 = currentPath 가 아니라 `useRouterState` 로 reactive | bugfix (이미 currentPath prop X) |

### Phase 2 · 채팅창 토큰 정합

| 변경 | 룰 |
|---|---|
| SideChatPanel: `bg-green-500` 연결 dot → `bg-success` | PALETTE |
| SideChatPanel: 검토 토글 `bg-green-500/10 text-green-700` → `bg-success/10 text-success` | PALETTE |
| ChatTodoCard: `bg-yellow-500/15 text-yellow-700` → `bg-warning/15 text-warning` | PALETTE |
| ChatTodoCard: `bg-green-500/15 text-green-700` → `bg-success/15 text-success` | PALETTE |

### Phase 3 · 사용자 메시지 구조 변경

| 변경 | 룰 / 효과 |
|---|---|
| 사용자 메시지 `bg-primary text-primary-foreground rounded-lg` 큰 면 → `border-l-2 border-primary bg-card text-foreground` (좌측 strip + 카드) | PALETTE §8.2 J (color strip) |
| `ml-8` 우측 정렬 유지, 메시지 카드 hairline border | B hairline 차등 |

### Phase 4 · 브랜드 워드마크 (◈ OctorAD ·)

| 변경 | 구체 |
|---|---|
| TopBar `<h1>OctorAD</h1>` → `<h1><DiamondMark /> OctorAD <PrimaryDot /></h1>` | C 마크 + D dot suffix |
| 마크 = lucide-react `Diamond` (4×4 size) 또는 custom svg | text-foreground |
| 우측 dot = `<span className="text-primary">·</span>` 또는 작은 원 | 브랜드 색 표시 |
| Sidebar 의 footer 또는 expanded view 상단에도 동일 워드마크 | 브랜드 일관성 |

### Phase 5 · TopBar 마이크로 정리

| 변경 | 룰 |
|---|---|
| 컨텍스트 토글 chip `gap-1 rounded-lg bg-muted p-1` 그대로 (정합) | — |
| 연결 상태 dot 크기 조정 (`w-2 h-2` → `w-1.5 h-1.5`) | dense |
| 사용자 아바타 `bg-secondary` → `bg-accent` (브랜드 색 family) | PALETTE |
| 채팅 토글 active 상태 `bg-primary` → `bg-accent text-accent-foreground` (덜 강한 옥스블러드) | 톤다운 |

## 4. OctorAD 워드마크 — 구체 시뮬레이션

```
[현재]
OctorAD

[Phase 4 후]
◈ OctorAD ·
│  │       └─ text-primary dot (옥스블러드, 브랜드 색 표시)
│  └─ Pretendard sans, text-base font-semibold tracking-tight
└─ Diamond mark (text-muted-foreground/60, h-3.5 w-3.5)
```

→ 미니 마크 + 워드 + 액센트 dot 의 3 요소. AI 어감 X (이모지 아님, 도형 lucide-react), 브랜드 색 명시.

## 5. 페이지 효과 매핑 (PALETTE.md §8.3 확장)

| 영역 | 적용 효과 |
|---|---|
| Sidebar | I card lifting (active) · J color strip (active border-l) · B hairline (보더) |
| TopBar | C 타이포 위계 (OctorAD 강조) · B hairline (border-b) |
| SideChatPanel | B hairline (메시지 카드) · J 사용자 메시지 좌측 strip · F message reveal motion (다음 단계) |
| ChatTodoCard | B hairline (단계 사이 divider) · J 진행 strip (선택, 후속) |

## 6. 작업 추정

| Phase | 영향 파일 | commit | 분 |
|---|---|---|---|
| 1 Sidebar | `Sidebar.tsx` + sidebar store 신설 + GlobalLayout | 1 | 40 |
| 2 채팅창 토큰 | `SideChatPanel.tsx` `ChatTodoCard.tsx` `PauseBox.tsx` `PhaseIndicator.tsx` | 1 | 20 |
| 3 사용자 메시지 | `SideChatPanel.tsx` | 1 | 15 |
| 4 브랜드 워드마크 | `TopBar.tsx` + 새 `Brand.tsx` 컴포넌트 | 1 | 25 |
| 5 TopBar 정리 | `TopBar.tsx` | 1 | 10 |

→ 총 5 commit / 약 2 시간. 페이지별 보고 + 사용자 redirect 가능.

## 7. 자취

- **2026-06-09 (계획서)** : 본 문서 박제. Phase 1 시작 권장.
