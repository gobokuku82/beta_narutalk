# 수집 재구조화 — 검증 로드맵 (2026-05-28)

> 작업: ① 위치정리(_base) ② 제거(Sprint15·잔재) ③ data 폴더 연결(external→mock_api / internal→data/{client}).
> **원칙: 각 단계 = 작업 → 검증 게이트 통과 → 커밋. 실패 시 즉시 git revert.** (memory `feedback_test_no_resource_limit`)

---

## 0. Golden 불변 (이게 깨지면 회귀)

**베이스라인 (2026-05-28 측정): `291 passed / 14 failed`.**
- 14 fail = **전부 `pyarrow` 미설치(환경)** — `dashboard1/test_active_orders_filter`·`test_member_metrics_validator`·`test_storage_backend`. **기존 실패, 작업 무관 → 회귀 판정에서 제외.**
- 회귀 기준 = **291 passed 유지 + 신규 실패 0** (parquet 14 외 새 실패 나오면 회귀).

**검증 명령 (golden suite):**
```
uv run pytest backend/tests/pipelines backend/tests/dashboard1 backend/tests/data_sources backend/tests/permissions backend/tests/ml_models -q
```

## 1. 핵심 정답값 (절대 불변 — 변하면 데이터/로직 오염)

| 항목 | 값 | 검증 위치 |
|---|---|---|
| S001 총매출 (2026-04) | **119,539,660** | pipelines/test_batch1 |
| orders count | 3,420 | dashboard1/test_collectors |
| customers | 8,500 | 〃 |
| signup_events | 600 | 〃 |
| customer_grade_history | 30,379 | 〃 |
| ga4_traffic_source | 38,319 | 〃 |
| naver_advoost | 90 / household 12 / category_sales 155 | 〃 |
| 52 pipeline | 전부 green | pipelines/test_batch1~6 |
| DC-PERM-1~6 | 신규위반 0 | permissions |

## 2. 단계별 작업 + 검증 게이트

### Phase 1 — 위치정리 (_base) 〔저위험〕
| | 내용 |
|---|---|
| 작업 | `collection/raw/_base.py` → `collection/_base.py`. 21 collector import `collection.raw._base`→`collection._base`. test_collectors.py import 2곳 수정. 빈 `collection/raw/` 제거 |
| 검증 | golden suite → 291 passed 유지. 특히 test_collectors(21 등록·count) green |
| 통과 | 회귀 0 → 커밋 |
| 롤백 | `git revert` (이동/치환만이라 안전) |

### Phase 2 — 제거 (Sprint15·잔재) 〔중위험: agent catalog〕
| | 내용 |
|---|---|
| 작업 | Sprint15 커넥터 6 (`collection/*.py` + catalog 6) 제거 (broken+중복, E5). 참조 정리: `team_catalog.yaml`·planning 프롬프트·`tests/sprint15/test_*_collector*`·`helpers.load_mock_csv`(unused 시) |
| 사전 | grep 으로 6 커넥터 클래스·이름 **전 참조 전수** 확인 후 제거 (memory 삭제전 검증) |
| 검증 | registry 로드 누락 0 + golden suite + `pytest backend/tests/sprint15`(조정 후) + grep 잔존참조 0 |
| 통과 | 회귀 0 + 잔존참조 0 → 커밋 |
| 롤백 | `git revert` |

### Phase 3 — data 폴더 연결 〔고위험: 데이터 모델〕
> external = `data/mock_api/{client}` / internal = `data/{client}`. **⚠️ mock_api 는 현재 옛 포맷(clumi_mock_NN+옛 컬럼), raw 는 신 포맷.** 경로만 바꾸면 정답값 깨짐 → **데이터 준비가 선행 게이트.**

| 단계 | 내용 | 검증 게이트 |
|---|---|---|
| 3.0 | **데이터 준비**(사용자 영역): external source 13개를 `data/{client}/external/`(또는 mock_api) 에 **신 포맷**으로, internal 8개를 `data/{client}/internal/` 에 배치. 없으면 설계문서 후 생성 | 파일 존재·포맷 = §1 count 와 동일해야 |
| 3.1 | `FileDataSource` 를 **kind 라우팅**: `source_kind(sid)` 로 external→external경로, internal→internal경로 (현 `raw/` 단일 → kind별 분기) | 단위테스트: 각 kind 올바른 경로 |
| 3.2 | 외부커넥터가 mock_api(API mock) 읽도록 (선택; 2-layer ingest) | golden + §1 정답값 |
| 3.3 | `clumi_loader` 제거 (CLUMI_SOURCES 의존 끊긴 후) | grep 참조 0 + golden |
| 통과 | **§1 정답값 전부 불변** + golden 291 → 커밋 | — |

## 3. 진행 규칙
1. Phase 1 → 2 → 3 순서. **각 단계 통과 못 하면 다음 진행 금지.**
2. 단계마다: 작업 → golden suite → (정답값 확인) → 커밋. 실패 = revert 후 원인 분석.
3. Phase 3 는 **데이터 준비(3.0)** 가 안 되면 시작 안 함 (경로만 바꾸면 정답 깨짐).
4. data/ 는 gitignore — 데이터 이동은 커밋에 안 잡힘. 코드/경로 변경만 git 반영.

## 변경 이력
| 일자 | 내용 |
|---|---|
| 2026-05-28 | 초안 — golden(291/14환경) + 정답값 9종 + Phase1(위치)·2(제거)·3(data연결) 검증 게이트. |
