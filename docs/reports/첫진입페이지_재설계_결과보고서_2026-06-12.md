# 첫 진입 페이지 (PortfolioPage) 재설계 — 결과보고서 (2026-06-12)

> 계획서: `docs/reports/계획_첫진입페이지_재설계_2026-06-12.md`
> 사용자 결정: 권장대로 진행 (Q1 A+C+D+B / Q2 V2+V5 / Q3 흰색 단색 + shadow-sm + hover ring + gap-6 / Q4 폰트 유지 + 위계 표 §6.2 / C1~C4 모두 폐기 / C5 카드 그리드).

## 1. 변경 요약

### 1.1 신규 컴포넌트

| 파일 | 역할 |
|---|---|
| `WelcomeHero.tsx` | 첫 진입 강조 섹션 — 좌측 옥스블러드 strip + 모노 라벨 + 큰 한국어 제목 + 부제 + 슬로건 |
| `PersonaCards.tsx` | 페르소나 3 카드 그리드 — Data Analyst (활성, 옥스블러드 strip) + Campaign Designer / Media Director (예정, dimmed + "예정" 라벨) |

### 1.2 폐기 컴포넌트 (git rm)

| 파일 | 폐기 이유 |
|---|---|
| `MirofishHeader.tsx` | 트레이딩 시뮬 결 ↔ 마케팅 분석 도메인 미스매치 |
| `OverviewCard.tsx` | mock 의존 (실데이터 시점 약함) |
| `YouTubeEmbed.tsx` | 영상 없는 단계, 후속 추가 (Q4=Y 결정으로 영상 만들 때 재추가) |

### 1.3 PortfolioPage 구성 (6 → 4 섹션)

| # | 섹션 | 컴포넌트 |
|---|---|---|
| 1 | **Welcome Hero** | `WelcomeHero` (신규) |
| 2 | **4-Layer Diagram** | `AgentLayerDiagram` (보존) |
| 3 | 시스템 컨텍스트 6 | `PageGroupSection` |
| 3 | 클라이언트 컨텍스트 8 | `PageGroupSection` |
| 4 | **페르소나 비전** | `PersonaCards` (신규) |

## 2. 디자인 지침 정합

| 룰 | 적용 |
|---|---|
| PALETTE | Warm Neutral + 옥스블러드 단일 액센트 (Hero strip + 활성 Persona). 그라데이션·glow 0 |
| TYPOGRAPHY | 위계 표 §6.2 적용 (Hero text-3xl/4xl bold / Section text-base semibold / 카드 text-sm semibold / 모노 라벨 text-2xs uppercase tracking-wider) |
| SPACING | `max-w-7xl px-4 py-6 gap-6` (4px 그리드, 임의값 0) |
| RADIUS | `rounded-lg` (Card) / `rounded-sm` (페르소나 예정 배지) |
| MOTION | `transition duration-200` (PersonaCard hover) |
| ELEVATION | `shadow-sm` (PersonaCard, E2 xl/2xl 금지 정합) |
| LAYOUT | `mx-auto max-w-7xl px-4 py-6` |
| VOCABULARY | Welcome Hero / Layer Diagram / Page Group Card / **Persona Card** (v12 신설) |
| Enforcement | `pnpm audit:tokens` + `pnpm tsc` 변경 영역 0 위반 |

## 3. 검증

| # | 검증 | 결과 |
|---|---|---|
| A1 | `pnpm tsc --noEmit` (변경 영역 portfolio/welcome/persona) | ✅ 에러 0 |
| A2 | `pnpm audit:tokens` (변경 영역) | ✅ 위반 0 |
| A3 | grep 잔존 import (MirofishHeader / OverviewCard / YouTubeEmbed) | ✅ 0건 |

외부 영역 잔존 (`agent/CardAsk·ChatSkeleton` + `conversations`) = 별도 작업 영역, (d) 작업 때부터 잔존.

## 4. 어휘 정리 (VOCABULARY §2 + §6 v12)

### 신설

- **Persona Card** = 멀티 페르소나 비전 카드 그리드 (1 활성 + N 예정)

### 갱신

- **Welcome Hero** (v2) — 차분한 Apple/Stripe 결로 재정의

### 폐기

- Session Header
- Overview Card
- Stage Strip
- Video Frame
- Persona Tab (Persona Card 로 대체)

## 5. 시각 확인 (Ctrl+Shift+R)

`/` 또는 `/portfolio` 접속 시:

```
[ Welcome Hero ]
  DATA · ANALYST                          (모노 2xs uppercase, 옥스블러드 좌측 strip 옆)
  OctorAD                                 (text-4xl bold tracking-tight)
  데이터분석가                            (text-xl semibold)
  raw 데이터를 분석으로 변환하는 파이프라인.
  자연어로 묻고, 시각으로 답한다.         (text-sm md:text-base muted)

[ 4-Layer Diagram (AgentLayerDiagram) ]
  데이터분석가                  raw 데이터 분석에서 행동까지
  [Data] → [Analysis] → [Decision] → [Execution]

[ 시스템 컨텍스트 ]                    6 페이지 — 전체 운영 + 관찰
  [포트폴리오] [리포트] [에이전트] [메모리] [System] [DB]

[ 클라이언트 컨텍스트 ]               8 페이지 — client 별 분석 + 작업
  [대시보드] [월간 결산] [채널] [트렌드]
  [소재] [비용] [워크플로우] [대화이력]

[ Personas ]                          1 active · 2 planned
  ┌─ DATA ANALYST ──────────┐ ┌─ CAMPAIGN DESIGNER ──── 예정 ┐ ┌─ MEDIA DIRECTOR ─ 예정 ┐
  │█ 데이터분석가            │ │ 캠페인 디자이너               │ │ 미디어 디렉터          │
  │  raw 데이터에서 행동까지 │ │ 예산·채널·소재 조합 설계.     │ │ 집행 운영·학습 반영. │
  └──────────────────────────┘ └────────────────(dimmed)──────┘ └────(dimmed)───────────┘
   (활성: 옥스블러드 strip)
```

## 6. 자취

- **2026-06-12** : 본 결과보고서. 계획서 적용 완료. 사용자 시각 확인 대기.
