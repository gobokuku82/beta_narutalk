# 외부 영역 + 채팅창 + 브랜드 정합 — 결과 보고서 (2026-06-09)

> 계획서 `외부영역_채팅창_브랜드_정합_계획서_2026-06-09.md` 의 5 Phase 적용 결과.
> 사용자 결정 = 적극 결 + ◈ OctorAD · 브랜드.

## 1. 적용 요약

| Phase | 산출물 | 영향 파일 |
|---|---|---|
| **1** Sidebar 적극 결 | `bg-stone-900` 다크 → `bg-card border-r border-border` 라이트. collapsed (w-20) ↔ expanded (w-56) toggle 신설. active = `bg-accent` + 좌측 `bg-primary` strip (PALETTE §8.2 J+I). `useRouterState` reactive. `Settings` prop 폐기. | `Sidebar.tsx` (전면 재작성) · `navigation/store.ts` (isSidebarExpanded + toggleSidebar) · `GlobalLayout.tsx` (Sidebar props 제거) |
| **2** 채팅창 토큰 정합 | Tailwind 기본색 **15건** → 디자인 토큰 일괄 변환 (`bg-green-500` → `bg-success`, `text-yellow-700` → `text-warning`, `text-blue-600` → `text-primary` 등) | `SideChatPanel.tsx` · `ChatTodoCard.tsx` · `PauseBox.tsx` |
| **3** 사용자 메시지 구조 변경 | `bg-primary text-primary-foreground` 큰 옥스블러드 면 → `border-l-2 border-primary bg-muted/40` (PALETTE §8.2 J color strip + B hairline) | `SideChatPanel.tsx` |
| **4** Brand 워드마크 | 신규 `Brand.tsx` 컴포넌트 — `◈ OctorAD ·` (lucide Diamond 마크 + 텍스트 + 옥스블러드 dot suffix). TopBar 의 `<h1>OctorAD</h1>` 평범 텍스트 → `<Brand size="sm" />` | `Brand.tsx` (신규) · `TopBar.tsx` |
| **5** TopBar 마이크로 정리 | 연결 dot `w-2` → `w-1.5` / 알림·채팅 버튼 `rounded-lg` → `rounded-md` + active 톤다운 (`bg-primary` 큰 면 → `bg-accent`) / 사용자 아바타 `bg-secondary` → `bg-accent` (브랜드 family) / 날짜 `tabular-nums` | `TopBar.tsx` |

## 2. 신규/변경 컴포넌트 / Store

| 항목 | 내용 |
|---|---|
| **`Brand.tsx`** (신규) | `<Brand size="sm"|"md" compact={bool} />` — `◈ OctorAD ·` 워드마크 컴포넌트 |
| **`Sidebar.tsx`** (재작성) | props 제거 (currentPath/onNavigate). 내부에서 `useRouterState` + `useNavigate`. expanded 모드 시 라벨 표시. collapsed footer = `펼치기/접기` 토글 + 설정 |
| **`navigation/store.ts`** | `isSidebarExpanded: boolean` + `toggleSidebar()` action 추가. localStorage persist (`partialize` 에 포함) |
| **`GlobalLayout.tsx`** | Sidebar 호출에서 props 제거. `currentPath` 미사용 — `_currentPath` 로 unused 표시 (RootLayout 호출 backward 호환) |

## 3. 영역별 변경 상세

### 3.1 Sidebar — `Sidebar.tsx`

| 측면 | 이전 | 이후 |
|---|---|---|
| 배경 | `bg-stone-900` (다크 strip) | `bg-card border-r border-border` (라이트, hairline) |
| 텍스트 | `text-stone-400` / active `text-white` | `text-muted-foreground` / active `text-foreground` |
| Active 표시 | `bg-stone-800 border-l-2 border-primary` | `bg-accent` + 별도 `<span>` 으로 좌측 `w-0.5` `bg-primary` strip (PALETTE §8.2 J) |
| 폭 | `w-20` 고정 | `w-20` (collapsed) ↔ `w-56` (expanded), `transition-[width] duration-200 ease-out` |
| Active 추적 | `currentPath` prop (호출처 의존) | `useRouterState` (reactive, prop 폐기) |
| Footer | `Settings` 만, 다크 스타일 | `펼치기/접기` toggle + `설정`, 라이트 스타일 |
| 라벨 | `text-2xs` 아이콘 아래 | collapsed = `text-2xs` 아래 / expanded = `text-sm` 옆 |

### 3.2 채팅창 토큰 정합 — `SideChatPanel.tsx` `ChatTodoCard.tsx` `PauseBox.tsx`

| 패턴 | 변경 횟수 |
|---|---|
| `bg-yellow-500/{15,20,30,40,5,10}` → `bg-warning/N` | 5 |
| `text-yellow-{600,700,800}` → `text-warning` | 4 |
| `border-yellow-500/N` → `border-warning/N` | 3 |
| `bg-green-500/N` → `bg-success/N` | 2 |
| `text-green-{600,700}` → `text-success` | 2 |
| `bg-green-500` → `bg-success` (raw, 연결 dot) | 1 |
| `text-blue-600` → `text-primary` (Loader2) | 1 |
| **총** | **15+ 회 일괄 변환** |

### 3.3 사용자 메시지 구조 변경 — `SideChatPanel.tsx` line 211

```diff
- <div className="ml-8 whitespace-pre-wrap rounded-lg bg-primary px-3 py-2 text-sm text-primary-foreground">
+ <div className="ml-8 whitespace-pre-wrap rounded-md border-l-2 border-primary bg-muted/40 px-3 py-2 text-sm text-foreground">
    {m.content}
  </div>
```

→ 큰 옥스블러드 면 (액센트 색이 메시지 영역 전체) → **좌측 2px strip + warm beige 배경**. PALETTE §8.2 J (color strip) 적용. 시각 강도 ↓ , 의미 (사용자 발언) 유지.

### 3.4 Brand 워드마크 — `Brand.tsx` (신규)

```tsx
<div className="inline-flex items-center gap-2">
  <Diamond className="h-3.5 w-3.5 text-muted-foreground/60" />
  <span className="text-base font-semibold tracking-tight text-foreground">OctorAD</span>
  <span className="text-base font-semibold leading-none text-primary">·</span>
</div>
```

| 요소 | 역할 | 토큰 |
|---|---|---|
| `Diamond` mark (좌) | 브랜드 식별 마크 (Octor = 감시자 의미) | `text-muted-foreground/60` (미세 강조) |
| `OctorAD` 워드 | 브랜드 명 | `text-base font-semibold tracking-tight` |
| `·` dot (우) | 브랜드 색 표시 | `text-primary` (옥스블러드) |

→ AI 어감 X (이모지 아닌 도형 + 텍스트), 브랜드 색 명시, 미니 마크.

### 3.5 TopBar 마이크로 정리 — `TopBar.tsx`

| 항목 | 이전 | 이후 |
|---|---|---|
| OctorAD 로고 | `<h1 className="text-lg font-bold">OctorAD</h1>` | `<Brand size="sm" />` |
| 연결 dot | `w-2 h-2` | `h-1.5 w-1.5` (덜 강조) |
| 알림 버튼 | `p-2 hover:bg-muted rounded-lg` Bell `w-5` | `rounded-md` hover bg+text, Bell `h-4` |
| 채팅 active | `bg-primary text-primary-foreground` (큰 옥스블러드) | `bg-accent text-foreground` (옥스블러드 lighter) |
| 사용자 아바타 | `w-8 h-8 bg-secondary` | `h-7 w-7 bg-accent text-accent-foreground` (브랜드 family) |
| 날짜 | `text-xs` | `text-xs tabular-nums` |

## 4. 검증

### 4.1 자동 검증

```
=== build (typecheck + vite build) ===
✓ built in 7.68s

=== audit:tokens ===
[TYPOGRAPHY] T1 임의값 text-[Npx] 금지 … OK
[SPACING]    S2 half step (1.5) 금지 … OK
[SPACING]    S3 sub-grid (0.5) 금지 … OK
[RADIUS]     R3 rounded (no suffix) 금지 … OK
[ELEVATION]  E2 큰 shadow blur (xl/2xl) 금지 … OK
✅ 모든 룰 정합
```

### 4.2 부가 정리 (audit 진행 중 발견된 다른 작업 잔존)

본 turn 의 변경은 아니지만 audit 위반 발견 → 같이 정리 (회귀 0):

| 파일 | 위반 | 수정 |
|---|---|---|
| `Markdown.tsx` | `space-y-0.5`·`py-0.5` (S3) · `rounded` (R3) | `space-y-1`·`py-1`·`rounded-sm` |
| `Attachments.tsx` | `gap-1.5` (S2) | `gap-2` |
| `SlideView.tsx` | `text-[10px]` (T1) | `text-2xs` |
| `SlideView.test.ts` | `slides[N].xxx` type errors | non-null assertion `slides[N]!.xxx` |
| `TrendPage.tsx` `CreativePage.tsx` | `data.client` undefined | `data?.client` |

## 5. 진단 — 본 turn 의 visual 효과

이전 turn 에서 사용자 지적 "똑같아 보이지" 에 대한 답:
- 본 turn 변경 = **명확한 visual 변화 5건** (의도적 강도 ↑):
  1. **Sidebar 다크 → 라이트** (가장 큰 변화) — 어두운 strip 이 사라지고 페이지 전체 warm beige 통일
  2. **Sidebar 펼치기 가능** — 클릭 시 라벨 표시, UX 변화
  3. **사용자 메시지 옥스블러드 면 사라짐** — 강한 빨간 배경이 좌측 strip 으로
  4. **OctorAD 로고 = ◈ + 텍스트 + 옥스블러드 dot** — 브랜드 식별 추가
  5. **AI 채팅 active 버튼 톤다운** — 옥스블러드 → accent lighter

→ 이전 turn 들 (디자인 시스템 박제) 이 **미세** 였다면 본 turn 은 **인지 가능 변화**.

## 6. 진단 — AI 어감 잔존 가능성

| 영역 | 현 상태 | 잔존 위험 |
|---|---|---|
| 본문 (트렌드·monthly 등) | 디자인 시스템 정합 | ✅ 낮음 |
| Sidebar | 라이트 통일 + accent active | ✅ 낮음 |
| TopBar | Brand + 토큰 정합 | ✅ 낮음 |
| 채팅창 영역 | 토큰 정합 + strip 사용자 메시지 | ✅ 낮음 |
| 작업 단계 카드 (ChatTodoCard) | 토큰 정합 | ✅ 낮음 |

→ Tailwind 외부 색 0 잔존 (agent feature 한정). 디자인 시스템 외 색 사용은 brand color 와 차트 도메인 한정.

## 7. 사용자 의사 결정 항목 (보고 후)

다음 결정 가능한 옵션들:

| 결정 | 이슈 |
|---|---|
| **(a)** Sidebar 디폴트 expanded? | 현재 collapsed default. expanded 디폴트로 변경 가능 (사용자 학습성 ↑) |
| **(b)** Brand mark 변경? | 현재 ◈ Diamond. ▲ Triangle / ⬡ Hexagon / ● Circle 등 변경 가능 |
| **(c)** Brand size 키울지? | 현재 size='sm' (text-base). 'md' (text-lg) 로 더 강조 가능 |
| **(d)** 채팅창 width / position 변경? | 현재 resize 가능한 우측 패널. 별도 결정 |
| **(e)** Sidebar active 의 `bg-accent` 강도? | 현재 `--accent` (옥스블러드 lighter). `bg-primary/8` 등으로 더 절제 가능 |
| **(f)** 사용자 메시지 strip 두께? | 현재 `border-l-2` (2px). 4px / 1px 변경 가능 |
| **(g)** 다음 작업 | 데이터/백엔드 작업으로 이동 / 빈 페이지 (Settings·AgentChat·Workflow) 완성 / 또는 다른 영역 |

## 8. 자취

- **2026-06-09 (계획서)** : `외부영역_채팅창_브랜드_정합_계획서_2026-06-09.md`
- **2026-06-09 (적용)** : 본 보고서 — 5 Phase 적용 + 검증 통과 + 잔존 audit 정리
