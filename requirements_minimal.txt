# Minimal requirements for quick setup
# Core only - add other packages as needed

# Core Framework
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-dotenv==1.0.1
pydantic==2.9.0
pydantic-settings==2.4.0

# LangGraph & LangChain (let pip resolve versions)
langgraph
langchain
langchain-anthropic
langchain-openai
langchain-community

# Essential HTTP & Network
httpx==0.27.0
python-multipart==0.0.9

# Logging
loguru==0.7.2

# For HuggingFace models (optional - install separately if needed)
# sentence-transformers
# transformers
# torch