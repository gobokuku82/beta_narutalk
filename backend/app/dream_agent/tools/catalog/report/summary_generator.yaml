name: summary_generator
description: "분석 결과를 한 문장(요약)으로 압축 (LLM)"
version: "0.1.0"
category: report

parameters:
  - name: max_length
    type: integer
    required: false
    default: 100

timeout_sec: 30
max_retries: 2
requires_approval: false

dependencies: []

produces:
  - summary

has_cost: true
estimated_cost: 0.005

tags: [summary, content, llm, shared]
