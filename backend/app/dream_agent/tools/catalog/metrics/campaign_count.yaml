name: campaign_count
description: "총 캠페인 수 — campaigns 행수 (period 무관). K10."
version: "0.1.0"
category: metrics

parameters:
  - {name: label, type: string, required: false, default: "총 캠페인 수", description: "표시 라벨"}
  - {name: unit, type: string, required: false, default: "", description: "단위"}

timeout_sec: 30
max_retries: 0
produces: [value, op, field]
has_cost: false
estimated_cost: 0.0
tags: [campaign, metric, kpi, K10]
