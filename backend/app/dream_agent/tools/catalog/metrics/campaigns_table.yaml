name: campaigns_table
description: "캠페인 행 목록 (T04 테이블). 선택적 columns projection."
version: "0.1.0"
category: metrics

parameters:
  - {name: columns, type: array, required: false, description: "projection 컬럼 (없으면 전체)"}

timeout_sec: 30
max_retries: 0
produces: [rows, count]
has_cost: false
estimated_cost: 0.0
tags: [campaign, table, dashboard_v1]
