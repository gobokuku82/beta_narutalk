name: review_keywords
description: "리뷰 키워드 랭킹 Top-N (C12) — ml_model.extract_keywords (POC=Mock)."
version: "0.1.0"
category: analysis

parameters:
  - {name: n, type: integer, required: false, default: 10}

timeout_sec: 30
max_retries: 0
produces: [rows, count]
has_cost: false
estimated_cost: 0.0
tags: [trend, keyword, ml_model]
