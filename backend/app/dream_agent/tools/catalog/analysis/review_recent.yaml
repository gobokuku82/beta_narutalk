name: review_recent
description: "최근 리뷰 카드 N건 (O03) — 작성일 desc sort + slice."
version: "0.1.0"
category: analysis

parameters:
  - {name: n, type: integer, required: false, default: 6}

timeout_sec: 30
max_retries: 0
produces: [rows, count]
has_cost: false
estimated_cost: 0.0
tags: [trend, review, card]
