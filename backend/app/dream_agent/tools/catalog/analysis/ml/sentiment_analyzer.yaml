name: sentiment_analyzer
description: "감성 분석 (POC: 정규화된 라벨 우선 + 규칙 기반 폴백, MVP: KoBERT)"
version: "0.1.0"
category: analysis

parameters: []

timeout_sec: 60
max_retries: 1
requires_approval: false

dependencies:
  - text_preprocessor

produces:
  - sentiment_distribution
  - sentiment_items

has_cost: false
estimated_cost: 0.0

tags: [analysis, ml, sentiment]
