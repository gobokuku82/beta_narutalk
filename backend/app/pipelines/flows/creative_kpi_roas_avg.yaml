name: creative_kpi_roas_avg
visualization_id: K20
category: creative
description: "평균 소재 ROAS — creatives.roas 평균"
methodology_id: M01
priority: P0
client: ${client}

trigger:
  type: manual

steps:
  - id: roas_avg
    tool: creative_roas_avg
    inputs:
      client: ${client}
      label: "평균 ROAS"
      unit: "%"

validator:
  schema: MetricScalarOutput
  expected:
    value_min: 0
  fail_policy: alert

cache:
  layer: computed
  key_template: "creative_roas_avg.json"
  ttl_seconds: null

owner: developer
tags: [creative, kpi, roas]
