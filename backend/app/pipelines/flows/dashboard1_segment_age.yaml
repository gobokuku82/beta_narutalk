name: dashboard1_segment_age
visualization_id: C03
category: dashboard1
description: "연령 5세 bucket 분포 (S037, period 없음)"
methodology_id: M04
priority: P0
client: ${client}

trigger:
  type: manual

steps:
  - id: age_segment_compute
    tool: age_segment
    inputs:
      client: ${client}

validator:
  schema: AgeSegmentOutput
  fail_policy: alert

cache:
  layer: computed
  key_template: "S037_age_segment.json"
  ttl_seconds: null

owner: developer
tags: [segment, age, dashboard1]
