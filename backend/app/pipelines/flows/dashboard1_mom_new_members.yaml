name: dashboard1_mom_new_members
visualization_id: M04
category: dashboard1
description: "전월 대비 신규 가입 비교 (S069 MoM)"
methodology_id: M02
priority: P0
client: ${client}
period_a: ${period_a}
period_b: ${period_b}

trigger:
  type: manual

steps:
  - id: mom_new_members_compute
    tool: new_members_mom
    inputs:
      client: ${client}
      period_a: ${period_a}
      period_b: ${period_b}

validator:
  schema: NewMembersMomOutput
  fail_policy: alert

cache:
  layer: computed
  key_template: "S069mom_new_members_${period_a}_to_${period_b}.json"
  ttl_seconds: null

owner: developer
tags: [mom, new_members, dashboard1]
