name: dashboard1_kpi_cac
visualization_id: K04
category: dashboard1
description: "전체 CAC KPI — 광고비/신규회원 (S032)"
methodology_id: M01
priority: P0
client: ${client}
period: ${period}

trigger:
  type: manual

steps:
  - id: cac_compute
    tool: cac_overall
    inputs:
      client: ${client}
      period: ${period}

validator:
  schema: CacOutput
  expected:
    value_min: 0
  fail_policy: alert

cache:
  layer: computed
  key_template: "S032_cac_overall_${period}.json"
  ttl_seconds: null

owner: developer
tags: [cac, kpi, dashboard1]
