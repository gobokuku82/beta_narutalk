name: cost_bar_budget_stacked
visualization_id: C10
category: cost
description: "구분 × 채널 예산 누적 stacked bar"
methodology_id: M10
priority: P0
client: ${client}

trigger:
  type: manual

steps:
  - id: budget_stacked
    tool: budget_stacked
    inputs:
      client: ${client}

validator:
  schema: BudgetStackedOutput
  expected:
    rows_min: 1
  fail_policy: alert

cache:
  layer: computed
  key_template: "budget_stacked.json"
  ttl_seconds: null

owner: developer
tags: [cost, bar, stacked]
