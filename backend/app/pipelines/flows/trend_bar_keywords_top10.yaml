name: trend_bar_keywords_top10
visualization_id: C12
category: trend
description: "키워드 랭킹 Top 10 — ml_model.extract_keywords (POC=Mock fixture)"
methodology_id: M12
priority: P0
client: ${client}

trigger:
  type: manual

steps:
  - id: keywords
    tool: review_keywords
    inputs:
      client: ${client}
      n: 10

validator:
  schema: KeywordsTopNOutput
  expected:
    rows_min: 1
  fail_policy: alert

cache:
  layer: computed
  key_template: "keywords_top10.json"
  ttl_seconds: null

owner: developer
tags: [trend, keyword, ml_model]
