name: cost_kpi_keyword_metrics
visualization_id: K24
category: cost
description: "키워드 평균 ROAS + 운영 수 (keyword_performance)"
methodology_id: M01
priority: P0
client: ${client}

trigger:
  type: manual

steps:
  - id: keyword_metrics
    tool: keyword_metrics_avg
    inputs:
      client: ${client}

validator:
  schema: KeywordMetricsOutput
  expected:
    value_min: 0
  fail_policy: alert

cache:
  layer: computed
  key_template: "keyword_metrics_avg.json"
  ttl_seconds: null

owner: developer
tags: [cost, kpi, keyword]
