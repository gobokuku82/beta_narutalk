name: dashboard_v1_kpi_target_roas_avg
visualization_id: K13
category: dashboard_v1
description: "평균 목표 ROAS — campaigns.target_roas 평균"
methodology_id: M01
priority: P0
client: ${client}

trigger:
  type: manual

steps:
  - id: target_roas_avg
    tool: campaign_target_roas_avg
    inputs:
      client: ${client}
      label: "평균 목표 ROAS"
      unit: "%"

validator:
  schema: MetricScalarOutput
  expected:
    value_min: 0
  fail_policy: alert

cache:
  layer: computed
  key_template: "campaign_target_roas_avg.json"
  ttl_seconds: null

owner: developer
tags: [campaign, kpi, roas, dashboard_v1]
