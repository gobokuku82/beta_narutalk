name: dashboard_v1_kpi_campaign_active
visualization_id: K11
category: dashboard_v1
description: "진행중 캠페인 수 — status=='active' 행수"
methodology_id: M01
priority: P0
client: ${client}

trigger:
  type: manual

steps:
  - id: campaign_active_count
    tool: campaign_active_count
    inputs:
      client: ${client}
      label: "진행중 캠페인 수"

validator:
  schema: MetricScalarOutput
  expected:
    value_min: 0
  fail_policy: alert

cache:
  layer: computed
  key_template: "campaign_count_active.json"
  ttl_seconds: null

owner: developer
tags: [campaign, kpi, dashboard_v1, filter]
