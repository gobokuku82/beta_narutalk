name: dashboard1_segment_unknown_share
visualization_id: O02
category: dashboard1
description: "알수없음 채널 매출비중 (S054)"
methodology_id: M01
priority: P0
client: ${client}
period: ${period}

trigger:
  type: manual

steps:
  - id: unknown_share_compute
    tool: unknown_revenue_share
    inputs:
      client: ${client}
      period: ${period}

validator:
  schema: UnknownShareOutput
  expected:
    value_min: 0
  fail_policy: alert

cache:
  layer: computed
  key_template: "S054_unknown_share_${period}.json"
  ttl_seconds: null

owner: developer
tags: [segment, unknown, share, dashboard1]
