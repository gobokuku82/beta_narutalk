name: dashboard1_kpi_promotion_revenue
visualization_id: K05
category: dashboard1
description: "프로모션 매출 KPI — 활성 + promotion_code != NULL (S002)"
methodology_id: M01
priority: P0
client: ${client}
period: ${period}

trigger:
  type: manual

steps:
  - id: promotion_revenue_compute
    tool: promotion_revenue
    inputs:
      client: ${client}
      period: ${period}

validator:
  schema: PromoRevenueOutput
  expected:
    value_min: 0
  fail_policy: alert

cache:
  layer: computed
  key_template: "S002_promotion_revenue_${period}.json"
  ttl_seconds: null

owner: developer
tags: [promotion, revenue, kpi, dashboard1]
