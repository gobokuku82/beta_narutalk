name: dashboard1_kpi_promotion_roas
visualization_id: K06
category: dashboard1
description: "프로모션 ROAS KPI — 프로모션매출/광고비 (S005)"
methodology_id: M01
priority: P0
client: ${client}
period: ${period}

trigger:
  type: manual

steps:
  - id: promotion_roas_compute
    tool: promotion_roas
    inputs:
      client: ${client}
      period: ${period}

validator:
  schema: PromoRoasOutput
  expected:
    value_min: 0
  fail_policy: alert

cache:
  layer: computed
  key_template: "S005_promotion_roas_${period}.json"
  ttl_seconds: null

owner: developer
tags: [promotion, roas, kpi, dashboard1]
