name: creative_kpi_ctr_avg
visualization_id: K19
category: creative
description: "평균 소재 CTR — creatives.ctr 평균"
methodology_id: M01
priority: P0
client: ${client}

trigger:
  type: manual

steps:
  - id: ctr_avg
    tool: creative_ctr_avg
    inputs:
      client: ${client}
      label: "평균 CTR"
      unit: "%"

validator:
  schema: MetricScalarOutput
  expected:
    value_min: 0
  fail_policy: alert

cache:
  layer: computed
  key_template: "creative_ctr_avg.json"
  ttl_seconds: null

owner: developer
tags: [creative, kpi, ctr]
