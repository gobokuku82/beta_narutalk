name: dashboard_v1_kpi_campaign_total
visualization_id: K10
category: dashboard_v1
description: "총 캠페인 수 — campaigns 행수 (period 무관)"
methodology_id: M01
priority: P0
client: ${client}

trigger:
  type: manual

steps:
  - id: campaign_count
    tool: campaign_count
    inputs:
      client: ${client}
      label: "총 캠페인 수"

validator:
  schema: MetricScalarOutput
  expected:
    value_min: 0
  fail_policy: alert

cache:
  layer: computed
  key_template: "campaign_count_total.json"
  ttl_seconds: null

owner: developer
tags: [campaign, kpi, dashboard_v1]
