name: dashboard1_kpi_new_members
visualization_id: K07
category: dashboard1
description: "신규 회원 수 KPI — customers.signup_date prefix (S069)"
methodology_id: M01
priority: P0
client: ${client}
period: ${period}

trigger:
  type: manual

steps:
  - id: new_members_compute
    tool: new_members_monthly
    inputs:
      client: ${client}
      period: ${period}

validator:
  schema: NewMembersOutput
  expected:
    value_min: 0
  fail_policy: alert

cache:
  layer: computed
  key_template: "S069_new_members_${period}.json"
  ttl_seconds: null

owner: developer
tags: [new_members, kpi, dashboard1]
