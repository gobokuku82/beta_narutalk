name: dashboard1_kpi_roas
visualization_id: K03
category: dashboard1
description: "전체 ROAS KPI — 매출/광고비 (S004)"
methodology_id: M01
priority: P0
client: ${client}
period: ${period}

trigger:
  type: manual

steps:
  - id: roas_compute
    tool: roas_overall
    inputs:
      client: ${client}
      period: ${period}

validator:
  schema: RoasOutput
  expected:
    value_min: 0
  fail_policy: alert

cache:
  layer: computed
  key_template: "S004_roas_overall_${period}.json"
  ttl_seconds: null

owner: developer
tags: [roas, kpi, dashboard1]
