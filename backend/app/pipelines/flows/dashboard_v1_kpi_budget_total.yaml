name: dashboard_v1_kpi_budget_total
visualization_id: K12
category: dashboard_v1
description: "총 월예산 — campaigns.monthly_budget 합산"
methodology_id: M01
priority: P0
client: ${client}

trigger:
  type: manual

steps:
  - id: budget_total
    tool: campaign_budget_total
    inputs:
      client: ${client}
      label: "총 월예산"
      unit: KRW

validator:
  schema: MetricScalarOutput
  expected:
    value_min: 0
  fail_policy: alert

cache:
  layer: computed
  key_template: "campaign_budget_total.json"
  ttl_seconds: null

owner: developer
tags: [campaign, kpi, budget, dashboard_v1]
