name: dashboard1_kpi_aov
visualization_id: K08
category: dashboard1
description: "객단가 KPI — 매출/주문수 (S048)"
methodology_id: M01
priority: P0
client: ${client}
period: ${period}

trigger:
  type: manual

steps:
  - id: aov_compute
    tool: aov_monthly
    inputs:
      client: ${client}
      period: ${period}

validator:
  schema: AovOutput
  expected:
    value_min: 0
  fail_policy: alert

cache:
  layer: computed
  key_template: "S048_aov_${period}.json"
  ttl_seconds: null

owner: developer
tags: [aov, kpi, dashboard1]
